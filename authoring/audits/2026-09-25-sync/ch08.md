---
orphan: true
---

## Sync Audit — Ch08: Strings and Text

Audited on 2026-09-25 against the working tree (including today's uncommitted edits to `0802-regex.ipynb`, `ORGANIZATION.md`, and `MATERIALS.md`). Cell numbers are 0-based notebook cell indices.

Files read: `chapters/08-strings/ORGANIZATION.md`, `MATERIALS.md`, `0800-strings.ipynb`, `0801-strings.ipynb` (183 cells), `0802-regex.ipynb` (131 cells), `0803-text-analysis.ipynb` (182 cells), `assignments/{preview,lab,homework}.ipynb`, `_html_extra/chapters/08-strings/overview.md`, `_toc.yml` (lines 115-128). Earlier chapters (01-07) were searched to decide what counts as already taught.

### Planning docs ↔ chapter

✓ Front matter: both `ORGANIZATION.md` and `MATERIALS.md` start with `orphan: true`.
✓ Notebook list: 0800-0803 and `assignments/index|preview|lab|homework` are named in both docs and match `_toc.yml` lines 115-128. No extra notebooks are in the folder.
✓ Section sequence for 0801 and 0803 matches the `##`/`###` headings in order.
✓ Section sequence for 0802 matches the five `##` headings (Backslashes in Regex Patterns, The `re` Module, Metacharacters, Advanced Topics, Applications) and their subsections, including today's new opening section.
✗ Minor 0802 doc gaps: ORGANIZATION.md does not mention the `### Quick Reference` subsection (0802 cell 130). It describes Applications > Cleaning Text as "cleaning text", but that subsection also teaches `find_first`/`count_matches` over *Dracula* and demonstrates `|`, `^`, and `$` again (cells 94-107).
✓ Exercise lists: all 36 exercises match by name, count, and order: 0801 (15), 0802 (11), 0803 (10). Each is followed by a `hide-input` solution. MATERIALS.md defers to ORGANIZATION.md for the list, so the two docs agree.
✗ Required core / enrichment: neither doc separates required-core topics from enrichment, so that check cannot be applied. Several sections re-teach topics from earlier chapters without marking them as review: Docstrings (0801 cells 155-157; already taught in 0102 and 0402), `Counter` (0803 cell 60; already taught in 0703 cells 24-28), and optional/default parameters (0803 cells 65-74; already taught in 0401 cells 40-53). The docs should say whether these are review or core.
✗ Topic taught only in exercises: `str.casefold()` never appears in any text or example cell. It appears only in 0801 exercise solution cell 71 (Case Methods), exercise cell 129 and solution cell 130 (String Comparison, where steps 3-4 require it), and lab Question 3. The text teaches only `.lower()` (0801 cells 69, 127-128).
✗ Exercises and examples in 0802 that use syntax before the text teaches it. This is the main content problem in the chapter:
  - Cell 9 (Backslashes in Regex Patterns): `import re` and `re.findall` are used before `## The re Module` (cell 13) introduces them. This is minor, because cell 8 explains `\b`.
  - Cell 19 (note under The `Match` object): uses `r'\$[\d.]+'` before escapes, `\d`, and character classes are taught (cells 31, 43-46).
  - Cell 22: `'is.?'`, `re.IGNORECASE`, and `re.DOTALL`, with no prose, come before Metacharacters (cell 28) and Flags (cell 70).
  - Cells 23-24: the markdown says "The `+` in the pattern means one or more", but cell 22 has no `+`. Cell 24 then uses `\$`, `[\d.]+`, `\s+`, `re.sub`, and `re.split` before any metacharacter is taught.
  - Cells 25-26, Exercise: The Match Object: finding "the first 4-digit number" requires `\d{4}`. `\d` is defined in cell 46 and `{n}` in cell 34, both after this exercise. (Confirmed; still stands.)
  - Cell 35 (Quantifiers): uses `\d{3}` and `\d{2,4}` before `\d` is defined (cell 46). The only earlier mention is the example column of the metacharacter table in cell 28.
  - Cell 38 (Greedy vs Non-greedy): the first demo is an email extraction with `flags=re.IGNORECASE` and the character classes `[a-z0-9._%+-]`. It comes before Character Classes (cell 43) and Flags (cell 70), and it does not illustrate greediness. The greedy/lazy tag demo also duplicates cells 35-36. (Confirmed; still stands.)
  - Cell 42 (Anchors): uses `\w` (defined in cell 46) and `re.MULTILINE` (Flags, cell 70).
  - Cell 57 (Groups & Capturing): the replacement string `r"\2/\3/\1"` (backreferences) is never explained.
  - Cell 65 (Alternation): uses `re.IGNORECASE` before Flags (cell 70).
  - Cell 71 (Flags): uses `re.compile` before Compiled Patterns (cell 74).
  - Cells 76-77, Exercise: Compiled Patterns: the task is True/False validation, and the solution uses `email_re.fullmatch(e)`. `re.fullmatch()` is taught in cell 126, and the compiled-pattern methods are never listed.
  - Cell 108, Exercise: Download and Clean Text: uses `Path.exists()` and `.stat().st_size`, which the chapter never teaches. This depends on the planned Files section, so it is not counted, but it should move with that section.
✗ Other untaught syntax outside 0802:
  - 0801 cell 182 (Word List Application solution): `sum(1 for w in ...)` and `sum(len(w) for w in ...)` are generator expressions, which are taught in ch11 (`1102-generators`).
  - 0803 cell 22 is a visible, untagged setup cell that defines `split_line`, the `unicodedata` punctuation scan, `clean_word`, and a `.get()` word counter before cells 23-41 teach them. The same block is repeated visibly in cells 50, 100, and 146. It should be tagged `remove-input`/`hide-input` or moved out of the reading flow.
✓ Learning objectives: ORGANIZATION.md has no learning-objectives section, so there is nothing to conflict with the landing page.
✓ ORGANIZATION.md and MATERIALS.md agree on section order, exercise lists, and assignments. The MATERIALS lab description matches the five lab questions.
✓ Material Files: `data/words.txt`, `data/pg345.txt`, `data/pg345_cleaned.txt`, `data/pg43.txt`, and `data/dr_jekyll.txt` all exist. There is no `materials/` folder for ch08, and the archive path `materials/_archived/ch09-strings-split/` exists.
✗ Stale references in 0803:
  - Cell 3 promises that "as an exercise, you'll have a chance to make a more general version" of the Markov generator. No such exercise exists; the only Markov exercises are cells 170 and 180.
  - Cell 4: "Instructions for downloading the book are in the notebook for this chapter" is a Think Python leftover; the download is in cell 6.
  - Cell 7: "We'll use `clean_file` from Chapter 8". This is Chapter 8, and `clean_file` comes from 8.2 (0802 cell 90).
  - Cell 64: "demonstrate a new feature -- optional parameters". Default parameters are taught in 0401.
  - 0802 cell 123: "uses some features we haven't seen" about `(?<!\.\s)`, but lookbehind is taught in cell 78.
✗ Content defects that change meaning (found during the audit):
  - 0802 cell 79: the comment `# numbers NOT followed by USD` on `re.findall(r"\d+(?!USD)", text)` misleads. The real output is `['10', '200', '30']` (verified), because backtracking lets `\d+` match partial numbers. Students will expect `['200']`.
  - 0802 cell 49: the `\s` table writes `` `\\s` ``, `` `\\n` ``, `` `\\t` ``, and so on. Inside code spans these render as double backslashes, which contradicts the backslash explanation that opens the notebook.
  - 0802 cell 13: the function table lists `re.fulmatch()` (misspelled). `re.match()` is listed there but never demonstrated.
  - 0802 cells 89-90: two conflicting `clean_file` definitions. The first uses `with` and skips `***` lines; the second immediately replaces it with manual `open`/`close` and a two-loop break. `is_special_line` in 0802 cell 91 also differs from the one in 0803 cell 8.

### Slide deck ↔ chapter

✗ Section headings: the deck has 6 content slides (String Methods, String Comparison & Sorting, `re` Module Basics, Key Regex Patterns, Word Frequencies & Counter, Quick Reference). They cover about 5-6 of the chapter's 15 `##` sections: String Methods, String Comparison, Looping and Sorting (sorting only), The `re` Module, Metacharacters (partly), and Word Frequencies. Nine sections have no slide: String Creation and Accessing (indexing, slicing, escapes, raw strings), Application: Word List, Backslashes in Regex Patterns (new today), Advanced Topics (flags, lookaround), Applications (Dracula, `re.sub`, `fullmatch`), Unique words, Punctuation, Random numbers/Bigrams, and Markov analysis. (Confirmed; still stands.) `overview.md` is dated Aug 31, so it predates today's 0802 changes.
✗ Learning goals: the deck has no learning-goals slide. Of the five goals in 0800 cell 3, goal 1 (create/index/slice) and goal 5 (random text generation) are not on any slide. (Confirmed; still stands.)
✗ Slide content that does not exist in the chapter:
  - The Quick Reference row "Markov | `{(w1,w2): [w3, ...]}` bigram model" (overview.md line 250). 0803 builds only a single-word `successor_map` (cells 157-165) and only mentions bigram keys in prose (cell 179). (Confirmed; still stands.)
  - `ord("A"), ord("a")` (line 99): `ord()` never appears in the chapter.
  - "compare lexicographically using Unicode code points" (line 94): 0801 cell 118 explains comparison with the ASCII table.
  - `\D`, `\W`, `\S` (lines 173-175): not taught in 0802.
  - `sorted(words, key=str.lower)` (line 115): the chapter never shows it; the exercises and lab use `key=str.casefold`.
  - The Counter slide tokenizes with `re.findall(r"\b[a-z]+\b", text.lower())`, and the manual version uses `strip(".,!?")` plus `lambda t: t[1]`. The chapter's pipeline is `split_line` + `clean_word` (with `unicodedata` punctuation) + `second_element` (0803 cells 23-55).
  - The phone example `555-1234` (line 153) is not in the chapter; 0802 cell 75 uses a `\d{3}[-.]\d{3}[-.]\d{4}` example.
✓ No slide references a removed or renamed topic. The mismatches above are omissions or examples that were never in the chapter.
✓ The rendered `overview.html` (Aug 31 15:43) is newer than `overview.md` (Aug 31 09:33).

### Preview ↔ chapter

✓ All 10 questions (q1-q10) map to glossary terms that the chapter teaches: string, escape sequence, raw string, indexing, slicing, splitting, joining, regular expression, Match object, bigram.
✓ No question tests a topic that moved to another chapter. Difficulty suits pre-reading.
(Note, not an issue: 9 of 10 questions target 0801/0802 vocabulary. 0803 is represented only by q10, and no question covers metacharacters, quantifiers, character classes, `Counter`, or Markov analysis, although all of those are glossary terms.)

### Homework ↔ chapter

✓ All ten problems use functions and patterns that exist in the current chapter: `strip`, `title`, `lower`, `split`, `replace`, `re.findall(r"\d+")`, dict counting with `.get`, and `join`. No problem refers to a removed example.
✗ Coverage: 0803 is assessed only through Q9 (a dict word count, which is really a ch07 pattern) and T/F Q5. Nothing assesses `Counter`, optional parameters, dictionary subtraction, `random.choices`/weights, bigrams, or successor maps/Markov generation. Regex coverage is a single `\d+` (Q8), with no groups, anchors, `re.sub`, or `fullmatch`. (Confirmed; still stands.)
✗ Difficulty ladder: the coding questions Q6-Q10 are all one- or two-line basics. Q10 (a single `join`) is easier than Q9, and nothing goes past Basic, so the ladder does not rise with the chapter's structure.

### Lab ↔ chapter

✓ Topic consistency: Q1-Q2 (cleaning and normalization) match 0801 Cleaning and Case Methods. Q4-Q5 (`findall`, `fullmatch`, capture groups) match 0802.
✓ Submission: the grader panel has 5 rows for the 5 questions, the instructions match the structure, and the MATERIALS.md lab description is accurate.
✗ Q3 depends on `key=str.casefold`, and `casefold` is never taught in the text (see Planning docs).
✗ Q4 solution uses the generator expression `sum(int(n) for n in numbers)`, which is taught in ch11. The prompt allows a loop, but the reference solution models untaught syntax.
✗ Coverage: no lab question touches 0803 (word frequencies, `Counter`, bigrams, Markov), which is a third of the chapter. (Confirmed; still stands.)

### Glossary ↔ content

✓ All 20 glossary terms in 0800 cell 5 are introduced in a content notebook.
✗ Key terms introduced (most in bold) but missing from the glossary. The earlier count of about 8 now stands at about 11 high-value terms:
  - **greedy / lazy quantifier** (0802 cells 34-36)
  - **capturing group**, **named group**, **non-capturing group** (0802 cell 56)
  - **anchor** and word boundary (0802 cell 41)
  - **flag** (0802 cell 70)
  - **lookahead / lookbehind** (0802 cell 78)
  - **alternation** (0802 cells 62-63)
  - **string substitution** (0802 cell 118)
  - **f-string** / **format spec** (0801 cells 96-98)
  - **concatenation** / **repetition** (0801 cell 52)
  - **pseudorandom** / **deterministic** (0803 cell 98)
  - **n-gram** / **trigram** (0803 cell 124)
  - **successor map** (0803 cells 148-157)

  Also bolded but lower priority: immutable, dot notation, invocation, search pattern, regex engine, default value.

### Summary

Sync issues: 23 (plus 4 content defects listed under Planning docs)
  - 0802 ORGANIZATION description omits Quick Reference and under-describes Cleaning Text
  - Docs have no required-core/enrichment split; re-taught topics (docstrings, Counter, default params) are not marked as review
  - `casefold` appears only in 0801 exercises and lab Q3, never in the text
  - 0802 Match Object exercise needs `\d{4}` before `\d`/`{n}` are taught
  - 0802 cells 19/22/24 use metacharacters, flags, `re.sub`/`re.split` before Metacharacters, with a mismatched "+" explanation
  - 0802 Quantifiers demo uses `\d` before Character Classes
  - 0802 Greedy vs Non-greedy demo uses IGNORECASE and character classes before they are taught, and is off-topic and duplicated
  - 0802 Anchors and Alternation use `\w`, MULTILINE, and IGNORECASE before Flags/Character Classes
  - 0802 Flags demo uses `re.compile` before Compiled Patterns; Compiled Patterns exercise uses `fullmatch` before it is taught
  - 0802 backreferences `\2/\3/\1` never explained
  - 0801 Word List solution uses generator expressions (ch11)
  - 0803 visible setup cells (22, 50, 100, 146) show the pipeline before it is taught
  - 0803 cell 3 promises a Markov exercise that does not exist
  - 0803 cells 4, 7, 64 and 0802 cell 123 contain stale references
  - Slides cover only about 6 of 15 `##` sections (Backslashes section and all of 0803 except Word Frequencies missing)
  - Slides have no learning-goals slide
  - Slide Quick Reference shows a `(w1, w2)` Markov model that 0803 does not build
  - Slide examples not in chapter (`ord`, `\D\W\S`, `key=str.lower`, Unicode code points, regex-tokenized Counter)
  - Homework barely assesses 0803 and regex
  - Homework difficulty ladder is flat
  - Lab Q3 depends on untaught `casefold`
  - Lab Q4 solution uses generator expression
  - Lab has no 0803 task
  - Glossary missing about 11 key terms

  Content defects (not counted above): misleading negative-lookahead comment (0802 cell 79), `\\s`/`\\n` double backslashes in the 0802 cell 49 table, `re.fulmatch` typo (0802 cell 13), and conflicting duplicate `clean_file` definitions (0802 cells 89-90).

Previous-audit findings rechecked: all four still stand (slides cover about 5-6 of 15 sections with no goals slide; the `(w1, w2)` Markov Quick Reference is unbuilt; homework and lab barely assess 0803; glossary gaps have grown from about 8 to about 11 terms). Today's 0802 rewrite fixed the escape ordering in the opening section, but the Match Object exercise and the Greedy demo still use syntax before it is taught.

### Top fixes

1. **Resequence 0802 so no example or exercise uses untaught syntax.** Change the Match Object exercise (cell 25) to a literal or `[0-9]` pattern, or move it after Character Classes. Replace cells 19, 22, and 24 with literal-pattern demos, or move them into Metacharacters. Move the email/IGNORECASE demo (cell 38) into Flags. Swap `re.IGNORECASE`/`MULTILINE` out of Anchors (cell 42) and Alternation (cell 65). Move Character Classes ahead of Quantifiers so `\d` and `\w` are defined before `\d{3}`. Explain backreferences at cell 57.
2. **Rebuild the slide deck to follow the 15 `##` sections.** Add a learning-goals slide that mirrors 0800 cell 3, a Backslashes/raw-string slide, and slides for Advanced Topics, Applications, Unique words/Punctuation, Random/Bigrams, and Markov analysis. Replace the `(w1, w2)` row with the single-word `successor_map`, and remove `ord`, `\D\W\S`, and the regex-tokenized Counter example. Regenerate `overview.html` afterwards.
3. **Add 0803 assessment.** Add at least one lab question and two homework coding questions covering `Counter.most_common`, bigram counting, and a successor-map lookup or generation step. Add one homework regex question using groups, `re.sub`, or `fullmatch`, and reorder the homework coding questions from easy to hard.
4. **Teach `casefold` in the text** (a short 0801 Case Methods or String Comparison example next to `.lower()`), or change the exercises and lab Q3 to `str.lower`. At the same time, replace generator expressions in 0801 cell 182 and lab Q4 with loops.
5. **Fix 0803 and 0802 correctness and staleness.** Hide or remove the visible pipeline cells (0803 cells 22, 50, 100, 146). Correct the negative-lookahead comment in 0802 cell 79 (or use `\d+(?!\d|USD)`, which returns `['200']`), and fix the `\\s` table, the `re.fulmatch` typo, and the duplicate `clean_file`. Remove the stale lines in 0803 cells 3, 4, 7, and 64. Add the ~11 missing glossary terms.
