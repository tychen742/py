---
orphan: true
---

## Sync Audit — Ch12: APIs

Files audited (working tree, 2026-09-25): `chapters/12-apis/{ORGANIZATION.md, MATERIALS.md, 1200-apis.ipynb, 1201-apis.ipynb, 1202-api-reliability.ipynb}`, `chapters/12-apis/assignments/{preview,lab,homework}.ipynb`, `_html_extra/chapters/12-apis/overview.md`, `_toc.yml` lines 181-196.

Note on exercise format: neither content notebook uses `### Exercise` / `### === EXERCISE` cells. Practice items are `### Checkpoint: ...` markdown headings followed by a `thebe-interactive` code cell (`### Your code starts here`) and a `hide-input` `### Solution` cell. They are treated as the chapter's exercises below. 1201 has 2 (cells 12-14, 21-23); 1202 has 3 (cells 6-8, 15-17, 27-29).

### Planning docs ↔ chapter

✗ Front matter — neither `ORGANIZATION.md` nor `MATERIALS.md` starts with MyST front matter `orphan: true`. Both open directly with a `#` heading. (Chapter 11's `ORGANIZATION.md` has the same gap, so this may be book-wide.)
✓ Notebook inventory — the docs name 1200, 1201, 1202 and the four assignment notebooks. All exist and match `_toc.yml` 181-196. There are no other notebooks in the folder.
✓ Material files — neither doc has a Material Files section, and the repo has no `materials/…12…` folder. Nothing to reconcile.
✓ Section sequence, 1201 — ORGANIZATION lists: what APIs are, methods/status codes, `requests.get()`/`response.json()`, query parameters, error handling, weather checkpoint. This matches the `##` headings in 1201 (What Is an API?, Making GET Requests, Query Parameters, Error Handling, Weather API Pattern, Summary).
✗ Error-handling topics not taught, 1201 `## Error Handling` (cells 15-17) — both docs list `raise_for_status`, timeout, and `RequestException` as required core. `RequestException` appears nowhere in the chapter's notebooks. The timeout is only a single phrase ("should use a timeout") plus `timeout=10` in a code snippet. The worked example (cell 17) catches only `requests.HTTPError` and `ValueError`, and no network-error or timeout handling is shown. Notebook is incomplete relative to the required core.
✗ `requests.post()` pattern, 1202 `## POST Requests` (cells 3-5) — ORGANIZATION says "`requests.post()` pattern with JSON body (`json={}`)". `requests.post(` does not occur in any chapter notebook: the text only builds `requests.Request("POST", …, json=…).prepare()`. Also, `headers={}` (ORGANIZATION, MATERIALS "Using the `requests` library… `headers={}`") is never passed to a request. `headers=` occurs 0 times in 1201/1202, because cell 11 only builds a dict. The notebook teaches how to prepare requests but never shows these two call forms.
✗ `X-API-Key` pattern, 1202 `## Headers and API Keys` (cells 9-11) — ORGANIZATION lists "API key patterns (Bearer token, `X-API-Key`)". MATERIALS instead lists "query param vs. Bearer token header". The notebook teaches only Bearer. `X-API-Key` and the query-param key pattern both occur 0 times. The two docs disagree with each other and with the notebook.
✗ Pagination styles, 1202 `## Pagination Patterns` (cells 18-20) — ORGANIZATION's sequence lists "page-based, cursor-based, link-based". The notebook works through only page-based pagination. Cursor-based gets one sentence and link-based is never mentioned. The required core ("one pagination pattern") is satisfied, but the sequence line overstates the coverage. Either trim it or move cursor/link to enrichment.
✗ Enrichment not written — ORGANIZATION/MATERIALS enrichment lists a comparative page/cursor/link lab, advanced retry tuning / rate-limit handling / status-specific strategies with jitter, and an extended API-client mini-project with logging. None of these exist: jitter and rate limits each get one sentence in 1202 cell 21. They are not in `## Planned Additions`, so either the docs are stale or the topics should move under Planned Additions.
✗ Order of topics in 1202 — ORGANIZATION puts "authentication workflow and secret handling with environment variables" after nested JSON as its own item. In the notebook, secret handling sits inside `## Headers and API Keys` (cell 9, `os.environ.get` in cell 11), before `## Nested JSON Parsing`. The notebook has no separate "authentication workflow" content. The doc sequence is stale.
✗ Exercise lists — the docs name only the "weather payload parsing checkpoint" (1201) and "resilient API client exercise" (1202). The notebooks have 5 checkpoints: 1201 Query Parameters (cell 12) and Weather Payload (cell 21), and 1202 POST Payload (cell 6), Defensive Parsing (cell 15), and Resilient Client (cell 27). Docs are stale: the Query Parameters, POST Payload, and Defensive Parsing checkpoints are unlisted.
✗ "Resilient API client" scope mismatch — the docs describe a resilient-client exercise after retry/backoff. The 1202 Resilient Client checkpoint (cells 27-29) only adds key/type validation to the pagination loop and uses no retry. Either the doc description or the exercise should change.
✓ No topic taught only in an exercise — every checkpoint practices a worked example that comes before it (prepared GET in cell 11, weather extraction in cell 20, prepared POST in cell 5, chained `.get()` in cell 14, pagination and validation in cells 20/26). `isinstance` (cell 29 solution) was taught in earlier chapters (0102, 0203, ch09).
✗ Taught only in code, not explained in text — 1201 cell 11 introduces `requests.Request(...).prepare()`, `urlparse`, and `parse_qs` with no prose explanation. Prepared requests are the pattern every later checkpoint and 1202 example depends on, so they need at least one explanatory sentence in cell 9 or 10.
✗ Text/example contradiction, 1202 cell 9 vs cell 11 — the text says "read them from environment variables and fail clearly when they are missing". `build_headers()` instead silently falls back to `"demo-token"`. (The slides use `os.environ["MY_API_KEY"]`, which does fail clearly.)
✗ Learning objectives — ORGANIZATION has no learning-objectives section, so there is nothing to compare directly. However, the landing page goals (1200 cell 3, 5 goals) cover only 1201 content: API/REST, GET, JSON, query params, and errors. None covers any 1202 topic (POST, headers/API keys/secrets, nested JSON, pagination, retry/backoff, response validation), even though those are required core in both docs.
✓ ORGANIZATION ↔ MATERIALS — section order, file roles, and required core agree, apart from the API-key pattern discrepancy noted above.
✓ Stale references — no old chapter/section numbers or renamed files. (The Planned Additions entry pointing to a "planned Files section in Chapter 8" is a known plan and not flagged. Note, though, that file I/O currently lives in `chapters/appendices/packaging/0501-files.ipynb`.)

### Slide deck ↔ chapter

✗ Section headings — the slides have 4 content slides: "What is an API?", "`requests.get()` Pattern and JSON", "Authentication & API Keys" (plus POST), and "Nested JSON, Pagination, and Retry", followed by a quick reference. Missing chapter `##` sections: 1201 Query Parameters, Error Handling, and Weather API Pattern (params and `raise_for_status` appear only inside the GET code), and 1202 Response Contract Validation (not in the slides at all). "Pagination" is in a slide title (line 184) but that slide has no pagination code, only defensive parsing and retry. Order also differs: the slides put auth before POST, while 1202 has POST (cell 3) before Headers (cell 9).
✗ Learning goals — the deck has no learning-goals slide. None of the 5 landing-page goals appears as a goal list.
✗ Slide content not in the notebooks — the slides teach `requests.post(...)` (lines 171-176), `X-API-Key` (line 154), `headers=` passed to `requests.get` (line 156), and a retry that catches `requests.RequestException` (line 220). None of these appears in the chapter text: the notebook retry catches `requests.Timeout` only (1202 cell 23). The slides also list specific codes 201/401/429 (lines 74-78), while the notebook's status table uses ranges (1201 cell 3). This is minor, but these four items are really gaps in the notebooks (see planning findings), not slide errors.
✓ Key examples still present — the `DemoResponse` fixture (1201 cell 8), Alice/student POST payload (1202 cell 5), `user/profile/department` defensive `.get()` chain (1202 cell 17), and `Request(...).prepare()` (quick reference) all match notebook code.
✓ Rendered HTML — `overview.html` (Aug 31 15:43) is newer than `overview.md` (Aug 31 09:33).
✓ Closing slide "Next: Chapter 13 — Abstract Data Structures" matches `_toc.yml`.

### Preview ↔ chapter

✓ All 10 questions map to glossary terms taught in the chapter (API, HTTP, REST, endpoint, GET, POST, query parameter, JSON, pagination, exponential backoff). Nothing tests a moved topic, and the difficulty suits pre-reading.

### Homework ↔ chapter

✓ T/F Q1-Q5 (cells 1-10) map to current content (methods, 404, query params, nested JSON, API-key hygiene).
✗ Q6 "Build query parameters" (cell 11-12) — the expected solution builds the query string by hand (`"&".join(f"{key}={value}" …)`). That contradicts 1201 cell 9 ("Build them with the `params=` argument instead of manually concatenating strings") and the same homework's Q3, whose answer is False for "pasted into a URL string by hand". The chapter's taught tool is `requests.Request(..., params=...).prepare()`.
✗ Coverage and difficulty ladder — coding Q6-Q10 (cells 11-20) are all one- or two-liners: dict literal, ternary, direct indexing, `.get()`. No homework item exercises error handling (`raise_for_status`/try-except), a pagination loop, retry/backoff, or response validation, though these are required core in both docs. The ladder is flat and never reaches 1202-level difficulty. Q10's "optional `next` URL" hints at link-based pagination, which the chapter does not teach.
✓ MATERIALS homework description ("Build request parameters, parse response payloads, and store results in a dict or list") roughly matches Q6/Q8/Q9.

### Lab ↔ chapter

✓ Task list — MATERIALS' 5 lab tasks match lab Q1-Q5 exactly in name and order. Offline fixtures are consistent with the chapter's offline convention. The grader panel (cell 11) lists q1-q5, which matches the 5 questions.
✗ Q5 "Parse and Re-serialize a JSON Payload" (cells 9-10) needs `json.loads` / `json.dumps` and the JSON-string vs dict distinction. Neither is taught in Chapter 12's text (1201 cell 1 imports `json` but never uses it), nor in any earlier chapter's text. The only prior use is inside an "Advanced: mocking context managers" dropdown in `05-testing/0502-unit-testing.ipynb` cell 55. The lab uses untaught functions. (The Planned Additions `json.dump` file item would not cover `loads`/`dumps` from a string.)
✗ Q1 "Build a URL-Encoded Query String" (cells 1-2) asks students to hand-encode spaces as `%20` and concatenate `key=value` pairs "without using requests". That practices the pattern the chapter tells students to avoid (1201 cell 9). The only-spaces encoder is also incorrect for real input. Recast it to use `requests.Request(..., params=...).prepare()` as taught, or add a sentence explaining that it is a look at what `params=` does internally.
✗ Coverage — like the homework, the lab has no error-handling, retry/backoff, or response-validation task, so the two graded coding assignments never assess these required-core 1202 topics.
✓ Submission instructions (cell 0: sign in, Live Code, multiple submissions) match the lab structure.

### Glossary ↔ content

✓ 19 of 20 glossary terms (1200 cell 5) are introduced in 1201/1202.
✗ "Response body" (term 14) never appears in either content notebook. Only "request body" is mentioned (1202 cell 3).
✗ Key terms used in the notebooks but missing from the glossary: prepared request (1201 cell 11, used in every checkpoint), environment variable / secret (1202 cells 9, 11), Bearer token / `Authorization` header (1202 cells 9, 11), cursor-based pagination (1202 cell 18), jitter (1202 cell 21), transient failure (1202 cell 21), `HTTPError` (1201 cell 17).

### Summary
Sync issues: 26
  - ORGANIZATION.md and MATERIALS.md lack `orphan: true` front matter (2)
  - `RequestException`/timeout handling listed as required core but not taught (1201 Error Handling)
  - `requests.post()` and `headers=` listed in the docs but never shown in the notebooks
  - `X-API-Key` / query-param key pattern: docs disagree with each other and with the notebook (Bearer only)
  - Cursor/link pagination listed in the sequence but not taught
  - Enrichment items (pagination comparison, rate-limit/jitter retry, mini-project) unwritten and not under Planned Additions
  - 1202 topic order in ORGANIZATION (secret handling after nested JSON) is stale
  - Exercise lists name 2 of 5 checkpoints
  - "Resilient client" exercise has no retry, so it does not match its doc description
  - Prepared requests / `urlparse` / `parse_qs` introduced only in code
  - 1202 text says "fail clearly" but `build_headers` falls back silently
  - Landing-page learning goals omit all 1202 content
  - Slides missing Query Parameters, Error Handling, Weather, and Validation sections; pagination slide has no pagination content; POST/auth order swapped
  - Slides have no learning-goals slide
  - Slides teach `requests.post`, `X-API-Key`, `headers=`, and `RequestException`, none of which is in the notebooks
  - Homework Q6 contradicts chapter guidance and homework Q3 (manual query string)
  - Homework has no error/pagination/retry/validation items, and the difficulty ladder is flat
  - Lab Q5 uses untaught `json.loads`/`json.dumps`
  - Lab Q1 practices manual URL encoding, which the chapter discourages
  - Lab has no error/retry/validation task
  - Glossary "Response body" not in the notebooks
  - Glossary missing prepared request, environment variable, Bearer token, cursor, jitter, transient failure, HTTPError

### Top fixes
1. Extend 1200 learning goals (and add a matching goals slide) to cover the 1202 required core: POST/headers/secrets, nested JSON, pagination, retry/backoff, and response validation.
2. Teach `json.loads`/`json.dumps` in 1201 text (natural spot: next to `response.json()`), or drop them from lab Q5. Also explain `requests.Request(...).prepare()` in prose, since every checkpoint depends on it.
3. Complete 1201 `## Error Handling`: add a `requests.Timeout`/`RequestException` branch and prose on network vs HTTP errors, so the required core is actually taught (and so the slides' `RequestException` retry is backed by the text).
4. Align assignments with the chapter's guidance and required core: rewrite homework Q6 and lab Q1 to use `params=` via prepared requests, and add at least one error-handling or retry/validation coding item to the homework or lab.
5. Refresh the planning docs and slides: add `orphan: true`, list all 5 checkpoints, reconcile the API-key patterns and the `requests.post()`/`headers=` wording with the notebook (or add them to 1202), trim cursor/link pagination or move it to Planned Additions, and add slides for Query Parameters, Error Handling, and Response Validation.
