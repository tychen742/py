---
orphan: true
---

## Sync Audit — Ch03: Control Flow

Files audited (working tree, 2026-09-25): `chapters/03-control-flow/{ORGANIZATION.md, MATERIALS.md, 0300-control-flow.ipynb, 0301-conditionals.ipynb, 0302-iteration.ipynb, assignments/{preview,lab,homework}.ipynb}`, `_html_extra/chapters/03-control-flow/overview.md`, `_toc.yml` (lines 37-48). The `match`/`case` item in ORGANIZATION.md "## Planned Additions" is known and not flagged as missing.

Content notebook structure, for reference:
- 0301: cells 3-4 overview figure and control-flow table (no heading); `## if statements` (cell 5) with `### Conditional execution (if)` (7), `### Alternative Execution (else)` (15), `### Chained Conditional (elif)` (18), `### Nested Conditionals` (22); `## Conditional Expressions` (32). Exercises: cell 20 (`### Exercise:`, untitled, score grading), cell 30 (Nested Conditional), cell 40 (Conditional Expressions).
- 0302: `## Iterators and Iterables` (5); `## For Loops` (8) with `### Loop through Iterables` (11), `### Looping with range` (23), `### Index-Based for Loop` (25), `### Nested Loops` (27), `### Advanced for Loop` (34: enumerate, zip, dictionaries); `## while Loops` (44) with `### Counting while` (49), `### Sentinel while Loop` (52), `### while True loop` (57); `## Loop Control Statements` (66: break, continue, pass, loop `else`). Exercises: cell 21 (Basic For Loop), 32 (Star Pattern), 42 (Accumulator), 55 (`### Practice: Sentinel Loop`), 77 (Break and Continue).

### Planning docs ↔ chapter
✗ Front matter: neither `ORGANIZATION.md` nor `MATERIALS.md` starts with `orphan: true` front matter. Both open with the `# Chapter 03 ...` heading. Other chapters already have it (for example, `chapters/08-strings/MATERIALS.md`).
✓ Notebook inventory: both docs name 0300, 0301, 0302 and the four `assignments/` notebooks. All exist and match the `_toc.yml` entries.
✗ Listed but not taught (0301): both docs list "Boolean expressions" as a 0301 topic. 0301 only names them as a prerequisite (cell 6: "Some topics are essential to understand before you learn conditional statements: boolean expressions ... logical operators") and uses the term once in cell 9. They are taught in Ch02 (`0202-operators.ipynb`, `### Truthy/Falsy` cell 27 and `## Logical operators` cell 45). Either the doc is stale, or 0301 needs a short Boolean-expression recap section. The slide deck also has one ("Boolean Expressions Feed Branches").
✗ Taught but not listed (0302): the docs reduce 0302 to "for and while loops; break, continue; accumulator pattern". They leave out these taught sections: `## Iterators and Iterables` (`iter()`/`next()`, dunder `__iter__`/`__next__`, cells 5-7; this overlaps Ch11 `1101-iterators`, so the docs should say it is a preview), `range` counting (23), index-based `range(len())` loops (25), nested loops (27-31), `enumerate` (35-37), `zip` (38-39), dictionary iteration with `.items()/.keys()/.values()` (40-41), the three `while` patterns (counting, sentinel, `while True`, cells 45-62), the `do-while` comparison (63-65), `pass` (66, 72-73, 76), and the loop `else` clause (66, 74-75). In 0301 the docs also omit `pass` (cells 9-14), which is taught there and named in landing learning goal 3.
✗ Exercise-only topic: the docs list the "accumulator pattern" as a 0302 topic, but it appears only in the exercise at 0302 cell 42 (`# === EXERCISE: Accumulator ===`), with its solution in cell 43. No markdown cell explains initializing before the loop, updating inside it, and using the result after. Cell 62 (`total += number` in a `while True` example) uses a running total without naming or explaining it. Under the house rule the topic is not taught. Homework Q3, Q7, and Q9, lab Q3, glossary term 20, and two slides all depend on it (see below). Order is also off: the docs place the accumulator after break/continue, but the only occurrence sits inside `## For Loops`.
✗ Exercise lists: neither doc lists any exercises. The notebooks have 3 in 0301 (cells 20, 30, 40) and 5 in 0302 (cells 21, 32, 42, 55, 77). Header formats are also inconsistent. 0301 cell 20 is an untitled `### Exercise:`. 0302 cells 21, 32, 42, 77 use `# === EXERCISE:` (single `#`, not the `### === EXERCISE` form). 0302 cell 55 uses `### Practice: Sentinel Loop`, which matches neither exercise convention. The ORGANIZATION.md checklist says "at least one checkpoint exercise per major section", but `## Iterators and Iterables` has no exercise, and `## while Loops` has only the nonconforming "Practice" cell.
✗ Required core vs enrichment: neither doc separates required-core topics from enrichment topics. MATERIALS "Must-Have Teaching Materials" lists only conditionals, for/while, and break/continue. So iterators, `enumerate`/`zip`, dictionary iteration, loop `else`, `do-while` comparison, and conditional expressions are all unclassified.
✓ Learning objectives: ORGANIZATION.md has no learning-objectives section, so there is nothing to compare.
✓ ORGANIZATION.md and MATERIALS.md agree with each other: identical topic strings and delivery order.
✓ Material files: neither doc has a Material Files section, and there is no `materials/**/03*` folder. The chapter uses no data files.
✗ Stale assessment description (MATERIALS "Practice and Assessment Pack"):
  - Lab: the doc lists 4 tasks (loyalty tier, for-loop accumulator, savings goal, ternary shipping fee), but the lab has 5 questions. Lab Q1 (unit conversion with `//` and `%`) is not listed.
  - Homework: the doc lists "Flow chart for a multi-branch decision; accumulator loop problem". The homework has no flow-chart task. It has 5 true/false cards plus 5 coding questions (ticket routing, qualifying sales, budget `while`, pass/fail labels, multiplication row).
✗ Forward references in 0301 (syntax used before it is taught):
  - Cell 39 is a recursive `factorial` example. Recursion is taught in Ch04 (`0403-recursion`).
  - Exercise cell 40, part 2, asks students to rewrite a `def absolute_value(x)` with `return`. Function definitions are taught in Ch04 (`0401-functions`). `def` has appeared earlier only incidentally (Ch01 `0102-basic-syntax` docstring examples, cells 52-54; Ch02 `0202-operators` cell 56).
  - Cell 38 uses `math.log` and `float('nan')`, and neither is introduced earlier.
  - Cell 9 explains `if` by analogy to "function definitions", which students have not studied yet.
✗ Forward references in 0302:
  - Cell 41 uses `dict.items()` with no prose explanation. Ch02 `0203-builtin-types` cell 72 teaches only `.keys()` and `.values()`.
  - Cell 9 shows `for line in open('data.txt')`. File I/O is taught only in the appendix (`appendices/packaging/0501-files`).
✗ Executability (ORGANIZATION checklist: "Keep each notebook executable top-to-bottom"):
  - 0302 cell 54 reads `number` in the `while` test, but the line that assigns `number` is commented out, so it raises `NameError`. The `input()` inside the loop body is left uncommented.
  - Cell 60 tests `text`, but the `input()` line that assigns it is commented out, so it raises `NameError`.
  - Cell 62 calls `input()` uncommented, so it blocks or fails when the notebook runs without a user.

### Slide deck ↔ chapter
✓ Overall order follows the notebooks: title, control-flow families, 3.1 Conditionals (forms, if/else, elif trace, nested, conditional expressions), 3.2 Iteration (iterators, for patterns, nested, while patterns, loop control). `overview.html` (Sep 8 13:47) is newer than `overview.md` (Sep 8 13:38).
✗ Slides teach topics the notebooks do not. Here the notebook, not the slides, looks stale.
  - "Accumulator Pattern" and "Accumulators Can Do More Than Sum" teach total, count, product, and list collection, but the notebook has no accumulator text (see above).
  - "Boolean Pitfalls to Catch Early" (`x == "a" or "b"`, `=` vs `==`) has no counterpart in 0301.
  - "Avoiding Infinite Loops" (the checklist of initialize, condition, update) goes well beyond 0302's single remark in cell 70.
  - "Choosing Between for and while", with its input-validation `while True` example, is only partly covered by 0302 cell 4.
✗ Slides omit notebook topics:
  - The loop `else` clause (0302 cells 66, 74-75) has no slide.
  - `break` has no worked slide. It appears only in the families table and the mini activity, and the "Loop Control" slide covers only `pass` vs `continue`.
  - `do-while` emulation (0302 cells 63-65) is not shown.
✗ `match`/`case` contradiction: the "Control-Flow Families" slide says "this chapter does not cover `match` / `case`". 0301 cell 6 lists "**Match** statement | `match` / `case`" as form 5 of the `if` structures, and cell 4 lists `switch-case`. Right now the notebook table promises a form it does not teach. When the planned `match`/`case` addition is written, this slide callout becomes wrong and must change.
✗ Learning goals: the deck has no learning-goals slide. The closest equivalents are "Chapter Route" and "Chapter 3 Checklist".
  - The Checklist includes "Accumulate results", and the Route includes conditional expressions and accumulators. Neither appears in the landing page's four learning goals (0300 cell 3).
  - Landing goal 3 ("Use `break`, `continue`, and `pass` intentionally") and goal 4 (trace nested branches and loops) are matched by the Checklist.
  - Fix: add a goals slide that mirrors 0300, and add conditional expressions and accumulators to the landing goals.
✓ Key examples still exist in the notebooks: the fruit tuple with `iter`/`next` (0302 cell 7), `times < 3` (47), matrix nested loop (31), `age`/`status` ternary (0301 cell 34), `0 < x < 10` (0301 cell 29), admin login nesting (0301 cell 31), and rainfall (0302 exercise cell 42 only).

### Preview ↔ chapter
✓ All 10 questions (q1-q10) map to glossary terms that are covered: control flow, Boolean expression, `if`, `else`, `elif`, iteration, iterable, `for`, `while`, `break`. None tests a topic that moved chapters, and the difficulty suits pre-reading.

### Homework ↔ chapter
✗ Accumulator dependence: true/false Q3 ("Loop Accumulators"), coding Q7 (count and total of qualifying sales), and Q9 (build a `labels` list in a loop) all require the accumulator pattern, which 0302 teaches only through exercise cell 42. Q9 and Q10 also need the "collect into a list" accumulator, which appears only in the slides. `list.append` itself is taught in Ch02 `0203` cells 59-61.
✓ The other problems (Q1/Q6 branching, Q2 comparisons, Q4/Q8 `while`, Q5 conditional expressions, Q10 `range`) use current chapter content. The ladder climbs sensibly from 5 true/false cards to 5 coding tasks. Submission IDs (`ch03-homework`, q1-q10) are consistent.

### Lab ↔ chapter
✗ Off-chapter question: lab Q1 ("Unit conversion with modulus and floor division", cells 1-2) uses only `//` and `%`, which are Ch02 operators, with no control flow. It is also missing from the MATERIALS lab list.
✗ Untaught formatting: Q4 and Q5 require exact output with `:.2f` (for example "Final savings: $2071.21"). The format specification mini-language is never taught. It appears once, incidentally, as `{score:.1f}` in a Ch02 `0203` type-hinting example.
✗ Coverage gap: no lab task, and no homework coding task, exercises `break`, `continue`, or `pass`, or a sentinel or `while True` loop. So landing learning goal 3 is assessed only by preview q10.
✓ Lab Q2-Q5 (nested tier, for-loop condition count, `while` savings goal, ternary fee) fit the chapter topics apart from the accumulator dependence in Q3. Submission wiring is correct: `labId = 'ch03-lab'`, and 5 code cells are collected (`slice(0, 5)`), matching Questions 1-5.

### Glossary ↔ content
✗ "Running total" (term 20) is not introduced in any content notebook's text. The definition ("such as total rainfall") comes straight from exercise cell 42 in 0302.
✗ "Boolean expression" (term 3) and "Control flow" (term 1) are only referenced in Ch03. Boolean expressions are explained in Ch02 `0202`, and "control flow" appears only as a table header in 0301 cell 4. This is acceptable only if the glossary is meant to recap earlier chapters.
✓ The other 17 terms are introduced in 0301 or 0302 text: branch, `if`, `else`, `elif`, nested conditional, conditional expression, iteration, iterable, iterator, `for`, `range`, `while`, sentinel, infinite loop, `break`, `continue`.
✗ Taught terms missing from the glossary:
  - `pass` statement (0301 cell 14, 0302 cells 66 and 76; named in learning goal 3)
  - loop `else` clause (0302 cell 66)
  - nested loop (0302 cell 27, bold)
  - `enumerate` and `zip` (0302 cells 34-39)
  - definite and indefinite iteration (0302 cell 4, bold)
  - loop variable (0302 cell 14, bold)
  - condition (0301 cell 9, bold)
  - chained conditional (0301 cell 18, bold)
  - accumulator (term used in homework, lab, and slides)

### Summary
Sync issues: 20
  - Planning docs:
    - ORGANIZATION.md and MATERIALS.md lack `orphan: true` front matter
    - "Boolean expressions" listed for 0301 but taught in Ch02 (doc stale, or add a recap)
    - Most 0302 topics (iterators, range/index loops, nested loops, enumerate/zip, dict iteration, while patterns, `pass`, loop `else`), plus 0301 `pass`, are taught but not listed
    - Accumulator pattern appears only in an exercise (0302 cell 42), so it is not taught
    - No exercise lists in either doc; exercise headers are inconsistent (untitled `### Exercise:`, `# === EXERCISE`, `### Practice:`); Iterators section has no exercise
    - No required-core vs enrichment split
    - MATERIALS lab and homework descriptions are stale (lab Q1 unlisted; homework has no flow chart)
    - 0301 forward references: recursion (cell 39), `def`/`return` exercise (cell 40), `math.log`/`float('nan')` (cell 38)
    - 0302 forward references: `dict.items()` (cell 41), `open()` (cell 9)
    - 0302 cells 54, 60, 62 break top-to-bottom execution
  - Slides:
    - Slides teach accumulator variants, Boolean pitfalls, and an infinite-loop checklist that the notebooks lack
    - Slides omit loop `else`, a worked `break` example, and do-while
    - `match`/`case` contradiction between the slide callout and 0301 cell 6 table (and cell 4 `switch-case`)
    - No learning-goals slide; slide checklist and route include accumulators and conditional expressions, which the landing goals lack
  - Assessments:
    - Homework Q3, Q7, Q9 depend on the untaught accumulator pattern
    - Lab Q1 is a Ch02 operators task with no control flow
    - Lab Q4 and Q5 require an untaught `:.2f` format spec
    - break, continue, pass, and sentinel loops are not assessed in lab or homework coding
  - Glossary:
    - "Running total" defined only via an exercise
    - Taught terms missing: `pass`, loop `else`, nested loop, `enumerate`/`zip`, definite/indefinite iteration, loop variable, condition, chained conditional, accumulator

### Top fixes
1. Add a taught `### Accumulator Pattern` subsection to 0302, inside `## For Loops` before exercise cell 42. Cover initialize, update, and use-after for a running total, a counter, and a list collection, and reuse the rainfall data. This one change covers homework Q3/Q7/Q9, lab Q3, glossary term 20, and the two accumulator slides.
2. Rewrite the ORGANIZATION.md and MATERIALS.md topic lists:
   - Add `orphan: true` front matter.
   - List the 0301 and 0302 topics in `##`/`###` heading order, including iterators (marked as a Ch11 preview), enumerate/zip, dict iteration, while patterns, `pass`, and loop `else`.
   - Resolve "Boolean expressions": drop it from the list, or add a short recap in 0301.
   - Add per-notebook exercise lists and a required-core vs enrichment split.
   - Correct the lab and homework descriptions.
3. Remove the Ch04 forward references in 0301: replace the recursive `factorial` (cell 39) and the `def absolute_value` exercise part (cell 40) with function-free ternary examples, or clearly label them as a Ch04 preview. Also add a one-line explanation of `.items()` in 0302 cell 41.
4. Align the assessments with the chapter goals. Replace lab Q1 (a Ch02 `//`/`%` task) with a `break`/`continue` or sentinel-loop task. Either teach `:.2f` briefly or relax the Q4 and Q5 output requirements. Add conditional expressions and accumulators to the 0300 learning goals, and add a matching goals slide.
5. Resolve the `match`/`case` inconsistency: remove the "Match statement" row from the 0301 cell 6 table until the planned section exists, or update the slide callout when it is written. Also fix the non-runnable 0302 cells 54, 60, 62, and standardize the exercise headers (0301 cell 20, 0302 cells 21/32/42/55/77).
