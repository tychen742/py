---
orphan: true
---

## Sync Audit — Ch02: Variables and Basics

Scope: `chapters/02-basics/` (0200–0203, ORGANIZATION.md, MATERIALS.md, assignments/), `_html_extra/chapters/02-basics/overview.md`, `_toc.yml` lines 20–35. Audited as on disk, 2026-09-25. Cell indices are 0-based JSON cell positions.

Exercise cells found (chapter uses `### Checkpoint:` rather than `### Exercise:` as the heading):

| Notebook | Cell | Heading | Tag | Solution |
|---|---|---|---|---|
| 0201 | 27 | Checkpoint: create useful names | thebe-interactive | 28 hide-input |
| 0201 | 69 | Checkpoint: mutation or reassignment? | thebe-interactive | 70 hide-input |
| 0202 | 37 | Exercise: what's the output? | **none** | **none** |
| 0202 | 38 | Exercise: what's the output? | **none** | **none** |
| 0203 | 22 | Checkpoint: convert input-like text | thebe-interactive | 23 hide-input |
| 0203 | 52 | Checkpoint: scalar types | thebe-interactive | 53 hide-input |
| 0203 | 77 | Checkpoint: choose basic collections | thebe-interactive | 78 hide-input |

### Planning docs ↔ chapter

✗ Front matter — neither `ORGANIZATION.md` nor `MATERIALS.md` starts with MyST front matter `orphan: true`; both open directly with an `#` heading.

✓ Notebook inventory — both docs name 0200, 0201, 0202, 0203 and the four assignment files; all exist and match the `_toc.yml` chapter entries. No `## Planned Additions` section exists.

✗ Exercise lists — neither doc has an exercise list at all. The notebooks contain 5 tagged checkpoints plus 2 untagged exercises (table above). Docs are stale/incomplete.

✗ Required core / enrichment / learning objectives — neither doc has a required-core list, an enrichment list, or learning objectives, so the "every required-core topic is taught" and "objectives agree with landing page" checks cannot be run. Docs are incomplete.

✗ Section sequence, 0201 — ORGANIZATION.md lists "names and assignment; objects, values, types, and identity; mutation vs. reassignment; f-strings". F-strings are **not taught** in 0201: they are only used in code (cells 12, 14, 17 comment "# f-string", 66) and asserted in the Summary (cell 71: "F-strings are the standard way…"). F-strings are taught in Ch01 `0102-basic-syntax.ipynb` (`#### F-Strings`, cells 28–30). Doc and 0201 Summary are stale (topic moved to Ch01). Also taught in 0201 but not listed in ORGANIZATION.md: Naming Conventions (cell 22), state diagrams (cell 18), Expressions and Arguments (cell 29), Aliasing/Sameness (cells 52, 60). MATERIALS.md does list aliasing, so the two docs disagree.

✗ Section sequence, 0202 — docs order: operators; arithmetic; comparison and Boolean logic; membership and identity; assignment updates and precedence. Notebook `##` order: Operators (3), **Assignment Operators (6)**, Arithmetic (8), Comparison (24, with Truthy/Falsy 27, Membership 39, Identity 43 as `###`), Logical (45), **Bitwise Operations (53)**, Operator Precedence (59). Assignment updates are taught second, not after membership/identity; Bitwise Operations, Truthy/Falsy, short-circuit evaluation, and the walrus operator are taught but not listed in either doc. Docs are stale.

✗ Section sequence, 0203 — docs order: scalar types; type checking and conversion; collection preview; type hints. Notebook `##` order: Python Built-in Types (6), Type Checking (9), Type Conversion (14, with Type Hinting as a `###` at cell 24), Core Scalar Types (29), Collection Types (54, with a `### Mutability` at 80). Scalar types come fourth, not first; type hints come in the middle, nested under Type Conversion, not last. The whole `### Strings` block (cells 32–51: creation, concatenation, `len()`, escaping, indexing and slicing) is not mentioned in either doc even though homework Q9 and lab Q4 assess it. Docs are stale.

✗ Chapter title disagreement — docs: "Variables and Basics"; landing page H1 (0200 cell 0): "Syntax and Types"; slides title: "Variables & Types"; assignments index: "Basics". Pick one.

✗ Build checklist not met — ORGANIZATION.md requires "at least one checkpoint exercise per major section" and "Tag question cells `thebe-interactive` and solution cells `hide-input`". 0202 has **no** checkpoint; its two exercises (cells 37, 38) have no tag and no solution cell.

✗ Topic only in an example/table, not taught — the walrus operator `:=` appears only in the two operator tables (0202 cells 5, 6) and one code comment (cell 7); the text never explains it. Either teach it or drop it from Ch02.

✗ Exercises/examples using untaught syntax (Ch01 mentions `if`/`for`/`def` only as indentation and docstring illustrations; control flow is Ch03, functions Ch04):
- 0203 cell 53 (checkpoint solution for "scalar types") uses a `for` loop; the text only used `for` in cell 31 as a shortcut example ("we can use a `for` loop") without teaching it. Students cannot be expected to produce this.
- 0202 cells 37–38 (exercises) use `not` and `or` before the Logical operators section (cell 45) teaches them.
- 0202 cell 7 (walrus with `if`), cells 35–36 (`if`/`else`), cell 31 (`if my_list:` idiom) rely on conditionals from Ch03; 0202 cell 56 defines `is_even` with `def`/`return`; 0203 cell 31 uses `for`, `repr()`, and a width format spec.
- 0201 cell 69 (checkpoint) uses list concatenation `colors + ["purple"]`; list `+` is never taught (string `+` is taught later, 0203 cell 35).
- 0203 cells 48, 50 use `.lower()` / `.upper()` without any explanation of string methods.

✗ Stale Summary — 0203 Summary (cell 81) says "Built-ins such as `len()`, `sum()`, `round()`, and `sorted()` are everyday tools"; `sum()` and `sorted()` are never discussed in 0203 (built-ins moved to Ch01 0102 cell 78; archived source `materials/0204-builtin-functions_in-0102.ipynb`).

✗ Meaning-changing content errors (these also put the notebook out of sync with slides/assignments):
- 0203 cell 54 table marks `dict` Mutable = **No**; cell 80, cell 73 ("update: MUTABLE"), and the slide preview table all say Yes.
- 0203 cell 24 writes the return annotation as `-->`; the correct token is `->` (the code block right below uses `->`).
- 0203 cell 16: "`str` can convert an integer or floating-point number to an integer" (should be "to a string").
- 0202 cell 10: modulus "return the quotient" (should be remainder; the table in the same cell says remainder).
- 0202 cell 60 precedence table puts `^` and `|` on the same level; in Python `^` binds tighter than `|`.
- 0201 cell 44: "…and other collection types are immutable" contradicts sets being mutable (0203 cells 54, 74).

✗ Duplicated / leftover cells — 0202 cells 32 and 33 are identical short-circuit paragraphs; 0202 cell 63 repeats the membership example from cell 42 after the Precedence section; empty cells at 0202[29] and 0203[79].

✓ Material files — docs reference `materials/_archived/02-overfragmented/`, which exists; there is no chapter-specific `materials/02-*` folder to reconcile.

### Slide deck ↔ chapter

✗ Learning goals — the deck has no learning-goals slide; none of the five landing-page goals (0200 cell 3) appear.

✗ Section coverage — deck sections follow the three notebooks, but most `##` topics are missing:
- 2.1: no slide for Python Objects / identity (`id()`), Mutability, Aliasing, or `==` vs `is` (the core of 0201), and none for Naming Conventions.
- 2.2: no slides for Assignment Operators, Membership, Identity, Bitwise, or Operator Precedence, although the section subtitle (line 89) promises "precedence".
- 2.3: no slide for Type Checking (only a quick-reference row) or Type Hinting, although the subtitle (line 142) promises "type hints".

✗ Misplaced / missing topic — the 2.1 subtitle (line 47) promises "formatted output" (f-strings), which 0201 does not teach; the 2.1 slide (lines 76–77) shows augmented assignment `x += 10`, which is taught in 0202.

✗ Topics not in the chapter — the Strings slide (lines 196, 207) and Quick Reference (line 245) feature `.split()`, `.strip()`, `.replace()`; none appears in Ch02 (0203 uses only `.lower()`/`.upper()`). Slide example `result = data or []` (line 130) differs from the chapter's `"" or "default"` (0202 cell 34); minor.

✗ Figures/tables not preserved — the deck drops the built-in data types figure (0203 cell 7, `figures/python-data-types-2.png`), the state diagram (0201 cells 18–21), the reassignment vs. mutation table (0201 cell 45), the `is` vs `==` table (0201 cell 61), the logical-operator truth table (0202 cell 46), and the precedence table (0202 cell 60). Per AGENTS.md these carry conceptual structure and should be kept.

✓ Rendered HTML — `overview.html` and `overview.md` share the same timestamp (Sep 7), so the render is current with the source; it must be regenerated after the fixes above.

### Preview ↔ chapter

✗ q8 "Return value" — taught in Ch01 (`0102-basic-syntax.ipynb`, `### Return Values`, cell 87); Ch02 mentions return values only in passing in the type-hint paragraph (0203 cell 24).

✗ q6 "Operand" — the term is used (0202 cells 27, 46; 0203 cell 14) but never defined or illustrated in Ch02 text.

✓ q1–q5, q7, q9, q10 map to Ch02 content (variables, assignment, type, object, operator, expression, mutable, dictionary). Difficulty is appropriate for pre-reading.

### Homework ↔ chapter

✓ T/F Q1–5 and coding Q6–10 use only current Ch02 (or Ch01) content: descriptive names, conversion, `//`/`%`, Boolean expressions, list vs tuple, f-strings, `float()`, slicing, set/dict/tuple. `sorted()` in Q10 is taught in Ch01 0102 cell 78. Ladder (T/F concepts → formatting → arithmetic → conversion → slicing → collections) is sensible.

✗ Coverage gap — no homework (or lab) item assesses object identity, mutation vs. reassignment, or aliasing, which is a full `##` section of 0201 and the chapter's most distinctive concept. (Also absent from the landing-page learning goals; see Glossary/goals below.)

### Lab ↔ chapter

✓ Q1–Q5 (variables/f-strings, numeric operators, conversion with `sum`/`len`/`max`/`type`, string concat/indexing/Boolean `and`, collection types with fruit data) match Ch01–Ch02 content and reuse the chapter's fruit dataset. No removed techniques.

✓ Submission panel (cell 11) lists q1–q5, matching the five `### Question N` cells; answer key `ch02-lab` follows the `chNN-type` convention.

### Glossary ↔ content

✗ Terms not introduced in Ch02 notebooks — "Soft keyword" (#5) appears nowhere in Ch02; it is taught in Ch01 0102 cell 71. "Return value" (#14) is taught in Ch01 0102 cell 87. "Keyword" (#4) is only listed via `kwlist` in 0201 cells 23–24 without definition (defined in Ch01 0102 cell 62). "Operand" (#11) is used but never explained. Either teach these in Ch02 or drop them from the glossary.

✗ Landing-page learning goals vs content — the five goals (0200 cell 3) omit object identity/mutability/aliasing (0201 `## Python Objects`) and type hints (0203 cell 24); goal 5 "Choose appropriate basic and collection types" is covered.

✗ Missing glossary terms introduced in the notebooks — state diagram (0201 cell 18, has an `{index}` entry), dynamic typing (0201 cell 4; 0203 cell 4), reassignment and mutation (0201 cells 15, 45), augmented assignment (0202 cell 6), floor division and modulus (0202 cells 8, 10), short-circuit evaluation (0202 cell 32), type casting and type coercion (0203 cells 14–20), string, concatenation, escape sequence, indexing and slicing (0203 cells 32–51).

✓ The other 18 glossary terms (variable, assignment statement, naming conventions, object, identity, alias, type, operator, expression, argument, type conversion, type hint, mutable, immutable, list, tuple, dictionary, set, Boolean, truthy, falsy) are introduced in 0201–0203.

### Summary

Sync issues: 27
  - Planning docs: missing `orphan: true` front matter (both files)
  - Planning docs: no exercise lists (7 exercise cells exist)
  - Planning docs: no required-core/enrichment lists or learning objectives
  - 0201 doc lists f-strings (taught in Ch01); 0201 topics naming/state diagrams/expressions/aliasing unlisted; docs disagree on aliasing
  - 0202 doc order wrong (assignment taught 2nd); bitwise, truthy/falsy, short-circuit, walrus unlisted
  - 0203 doc order wrong (scalar first, hints last); Strings block unlisted
  - Chapter title differs across docs, landing page, slides, assignments index
  - 0202 has no checkpoint; cells 37–38 untagged with no solutions
  - Walrus operator appears only in tables/example, never taught
  - 0203 checkpoint solution (cell 53) requires an untaught `for` loop
  - 0202 exercises 37–38 use `not`/`or` before logical operators are taught
  - Text examples rely on Ch03/Ch04 syntax (`if`, `for`, `def`) in 0202 cells 7, 31, 35, 36, 56 and 0203 cell 31; list `+` in 0201 checkpoint; unexplained string methods
  - 0203 Summary cites `sum()`/`sorted()` (moved to Ch01); 0201 Summary cites f-strings
  - dict listed as immutable in 0203 cell 54
  - Other meaning-changing errors (`-->`, str→integer, modulus→quotient, `^`/`|` precedence, "other collection types are immutable")
  - Duplicate/empty cells in 0202 and 0203
  - Slides: no learning-goals slide
  - Slides: missing objects/identity/mutation/aliasing, membership/identity/bitwise/precedence, type checking/type hints
  - Slides: 2.1 promises f-strings; shows augmented assignment that belongs to 2.2
  - Slides: `.split()`/`.strip()`/`.replace()` not in chapter
  - Slides: key figures/tables dropped
  - Preview q8 "Return value" is a Ch01 topic
  - Preview q6 "Operand" never defined in Ch02
  - Homework/lab do not assess identity, mutation, or aliasing
  - Glossary terms not taught in Ch02 (soft keyword, return value, keyword, operand)
  - Landing-page goals omit identity/mutability and type hints
  - Glossary missing ~10 introduced terms (state diagram, dynamic typing, type casting/coercion, escape sequence, slicing, etc.)

### Top fixes

1. Rewrite ORGANIZATION.md and MATERIALS.md: add `orphan: true`, correct the 0202/0203 section order to match the notebooks (or reorder the notebooks), list every exercise, add required-core/enrichment lists, remove f-strings from 0201 (Ch01 topic), and add Strings and Bitwise.
2. Fix untaught-syntax problems in exercises: replace the `for` loop in the 0203 cell 53 solution with five `print()` calls; move 0202 exercises 37–38 after `## Logical operators`, tag them `thebe-interactive`, add hide-input solutions, and add a real 0202 checkpoint. Replace or clearly flag the `if`/`for`/`def` examples in 0202/0203 as previews.
3. Correct meaning-changing errors: `dict` Mutable = Yes (0203 cell 54), `->` not `-->` (cell 24), `str` converts to a string (cell 16), modulus gives the remainder (0202 cell 10), `^` above `|` in precedence (cell 60).
4. Rebuild the slide deck: add a learning-goals slide matching 0200, add slides for objects/identity/mutation/aliasing, membership/identity/bitwise/precedence, and type checking/type hints, restore the data-types figure, state diagram, and mutation/equality tables, drop `.split()/.strip()/.replace()`, then regenerate `overview.html`.
5. Align glossary, goals, and assessments: drop or teach "Soft keyword", "Return value", and "Operand"; add state diagram, dynamic typing, type casting/coercion, escape sequence, slicing; add a learning goal plus at least one homework or lab item on identity, mutation, and aliasing; replace preview q8 with a Ch02 term.
