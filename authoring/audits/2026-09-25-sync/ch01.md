---
orphan: true
---

## Sync Audit — Ch01: Introduction to Python

Files audited (working tree, 2026-09-25): `chapters/01-intro/{ORGANIZATION.md, MATERIALS.md, 0100-py.ipynb, 0101-programming.ipynb, 0102-basic-syntax.ipynb}`, `chapters/01-intro/assignments/{index,preview,lab,homework}.ipynb`, `_html_extra/chapters/01-intro/overview.md`, `_toc.yml` lines 7-18. ORGANIZATION.md has no "Planned Additions" section.

### Planning docs ↔ chapter

✗ Front matter: neither `ORGANIZATION.md` nor `MATERIALS.md` starts with MyST front matter `orphan: true`. Both open directly with `# Chapter 01 ...`.

✓ Notebook inventory: the docs name 0100, 0101, 0102, and `assignments/index|preview|lab|homework`. All of these exist and match the `_toc.yml` entries (lines 7-18). There are no extra notebooks in the folder.

✗ Section sequence, 0101: the docs (ORGANIZATION "Scope and Sequencing" #2 and "File Roles"; MATERIALS "Notebook Content Targets" and "Delivery Order") describe 0101 as "Programming learning habits, what programs and algorithms are, abstraction, binary/hex/ASCII basics."
  - Listed but not taught: **algorithms**. The word does not appear in any Ch01 notebook, and MATERIALS "Must-Have" also lists "Algorithms and programs" for the slides. Either the docs are stale or the topic was never written.
  - Taught but not listed: `## Programming Language` / `### Natural vs Formal Languages` (cell 8), `## Interpreted vs. Compiled` (cell 12), `## Programming Constructs` (cell 14), `## Expressions and Statements` (cells 16-22), octal/decimal and positional-notation base conversion (cells 23-29), `## Resources` (cell 36). These docs look stale: they describe only about 40% of the notebook.

✗ Section sequence, 0102: the docs give the order "Syntax rules, comments, print statements, built-in functions, module imports, first programs." The notebook's `##` order is Indentation and Block Structure (4) → Statement Formatting (11) → Input and Output (21) → Comments (46, with `### Docstring` 50) → [object section, empty] (55-61) → Python Keywords (62, with `### Soft keywords` 71) → Built-In Functions (75, with `### Arguments` and `### Return Values`) → Modules and Packages (92, with `### Importing Modules` and `### Installing Packages`).
  - Order: the docs put comments before print; the notebook teaches Input and Output before Comments.
  - Listed but not taught: "first programs". There is no such section or heading, and the phrase appears only in homework Q6.
  - Taught but not listed in the topic lines: statement formatting and line continuation, `input()` and type conversion, docstrings, keywords and soft keywords, installing packages with `%pip`.

✗ Landing page scope: both docs say 0100 covers "how to use Jupyter." 0100 has no Jupyter content and no link to setup guidance: the only Jupyter mention is the bullet in cell 1, and there is no link to `chapters/appendices/jupyter/`. Landing goal 4 ("Identify where to find environment and Jupyter setup guidance") is therefore unsupported. Either add a pointer to the Jupyter appendix or drop the doc line and goal 4.

✗ Object section gutted, leaving the topic exercise-only (0102 cells 55-61). Cells 55, 57, and 59 are empty. Only the `{index}` stubs remain: `object` (56) and `object ID, id() function` (58). The `### Exercise: Inspect object information` exercise (cell 60, `type()`/`id()`) and its solution (61) are still present, and MATERIALS still lists "object inspection" as a section exercise. As a result, objects, identity, and `id()` are covered only by an exercise. The slides also keep an "Objects: Type, Value, Identity" slide. Either restore the teaching text or remove the stubs, the exercise, the MATERIALS entry, and the slide.

✗ Exercise lists:
  - MATERIALS lists a **built-in functions** exercise for 0102. The Built-In Functions section (cells 75-91) has no exercise. It is also the only major 0102 section without one, which violates ORGANIZATION's checklist rule of at least one checkpoint exercise per major section.
  - 0101 has 2 exercises (cell 30, number-system conversion; cell 34, ASCII code for "C"). Neither doc mentions them: MATERIALS lists section exercises only for 0102, and ORGANIZATION has no per-notebook exercise list at all.
  - 0101 exercises are not in house format. Cells 30 and 34 start `### Exercise ` with no title and have no `thebe-interactive` tag. Solutions 31 and 35 have no `hide-input` tag and no `### Solution` header. This conflicts with ORGANIZATION's "Tag question cells `thebe-interactive` and solution cells `hide-input`."
  - 0102's 9 exercises match MATERIALS in order apart from the missing built-in-functions exercise and two print/f-string exercises (33, 35) sharing one list entry.

✗ Exercises that use syntax not taught at that point (house rule b):
  - 0101 cell 34 asks for the output of cell 35, which uses f-strings and escaped quotes (`\"`). The prompt itself says "use ... escape sequences." F-strings are first taught in 0102 (cell 28), and escape sequences are not taught in Ch01. The prompt also asks for `c_dec`/`c_bin`, but the solution uses `cc_dec`/`cc_bin`.
  - 0102 cell 44 (Convert text input to numbers): the solution requires `//` and `%`. Operators are taught in Ch02 (`0202-operators`), and neither operator appears in Ch01 text.
  - 0102 cell 53 (Add a useful docstring): students must write a function body with `return`. Function definition is taught in Ch04. Ch01 shows only a single `def` example (cell 52) and never explains `def`, parameters, or `return`.
  - 0102 cell 73 (Check keywords): the solution uses `import keyword` and `keyword.iskeyword()`. The text shows only `from keyword import kwlist` (cell 66) and never mentions `iskeyword`. Module dot notation is explained later, at cell 98.
  - 0102 cell 100 (Import a module): the solution needs `**`, which appears only incidentally in the cell 52 example and is not taught until Ch02.
  - Minor: 0101 cell 29 uses `print(..., sep="\n")` before `sep` is taught (0102 cell 31).

✗ Material Files: `materials/01-intro-text_file.txt` exists, but no Ch01 notebook, assignment, or slide references it (a grep finds only ORGANIZATION.md, MATERIALS.md, and authoring/PROGRESS.md). The claim "text file used by introductory examples" is stale. Either remove the listing or add the example. There is no `materials/01-intro/` folder, so there are no unlisted files to flag.

✗ Checklist claim "Keep each notebook executable top-to-bottom" does not hold for 0102:
  - Cells 39, 41, and 43 comment out `input()`, so `name` and `age` are undefined on a fresh run (NameError). Their stored outputs are stale: cell 41 prints `<class 'int'>` on the line annotated "age is of string type."
  - Cell 39's prompt `"Enter your name [{default_name}]: "` is a non-f-string with an undefined placeholder.

✗ Learning objectives: ORGANIZATION.md has no learning objectives, so it cannot be checked against the landing page. The landing goals are checked against the slides below.

✓ ORGANIZATION ↔ MATERIALS: section order, notebook roles, material file, and assignment roles agree. Only MATERIALS carries the exercise list, and both share the stale topic lines noted above.

✗ Stale or dangling items: 0101 cell 23 contains `<!-- TODO: add base 16 conversion -->`. 0101 cell 5 ends "Some of the essential concepts include:" and is followed by a video iframe (cell 6) instead of a list, so the list is effectively in cell 7 after the video.

### Slide deck ↔ chapter

✗ Section headings, 1.1 (0101): the slides have Natural vs Formal → How Python Runs → Interpreters and Compilers → Constructs → Expressions and Statements → Worked Example → Number Systems → Binary/Hex Helpers → Character Encoding → Checkpoint.
  - Missing: a slide for `## Levels of Abstraction` (0101 cell 10). The section-divider subtitle says "abstraction", but no slide covers it.
  - Slide-only: "How Python Runs" (`assets/how-python-works.png`, source → bytecode → PVM) and the `interpreter.jpg`/`compiler.jpg` figures. None of these figures appears in any Ch01 notebook, which has only a one-paragraph mention of bytecode in cell 12.

✗ Section headings, 1.2 (0102): the slide order is Syntax → Indentation → print → F-Strings → input → **Built-In Functions** → Comments and Docstrings → Objects → Keywords → Modules. The notebook order is Indentation → Statement Formatting → Input and Output → Comments → (Objects, empty) → Keywords → **Built-In Functions** → Modules. Built-in functions are therefore out of order. **Statement Formatting** (0102 cell 11) and **Installing Packages** (cell 102) have no slides.

✗ Learning goals: the slides ("Today's Route", lines 46-52, and "Chapter 1 Recap", lines 629-638) do not match the 0100 goals (cell 3).
  - Landing only: "Explain what programming is at a high level," "Describe how Python fits into the broader computing landscape," "Distinguish conceptual programming ideas from Python-specific syntax," and "Identify where to find environment and Jupyter setup guidance."
  - Slides only: "Set up Python as a working language," "Distinguish expressions, statements, and objects," "Run small syntax examples in Jupyter," "Use ... imports," "trace short assignment-based programs," "inspect values with `type`, `bin`, `hex`, `ord`, and `chr`."
  - Only "use basic built-in functions" matches cleanly.

✗ Slide examples not in the notebooks:
  - `ord()`/`chr()` appear in "Character Encoding" (lines 328-333), "Checkpoint: Section 1.1" (line 356), and the Recap (line 637), but no Ch01 notebook teaches them. 0101 cell 35 hardcodes `67`.
  - `from collections import Counter` / `Counter("banana")` ("Modules and Packages", lines 579-584) is not in 0102.
  - The "Jupyter Workflow" slide (lines 114-137) has no counterpart in 0100 or the content notebooks.
  - The "Comments and Docstrings" slide (lines 523-525) uses `//`, `%`, and a tuple return, none of which are taught in Ch01.

✗ Removed topic: the "Objects: Type, Value, Identity" slide (lines 532-552) teaches content whose notebook text is now empty (0102 cells 55-59).

✗ Stale reference: the closing slide (line 646) says "Chapter 2: Variables, Operators, And Types." The Ch02 landing page title is "Syntax and Types" (`chapters/02-basics/0200-py-basics.ipynb`, cell 0).

✓ Size and assets: about 32 slides with section checkpoints, in line with the "about 30" target. The asset files exist in `_html_extra/chapters/01-intro/assets/`.

### Preview ↔ chapter

✗ q8 ("basic building block used to express computation, such as a statement or expression" → Programming construct) conflicts with 0101 cell 14. The notebook defines programming constructs as sequence/selection/iteration plus supporting elements (subroutine, variable, type, operator, array), and treats expressions and statements in a separate `##` section. The glossary entry (0100 glossary #8) has the same mismatch.

✗ q5 (Abstraction): the chapter never explains abstraction in prose. `## Levels of Abstraction` (0101 cell 10) is one sentence plus a language-level table, so the preview's definition ("a simplified view that hides lower-level details...") is not taught.

✓ q1-q4, q6, q7, q9, and q10 map to taught content: program (0101 cell 14), programming, natural, and formal language (cell 8), interpreted and compiled (cell 12), expression (cell 16), and f-string (0102 cell 28). No question tests a topic from another chapter. The difficulty suits pre-reading.

Note (not a sync error): 9 of 10 questions are 0101 vocabulary; only q10 touches 0102 (syntax, indentation, input, built-ins, modules).

### Homework ↔ chapter

✗ Q2 "Algorithms" (cell 3) tests a concept that is not taught anywhere in Ch01 (see planning-docs finding).

✗ Q10 "Build an Audit Tag" (cell 19) requires `ord()`, which is not taught, and `str()`, which appears only in the grouping table at 0102 cell 80 and is never demonstrated.

✗ Q6 "Fix a Syntax Error" (cell 11) says the code "contains two syntax problems." The shown code has only one: the missing `)` on line 1. Line 2 is valid. The prompt's claim is wrong and will confuse students.

✓ Q1, Q3-Q5, and Q7-Q9 use content present in the chapter: programs, syntax, `#` comments, `input()` returning text, reassignment, `int()`/`type()`, and f-strings. The difficulty ladder (T/F concepts → one-line fixes → combined formatting) is sensible, and the format matches the MATERIALS/ORGANIZATION description (5 T/F in the two-cell format, 5 coding).

### Lab ↔ chapter

✗ Q5 (cell 9) requires `ord()`, which is not taught in 0101 or 0102.

✗ Q4 (cell 7) is a verbatim copy of 0102 exercise cell 44, and Q3 (cell 5) is a verbatim copy of 0102 exercise cell 35 (and of slide "Checkpoint: Section 1.2"). The docs say lab questions "extend the section exercises," so these do not. Q4 also depends on the untaught `//` and `%`.

✓ Q1 and Q2 (variables, arithmetic expressions, `round`, `print`) match the chapter. The submission block (`data-assignment-answers="ch01-lab"`, Live Code, highest score kept) matches the 5-question structure and the `chNN-type` naming.

### Glossary ↔ content

✗ "Programming construct" (#8): the definition ("such as a statement or expression") disagrees with 0101 cell 14 (sequence/selection/iteration).

✗ "Abstraction" (#5) and "Syntax" (#15) are not explained in any notebook. 0101 cell 10 has only a heading and table for abstraction. 0102 has no introductory text defining syntax: cell 3 is a figure and cell 4 goes straight to indentation.

✓ The other 18 terms are introduced in the notebooks: program, programming/natural/formal language, interpreted/compiled language, expression, statement, number system, binary, hexadecimal, character encoding, indentation, block, built-in function, `print`, f-string, `input`.

✗ Key terms taught or indexed but missing from the glossary: comment, docstring, keyword (and soft keyword), argument, return value / `None`, module, package, library, import statement, dot operator, bit, byte, ASCII, Unicode/UTF-8, octal, decimal, bytecode, interpreter, compiler, evaluation vs. execution, operator/operand. Most carry `{index}` entries in 0101 cells 13, 15, 24, 33 and 0102 cells 47, 51, 62, 72, 85, 93, 95.

### Summary
Sync issues: 27
  - ORGANIZATION.md and MATERIALS.md lack `orphan: true` front matter
  - 0101 topic lines stale: "algorithms" not taught; 5 taught sections unlisted
  - 0102 topic lines stale: order differs, "first programs" not taught, 5 taught topics unlisted
  - Docs and landing goal 4 promise Jupyter guidance that the chapter does not provide or link to
  - Object section emptied; topic is now exercise-only (cell 60), still listed in MATERIALS and slides
  - MATERIALS lists a built-in-functions exercise that does not exist (and the section has no exercise)
  - 0101's 2 exercises are unlisted in the docs and missing `thebe-interactive`/`hide-input` tags
  - 0101 cell 34 exercise needs f-strings and escape sequences before they are taught; variable names in prompt and solution differ
  - 0102 cell 44 exercise needs `//`/`%` (Ch02)
  - 0102 cell 53 exercise needs `def`/`return` (Ch04)
  - 0102 cell 73 exercise needs `keyword.iskeyword()`, never shown
  - 0102 cell 100 exercise needs `**` (Ch02)
  - `materials/01-intro-text_file.txt` listed but unused
  - 0102 input cells 39/41/43 not executable top-to-bottom; stale outputs
  - Slides missing Levels of Abstraction; slide-only How Python Runs figures
  - Slide 1.2 order differs (built-ins); Statement Formatting and Installing Packages have no slides
  - Slide learning goals diverge from landing page (4 landing-only, 6 slide-only)
  - Slides use `ord`/`chr`, `Counter`, and Jupyter workflow content not in the notebooks
  - Slides keep the Objects slide for removed content
  - Closing slide names Ch02 "Variables, Operators, And Types"; actual title "Syntax and Types"
  - Preview q8 / glossary "Programming construct" definition conflicts with 0101
  - Preview q5 tests abstraction, which is not explained in prose
  - Homework Q2 tests algorithms (not taught)
  - Homework Q10 needs `ord()`/`str()` (not taught)
  - Homework Q6 says "two syntax problems" but the code has one
  - Lab Q5 needs `ord()` (not taught); lab Q3/Q4 duplicate section exercises verbatim instead of extending them
  - Glossary: Abstraction/Syntax unexplained in text; about 20 taught/indexed terms missing

### Top fixes
1. **Teach `ord()`/`chr()` in 0101 `### Character Encoding`** (a short prose paragraph plus a code cell). This single change fixes lab Q5, homework Q10, three slides, and the 0101 cell 35 hardcoded `67`.
2. **Resolve the gutted object section in 0102 (cells 55-61).** Either restore a short "Objects: type, value, identity" text section before the exercise, or delete the stubs, the exercise, the MATERIALS "object inspection" entry, and the Objects slide.
3. **Rewrite the 0101/0102 topic lines in ORGANIZATION.md and MATERIALS.md** to match the actual `##` headings and order. Either drop "algorithms", "first programs", and "how to use Jupyter", or add that content (at minimum, a link from 0100 to the Jupyter appendix for goal 4). Add `orphan: true` front matter to both files and remove the unused `01-intro-text_file.txt` listing.
4. **Fix exercises and assignments that use untaught syntax.**
   - Either teach `//`/`%` briefly in 0102 or change the minutes exercise and lab Q4 (and make lab Q3/Q4 extend the exercises instead of copying them).
   - Replace the `def`-writing docstring exercise with a read-and-inspect task.
   - Show `keyword.iskeyword()` in the Keywords text.
   - Fix the 0101 exercises: tags, titles, f-string/escape dependency, and the `c_dec`/`cc_dec` mismatch.
   - Correct homework Q6's "two syntax problems."
5. **Realign the slide deck with the chapter.**
   - Add a Levels of Abstraction slide and move Built-In Functions after Keywords.
   - Add Statement Formatting and Installing Packages slides.
   - Replace Today's Route and Recap with the landing-page goals.
   - Fix the Ch02 title on the closing slide.
   - Also align the "Programming construct" glossary entry and preview q8 with 0101 cell 14.
