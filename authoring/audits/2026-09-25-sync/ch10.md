---
orphan: true
---

## Sync Audit — Ch10: Functional Programming

Scope: `chapters/10-functional/` (1000, 1001, 1002, assignments/preview, lab, homework), `_html_extra/chapters/10-functional/overview.md`, `_toc.yml` lines 151–165. Working tree as of 2026-09-25. The Planned Additions items (use, don't re-teach for comprehensions and recursion; closures planned for 4.2) are treated as known and are not flagged on their own.

### Planning docs ↔ chapter
✗ Front matter: neither `ORGANIZATION.md` nor `MATERIALS.md` starts with MyST front matter `orphan: true`. Both begin with a `# Chapter 10 ...` heading on line 1.
✓ Notebook inventory: the docs name 1000, 1001, and 1002. These match the folder and `_toc.yml` (151–165), and the assignments/index, preview, lab, and homework files all exist.
✗ 1000 role is stale: both docs say `1000-func-prog.ipynb` holds the "conceptual framing: pure functions vs. side effects; immutability; first-class functions" (ORG Scope item 1, File Roles, and Required Core "Full notebook"; MATERIALS `1000` Required core). In fact 1000 has only an intro bullet list (cell 1), a video, goals, a glossary, and a slides link. The framing is taught in `1001` (`## FP in Python` cells 3–6, `## Purity & Immutability` cells 7–16, `## Functions as Values` cell 17). The docs are stale.
✗ 1001 section sequence: ORG and MATERIALS list "Decorators → comprehensions + lambda/map/filter/sorted → decision criteria → readability". The notebook's `##` order is `FP in Python` → `Purity & Immutability` → `Functions as Values` (lambda, map, filter, sorted, **reduce**) → `Higher-Order Functions` (`### Decorators`, `### Built-in Decorators` with `@property`) → `Comprehensions` → `Choosing a Style`. Several topics are taught but not listed: the FP paradigm overview and taxonomy diagram, purity and immutability (the docs assign these to 1000), `reduce()` (cells 21–23), higher-order functions as a section, and built-in decorators / `@property` (cells 26–27). Decorators are listed first but are taught after lambda/map/filter. The docs are stale.
✗ `reduce()` is taught twice: once in `1001` (cells 17, 21–23) and again in `1002` `### functools.reduce()` (cells 13–14), with the same sum example (`15`) and product example (`120`). The docs place `reduce` only in 1002. Either drop the 1001 copy or have 1002 build on it, then update the docs.
✗ Tree traversal is listed as taught (ORG 1002 "Recursion (factorial, Fibonacci, tree traversal)"; MATERIALS slide list), but it appears only in `1002` cell 6. That cell is a `hide-input`, `skip-execution` code cell that is never run and has no markdown explanation, and it relies on `os.listdir` / `os.path`, which appear only in the appendix (`appendices/packaging/0501-files`). By the house rule it is not taught.
✗ Recursion vs. iteration format: MATERIALS asks for a "recursion-vs-iteration table", but `1002` cell 7 is a bullet list of advantages and disadvantages with no table and no side-by-side iterative code. The comparison is kept under Planned Additions, but the doc's description does not match the notebook.
✗ Exercise lists: ORG File Roles lists all three exercises (Sorting and Filtering with Lambda; Recursive Functions; functools) under `1002`, but Sorting and Filtering with Lambda is in `1001` cell 36. MATERIALS `1001` lists no exercises, and MATERIALS `1002` lists "Sorting and Filtering with Lambda; one recursion exercise", which names the wrong notebook and leaves out the `functools` exercise (`1002` cell 19). Actual: 1001 has 1 exercise (cell 36); 1002 has 2 (cells 8 and 19). Both docs are stale.
✗ Exercise tagging (ORG Build checklist: "Tag question cells `thebe-interactive` and solution cells `hide-input`"): `1002` cell 19 (`### Exercise: functools`) has no tags, and its solution cell 20 has no `hide-input`. The solution is shown fully expanded right below the exercise.
✗ "At least one checkpoint exercise per major section" (ORG Build checklist) is not met. In `1001`, five of six `##` sections have no exercise (only `Choosing a Style` has one, cell 36; Purity & Immutability, Functions as Values, Higher-Order Functions/Decorators, and Comprehensions have none). In `1002`, `## Context Managers` (cells 10–11) has no exercise. MATERIALS also lists "Context-manager implementation exercises" as enrichment, and none exist.
✗ Re-teaching that Planned Additions does not cover: `1001` re-teaches pure functions and side effects (cells 8–13) and lambda syntax with `sorted(key=..., reverse=True)` (cells 17, 20). Both are already taught in 4.2 (`0402-function-design.ipynb` `## Pure Functions and Side Effects` cell 19; `## Lambda Functions` cells 60–72, including sort keys and a dict of lambdas). The `lru_cache` section (`1002` cells 17–18) does not link back to 4.3 `## Memoization` (0403 cells 47–51), even though 0403 cell 51 promises "Chapter 10 shows `functools.lru_cache`". Neither 1001 nor 1002 contains any back-reference to an earlier chapter. The "use, don't re-teach" plan covers only comprehensions, recursion, and closures, so these items are not recorded anywhere.
✗ Stale reference: `1001` cell 26 says `@staticmethod` and `@classmethod` are "(covered in ch12)". Chapter 12 is APIs; both decorators are taught in 9.3 (`09-oop/0903-oop-advanced.ipynb`).
✗ Untaught syntax in a required-core example: `1002` cell 11 (`@contextmanager` timer) depends on `yield` inside a generator function. Cell 10 says `@contextmanager` "transforms a generator function into a context manager", but generators are not taught until Chapter 11 (`11-iter-gen/1102-generators`), and no earlier notebook uses `yield` in code. The text never explains how `yield` splits setup from cleanup or where the `as start` value comes from. For a required-core topic, the explanation is carried by the code alone.
✗ Untaught syntax: `1001` cell 30 (class-based `CountCalls` decorator) uses `__call__`, which is explained only in a code comment. No earlier notebook uses `__call__` (grep across ch01–09). The example sits under Enrichment in ORG, but the text has no markdown explanation of it.
✗ Untaught syntax: `1002` cell 14 uses `itertools.chain.from_iterable` ("In practice, prefer chain.from_iterable()"). Only `itertools.zip_longest` is mentioned earlier (6.2); `itertools` is covered in Chapter 11.
✗ `1002` `## Summary` (cell 21) does not match the notebook. It summarizes 1001 topics (lambda, map, filter, sorted, list/dict/set comprehensions, decorators, `@wraps`) and leaves out 1002's `reduce`, `partial`, `lru_cache`, and recursion vs. iteration.
✗ Learning objectives: ORG has no learning-objectives section, so there is nothing to compare. The landing-page goals (`1000` cell 3) do not mention comprehensions or choosing between comprehensions and `map`/`filter`, although ORG lists both as Required Core for 1001.
✓ ORG and MATERIALS agree with each other on notebook order and required core. Both are stale in the same ways noted above; the exercise lists disagree.
✓ Material Files: neither doc has a Material Files section, and there is no `materials/10-functional/` folder. The archive path `materials/_archived/ch10-functional-stray/` named in ORG exists (contains `generics.ipynb`).

### Slide deck ↔ chapter
✗ Stale numbering: the title slide (overview.md line 40) reads "*11.0 Intro · 11.1 Functional Concepts · 11.2 Functional Practice*". It should be 10.0/10.1/10.2. The same text is in the rendered `chapters/10-functional/overview.html`.
✗ Learning goals: the deck has no Learning Goals slide. None of the five landing-page goals (`1000` cell 3) appear in the deck.
✗ Section order and grouping: slide 10.1 runs Pure Functions → Immutability → Functions as Values → Lambda → **Map, Filter, and Comprehensions** → Higher-Order Functions → Decorators. The notebook teaches Comprehensions *after* decorators and puts `reduce` in Functions as Values. `## Choosing a Style` is a `1001` section (cell 35), but the deck places it at the end of 10.2, after functools. The deck has no slide for `FP in Python` or for Built-in Decorators / `@property`.
✗ Figures and tables not carried over (AGENTS.md: preserve figures and tables that carry conceptual structure): the programming-paradigm taxonomy diagram (`1001` cell 3), the `map`/`filter`/`reduce` syntax table (cell 17), and the Situation → Prefer decision table (cell 35) are missing. "Choosing a Style" is reduced to prose bullets that do not match the notebook's rubric (the ~30-character lambda rule, lazy iterators, and the 2–3-step pipeline check are all absent).
✗ Slide examples not in the chapter: "Higher-Order Functions" uses a `make_multiplier` closure. The notebook uses `apply_twice` (cell 24) and teaches no closures (closures are planned for 4.2), so the slide teaches something the chapter does not. Most other slide examples (`add_tax`, the `cart` list, `apply(square, nums)`, `announce` decorator, `section` context manager) also do not appear in the notebooks. The chapter's own examples (student tuples, `settings` dict unpacking, `timer_decorator`, `CountCalls`, `timer()` context manager) are not shown. Matching examples: `factorial`, `power` + `partial`, `fib` + `lru_cache`.
✗ Missing content: the deck omits `@wraps` (taught `1001` cells 26, 29, 31; listed in MATERIALS' slide list), class-based decorators (MATERIALS slide list), dict/set comprehensions, and recursion vs. iteration (`1002` cell 7; MATERIALS slide list).
✓ No slide refers to a removed or renamed topic, apart from the closure example above.

### Preview ↔ chapter
✓ All 10 questions (q1–q10) map to glossary terms taught in 1001/1002: FP, pure function, side effect, first-class, higher-order, map, filter, reduce, decorator, `functools`. None tests a moved topic, and all are recognition-level, which suits pre-reading.

### Homework ↔ chapter
✗ Coverage gap versus the docs: MATERIALS lists the homework as "Implement a recursive solution and compare it to an iterative equivalent". No homework item involves recursion. ORG Required Core says 1002's recursion, `functools` essentials, and context managers must be "assessed". Homework covers none of recursion, context managers, `partial`, or `lru_cache`, and has no decorator coding item (Q5 is true/false only). Only `reduce` (Q10) comes from the functools material.
✗ Difficulty ladder: the coding items Q6–Q10 are all one-line transformations (comprehension, `map`, `filter`, `sorted`, `reduce`) at about the same level. No item rises above the 1001 exercise, so the ladder does not reach 1002 content.
✓ All functions and datasets used (lists, tuples, dicts, `lambda`, `map`, `filter`, `sorted(key=..., reverse=True)`, `functools.reduce`) are taught in the chapter or earlier; no removed example is referenced.

### Lab ↔ chapter
✓ Dataset: the `students` (name, grade, year) tuples match the `1001` cell 36 exercise, and the tasks (sort+map, reduce, recursion, dict comprehension, function returning a lambda) match MATERIALS' lab description.
✗ Q1 (cell 1) requires a tuple sort key with negation, `key=lambda s: (s[2], -s[1])`. Multi-key tuple keys and the negation trick are not taught in Chapter 10 or earlier; no earlier notebook has a `key=lambda ...: (` pattern.
✗ Q2 (cell 3) requires `reduce` with an initializer ("starting from 0", solution `reduce(..., students, 0)`). The chapter shows only the two-argument form (`1001` cells 17, 22; `1002` cells 13–14). `1001` cell 21 mentions only what happens "when accumulator is not initialized".
✗ Q5 (cell 9) requires a function that returns a lambda capturing `min_grade`, which is a closure. The chapter text never teaches returning a function outside the decorator wrapper pattern, and closures are only planned for 4.2 (not yet written). Until 4.2 exists, the lab depends on an untaught technique.
✓ Submission: the 5 `thebe-interactive` question cells match the grader's `slice(0, 5)` and its q1–q5 feedback rows; assignment ID `ch10-lab` follows the naming convention.

### Glossary ↔ content
✓ All 20 glossary terms (`1000` cell 5) are introduced in 1001/1002. ("Partial function" matches `1002` cell 15's partial-application text; "Cached result" matches cell 17.)
✗ Key terms used in the notebooks but missing from the glossary: **memoization / memoize** (`1002` cell 17; also the slides), **partial application** (bolded, `1002` cell 15), **LRU / Least Recently Used** (bolded, `1002` cell 17), **deterministic** (bolded, `1001` cell 8), **programming paradigm / imperative / declarative** (`1001` cell 3), **accumulator / fold** (`1001` cell 22, `1002` cell 13), **wrapper function / `@wraps`** (`1001` cells 26, 29).

### Summary
Sync issues: 28
  - Planning docs: 16 (missing `orphan: true`; stale 1000 role; 1001 sequence and unlisted topics; `reduce` taught twice; tree traversal only in a hidden cell; recursion-vs-iteration table missing; stale exercise lists; functools exercise untagged with its solution visible; missing per-section exercises; re-teaching of 4.2 pure functions and lambda with no back-links; stale "covered in ch12"; `yield` used before Ch11; `__call__` untaught; `itertools.chain` untaught; wrong 1002 Summary; landing goals omit comprehensions)
  - Slides: 6 (11.x numbering; no learning goals; section order; dropped figures and tables; closure and other off-chapter examples; missing `@wraps`, class decorators, recursion vs. iteration)
  - Homework: 2 (no 1002 or recursion coverage despite the docs; flat ladder)
  - Lab: 3 (tuple and negation sort key, `reduce` initializer, closure all untaught)
  - Glossary: 1 finding covering 7 missing terms

### Top fixes
1. **Fix the `1002` functools exercise tags**: add `thebe-interactive` to cell 19 and `hide-input` to cell 20. The solution is currently shown to students.
2. **Rewrite ORGANIZATION.md and MATERIALS.md to match the notebooks**: add `orphan: true`; move the purity, immutability, and first-class framing from 1000 to 1001; give the real 1001 `##` order (FP in Python → Purity & Immutability → Functions as Values → Higher-Order Functions/Decorators → Comprehensions → Choosing a Style); put the Sorting/Filtering exercise under 1001 and the functools exercise under 1002; decide where `reduce` lives and remove the duplicate copy.
3. **Close the lab's untaught-technique gaps**: in 1001 text, teach `reduce`'s initializer argument and multi-key tuple sort keys (or simplify lab Q1 and Q2), and teach returning a function from a function (or add a link once the closures section in 4.2 exists) before lab Q5 depends on it.
4. **Fix the slide deck**: correct the "11.x" title numbering; add a Learning Goals slide; reorder to the notebook's `##` sequence, with Choosing a Style in 10.1; restore the paradigm diagram, the map/filter/reduce table, and the decision table; replace the `make_multiplier` closure with the notebook's `apply_twice` or `timer_decorator`; then regenerate `overview.html`.
5. **Assess 1002 required core in homework and explain `yield` in context managers**: add a recursive-vs-iterative coding item (as MATERIALS specifies) and a `partial`/`lru_cache` item; in `1002` cell 10, add a short explanation of how `yield` splits setup from cleanup (with a forward link to Chapter 11); and change "covered in ch12" in `1001` cell 26 to 9.3.
