---
orphan: true
---

## Sync Audit — Ch06: Collections

Files audited as they are on disk: `chapters/06-collections/{ORGANIZATION.md, MATERIALS.md, 0600-collections, 0601-lists, 0602-tuples, 0603-sets}.ipynb`, `assignments/{preview,lab,homework}.ipynb`, `_html_extra/chapters/06-collections/overview.md`, and `_toc.yml` (lines 83-97). Neither ORGANIZATION.md nor MATERIALS.md has a `## Planned Additions` section.

Exercise counts found (code cells whose first line is an exercise header, each followed by a `hide-input` solution): 0601 = 16 (all headed `### EXERCISE:` in upper case), 0602 = 13, 0603 = 7.

### Planning docs ↔ chapter

✗ **Front matter:** neither `ORGANIZATION.md` nor `MATERIALS.md` starts with MyST front matter `orphan: true`. Both start directly with `# Chapter 06 ...`.

✓ **Notebook inventory:** both docs name 0600-0603 and the four assignment notebooks. That matches the folder and `_toc.yml` lines 83-97.

✓ **`##` section sequence:** the five `##` topics listed for each of 0601, 0602, and 0603 match the notebooks' `##` headings word for word and in order.

✗ **Topics taught but not listed, or taught under a different topic than the doc says** (the docs give only `##` titles, so what sits under each title is undocumented):
  - 0601 teaches content that neither doc names: `### Lists and Strings` / `split()` / `join()` (cells 27-36), `### List Unpacking` including star and function-call unpacking (cells 98-112), `### Objects and Values` (identity with `is`, equivalent vs. identical; cells 137-151), and `### any() and all()` (cells 176-181).
  - In 0602, `zip()` (cell 91), `enumerate()` (cell 109), `### Comparing and Sorting` (cell 116), and `### Sorting by Value` (cell 130) all sit under `## Tuple Operations and Immutability`. The docs put "Iteration Helpers" in `## Unpacking and Iteration Helpers`, but that section (cells 38-64) teaches only unpacking. Meanwhile `## Tuple Operations and Immutability` never teaches immutability: the TypeError demo is at cell 10, under `## Creating and Accessing Tuples`.
  - 0603 teaches hashability (`### Hashable vs. Mutable Values` cell 15, `### Hashability` cells 21-24) and in-place set operations (`### In-place Set Operations`, cells 19-20: `update`, `intersection_update`, and the rest) inside `## What Sets Are`. That puts them before Set Algebra and before `## Mutating Sets Safely`, although the docs place hashability in the last section.
  - 0603 `### Set Comprehensions` (cells 115-119) and `### Comparing with dict Comprehension` (cells 120-121) sit under `## Frozensets, Hashability, and Performance` and appear in neither doc.

✗ **Duplicate coverage the docs do not resolve:** unpacking with `*` is taught in full twice (0601 cells 98-112 and 0602 cells 40-64), and so are `enumerate`/`zip` (0601 cells 88-97 and 0602 cells 91-115). Inside 0601, `### Aliasing` appears twice: under `## Aliasing, Copying, and Nested Lists` (cell 116) and again under `## Lists in Practice` (cell 152), with the same content. The docs list "Unpacking and Iteration Helpers" only for 0602.

✗ **Exercise lists:** neither doc has any exercise list. The notebooks contain 16 + 13 + 7 = 36 exercises. The docs are stale or incomplete here.

✗ **Required core / enrichment:** neither doc separates required-core topics from enrichment topics, so nothing in the notebooks can be checked against a core list. The unlisted topics above (split/join, `any`/`all`, set and dict comprehensions, `itertools.zip_longest` in 0602 cell 96, the performance demo in 0603 cell 112) cannot be classified.

✗ **Topics covered only in exercises, and exercises that use material not yet taught:**
  - 0601 cell 35 (`EXERCISE: Using split() and join()`) asks students to "reverse the list", and the solution uses `words2.reverse()`. At that point `.reverse()` has not been taught. Later it appears only as a row in the methods table (cell 77) and is never demonstrated. The same holds for `list.sort()`.
  - 0602 cell 174 (`Exercise: When to Use Tuples`, part 3) requires a tuple as a dictionary key. The text only asserts "Tuples are hashable" (cells 5 and 7) and never shows a tuple used as a dict key. The first explanation of dict keys and hashability comes in 0603 cell 15.
  - 0601 cell 21 (`EXERCISE: convert string to uppercase ...`) gives no input data. The `words` list exists only in the solution cell 22, so the task cannot be done as written.
  - 0601 cell 74 (`EXERCISE: Membership Testing`, part 3) expects `sum()` over a list of booleans. `sum()` is not introduced in this notebook until cell 83, and "True counts as 1" is explained only in the solution comment (cell 75). Chapter 02 covers `bool is int`, so this is a minor ordering issue.

✗ **Misplaced exercise:** 0601 cell 94 (`EXERCISE: Using List Methods`: `count`, `append`, `remove`, `index`) sits under `### Iteration Helpers: enumerate and zip`, between the zip demo and the enumerate/zip exercise. It belongs after `### List Modifying Methods` (cells 85-87).

✗ **Examples that use syntax not yet taught** (Ch07 Dictionaries comes after Ch06 in `_toc.yml`; Ch02 introduces only dict literals, indexing, `keys()`, and `values()`):
  - 0601 cell 93: `dict(zip(names, ages))`, before the `dict()` constructor has been taught.
  - 0601 cell 179: `any([pd.isna(v) for v in row])` and `df.columns`, although pandas has not been introduced.
  - 0602 cells 121-127: the `value_counts` counter dict with `counter[letter] += 1` and `.items()`, which are Ch07 patterns.
  - 0603 cell 35: `d.get('z', 'not found')`, before `dict.get` is taught (Ch07).
  - 0603 cell 121: dict comprehension `{x: x**2 for x in range(5)}`, before it is taught (Ch07/Ch10).

✗ **Checkpoint coverage** (ORGANIZATION.md's Build/Quality Checklist asks for "at least one checkpoint exercise per major section"): four sections have none.
  - 0601 `## What Lists Are`
  - 0601 `## Aliasing, Copying, and Nested Lists`: no exercise on shallow copy or `deepcopy`.
  - 0602 `## What Tuples Are`
  - 0602 `### Argument Packing` (cells 158-173): no practice of `*args`, `divmod(*t)`, or `(*q1, *q2)`. The exercise placed there (cell 174) tests list vs. tuple choice instead.

✗ **Material Files:** neither doc has a Material Files section. There is no chapter folder under `materials/`. The three archive paths named in MATERIALS.md do exist (`materials/_archived/ch06-lists-split/`, `ch07-tuples-original/`, `ch08-set-split/`).

✗ **Stale references in notebooks:**
  - 0601 cell 179 says "In Chapter 12 (Iterators & Generators)". Iterators & Generators is Chapter 11 (`chapters/11-iter-gen`); Chapter 12 is APIs.
  - 0602 cell 120 says "In the previous chapter, we wrote `value_counts`". The previous chapter is Ch05 Testing; `value_counts` is a leftover from Think Python's chapter order, where dictionaries come first.
  - 0603 cells 110-111 say "You already saw this in the **dictionary** section" and "This benchmark mirrors the dictionary example". Dictionaries (Ch07) come after this chapter.

✓ **ORGANIZATION.md ↔ MATERIALS.md:** the section order and the assignment list agree. MATERIALS.md additionally describes the lab topics accurately.

n/a **Learning objectives:** ORGANIZATION.md has no learning objectives to compare.

### Slide deck ↔ chapter

✗ **Learning goals:** the deck has no learning-goals slide. Separately, the landing page's four goals (0600 cell 3) cover lists only: list manipulation, identity/aliasing/copies, lists passed to functions, comprehensions/unpacking/`enumerate`/`zip`/sorting. No goal covers tuples (immutability, tuple returns, `*args`) or sets (set algebra, hashability, frozenset, O(1) membership), although 0602 and 0603 are two-thirds of the chapter and the 0600 intro bullets (cell 1) mention sets.

✗ **Section headings:** the deck does not follow the `##` sequence.
  - Missing sections: 0601 `What Lists Are` and `Lists in Practice` (no `is`/identity, list arguments in functions, or `any`/`all`); 0602 `What Tuples Are` and `Tuple Operations and Immutability` (no concatenation/repetition, `count`/`index`, `sorted`/`reversed`, lexicographic comparison, or sort by value); 0603 `What Sets Are`.
  - Merged or split sections: 0602 `Unpacking and Iteration Helpers` + `Tuples in Function Design` become one slide, "Unpacking and Function Design". 0603 `Set Algebra` + `Mutating Sets Safely` become "Set Algebra and Mutation". 0601 `Creating and Accessing Lists` is split into two slides.

✗ **Slide content the chapter does not teach:**
  - "Updating and Transforming Lists" slide: `del fruits[1]` (no `del` anywhere in 0601-0603), `nums.sort()`, `nums.sort(reverse=True)`, and `nums.reverse()`. These appear only as table rows in 0601 cell 77 and are never demonstrated.
  - "Unpacking and Function Design" slide: `pairs.sort(key=lambda p: p[1])`. The notebook teaches `sorted(items, key=second_element)` (0602 cells 132-138) and never uses `list.sort(key=...)`.
  - "Creating and Accessing Tuples" slide: "Performance: slightly faster" for tuples. The notebooks do not make this claim.

✗ **Figures and tables not preserved** (AGENTS.md: keep figures and tables that carry conceptual structure):
  - The aliasing / shallow copy / deep copy comparison table (0601 cell 114) is shown only as code on the slide.
  - None of the list state diagrams appear in the deck (0601 cells 8-9, 140-141, 147-148, 155-156, 171-172).
  - The set/list/dict comparison table (0603 cell 21) and the `list.pop`/`dict.pop`/`set.pop` table (0603 cell 80) are missing.

✓ **Key examples:** `min_max`, `*args`, zip of names/scores, set algebra on `{1,2,3,4}`/`{3,4,5,6}`, frozenset as a dict key, and the O(1) vs. O(n) membership comparison all exist in the notebooks. The notebooks' business examples (sales vs. target, the mississippi letter count, trimmed mean) are not on the slides; that is acceptable.

✓ **Rendered HTML:** `overview.html` (Aug 31 15:43) is newer than `overview.md` (Aug 31 09:33).

### Preview ↔ chapter

✓ All 10 questions (q1-q10) map to glossary terms taught in the chapter: collection, list/tuple/set properties, mutability, index, slice, list comprehension, aliasing, `zip`. No question tests a topic that moved to another chapter.

  (Coverage note, not a sync error: no question touches set operations, hashability, frozenset, or shallow vs. deep copy.)

### Homework ↔ chapter

✓ **References:** all 10 items (5 true/false, 5 coding) use current chapter concepts: list `append`, tuple unpacking, `sorted(set(...))`, alias vs. `.copy()`, set intersection. No removed examples or datasets.

✗ **Difficulty ladder is flat:** the five coding questions (cells 11-20) are each one or two lines at the same level. None exercises list comprehensions, `zip`/`enumerate`, starred unpacking, deep copy of nested lists, frozenset, or tuple returns, even though those are central to 0601-0603. Q6 (`append`) is easier than the preview.

### Lab ↔ chapter

✓ **Topics:** the lab covers Q1 list comprehensions, Q2 starred unpacking with `dict.items()`, Q3 set `intersection`/`difference`, Q4 aliasing vs. slice copy, and Q5 `dict(zip(...))` plus a comprehension over `.items()`. This matches MATERIALS.md and material taught in 0601-0603 (Q2 and Q5 rely on 0602 cells 59-61 and 106).

✓ **Submission:** the lab uses `ch06-lab` IDs throughout, has five `thebe-interactive` cells, and the grader panel has five rows. This follows the `chNN-type` naming rule.

### Glossary ↔ content

✓ **Existing terms:** all 20 glossary terms are introduced in 0601-0603. "Collection" is used but never defined in a content notebook; it is defined only in the landing glossary.

✗ **Key terms introduced in the notebooks but missing from the glossary:**
  - set comprehension (0603 cell 115)
  - argument packing / `*args` (0602 cell 158)
  - reference, and equivalent vs. identical (0601 cells 149 and 157)
  - sort key (0602 cell 133)
  - hash table / hash value (0603 cells 22 and 103)
  - lexicographic comparison (0602 cell 116)

### Summary

Sync issues: 20
  - Both planning docs are missing `orphan: true` front matter.
  - The planning docs have no exercise lists (36 exercises exist).
  - The planning docs have no required-core/enrichment lists; split/join, `any`/`all`, set/dict comprehensions, and `zip_longest` are unlisted.
  - Subsection placement drifts from the doc topic labels: zip/enumerate/sorting sit under 0602 "Tuple Operations and Immutability", and hashability and in-place ops sit under 0603 "What Sets Are".
  - Duplicate coverage: unpacking and `enumerate`/`zip` are taught in both 0601 and 0602, and `### Aliasing` appears twice in 0601.
  - Exercises use material not yet taught: `.reverse()` (0601 cell 35), a tuple as dict key (0602 cell 174), and `sum` of booleans (0601 cell 74).
  - 0601 cell 21 exercise has no input data.
  - 0601 cell 94 list-methods exercise is misplaced under Iteration Helpers.
  - Examples use untaught syntax: `dict()` (0601 cell 93), pandas (0601 cell 179), a dict counter (0602 cell 121), `d.get` (0603 cell 35), dict comprehension (0603 cell 121).
  - Four sections have no checkpoint exercise, including copying/deepcopy and argument packing.
  - The planning docs have no Material Files section.
  - Stale references: "Chapter 12 (Iterators & Generators)", "previous chapter ... value_counts", "already saw this in the dictionary section".
  - The slide deck has no learning-goals slide.
  - Landing-page learning goals cover lists only (no tuple or set goals).
  - Slide sections do not follow the `##` headings; five sections are missing and others are merged.
  - Slides show material the chapter never teaches: `del`, `list.sort()`/`reverse()`, `sort(key=lambda)`, and the "tuples slightly faster" claim.
  - Slides drop the aliasing/copy table, the state diagrams, and the set/list/dict comparison tables.
  - The homework difficulty ladder is flat and misses core skills.
  - Six key terms are missing from the glossary.
  - (Minor) "Collection" is defined only in the landing glossary.

### Top fixes

1. **Rewrite the landing-page learning goals (0600 cell 3) to cover the whole chapter**, adding tuple goals (immutability, returns and `*args`) and set goals (set algebra, hashability/frozenset, O(1) membership). Then add a matching learning-goals slide to `overview.md` and regenerate `overview.html`.
2. **Fix the three stale cross-references:**
   - 0601 cell 179: change "Chapter 12" to "Chapter 11".
   - 0602 cell 120: replace "In the previous chapter, we wrote `value_counts`" with a self-contained introduction.
   - 0603 cells 110-111: remove the claim that the dictionary benchmark came first, or move it to Ch07.
3. **Bring the planning docs up to the audit contract:**
   - Add `orphan: true` front matter.
   - Add per-notebook exercise lists (16/13/7).
   - Add required-core vs. enrichment lists, and decide where split/join, `any`/`all`, and set/dict comprehensions belong.
   - Add a Material Files section.
4. **Resolve duplicated and misplaced content:**
   - Assign unpacking and `enumerate`/`zip` to one notebook, and cross-reference them from the other.
   - Move 0602 `zip`/`enumerate` under `## Unpacking and Iteration Helpers`.
   - Merge the duplicate 0601 `### Aliasing` sections.
   - Move 0601 cell 94 after the modifying-methods section.
   - Move 0603 hashability and in-place set operations into their planned sections.
5. **Close the exercise and teaching gaps:**
   - Add a short taught example of `sort()`/`reverse()` before 0601 cell 35.
   - Show a tuple used as a dict key in 0602 before cell 174.
   - Give 0601 cell 21 its input data.
   - Add checkpoint exercises for shallow vs. deep copy and argument packing.
   - Remove `del`, `list.sort(key=lambda...)`, and the "slightly faster" claim from the slides, or teach them.
