---
orphan: true
---

## Sync Audit — Ch09: Object-Oriented Programming

Files audited (working tree, 2026-09-25): `chapters/09-oop/{ORGANIZATION,MATERIALS}.md`, `0900-oop.ipynb`, `0901-oop-design.ipynb`, `0902-oop-pillars.ipynb`, `0903-oop-advanced.ipynb`, `assignments/{preview,lab,homework,index}.ipynb`, `_html_extra/chapters/09-oop/overview.md`, `_toc.yml` (lines 133-149).

Actual `##` structure on disk:

- 0901: Why Object-Oriented Programming? · Classes and Instances · A First Class (### Custom and Built-in Types, ### Classes and Methods, ### Defining Methods) · More Methods (### `__init__`, ### Properties, ### `__str__`, ### `__repr__`) · Summary
- 0902: Encapsulation (### Access Level Conventions) · Polymorphism · Inheritance (### Parents and children, ### Specialization, ### `issubclass()`, ### Class Variables, ### Method Overriding) · Abstraction (### Abstract Base Classes) · Summary
- 0903: Comparison Dunder Methods (### `__lt__`, ### `__hash__`) · Operator Overloading · `@dataclass` (### Frozen) · namedtuple · Class vs. Instance Variables (### Mutation Trap) · Static and Class Methods · Summary

Exercises on disk: 0901 has 4 (Classes vs Instances [14], A First Class [40], Independent Class Practice [66], Chapter Review [69]); 0902 has 2 (Banking Polymorphism [19], Four Pillars Review [65]); 0903 has 2 (namedtuple [28], Class Methods [46]).

### Planning docs ↔ chapter

✗ Front matter: neither `ORGANIZATION.md` nor `MATERIALS.md` starts with MyST front matter `orphan: true`; both open directly with `# Chapter 09 ...`.

✓ Notebook inventory: the four notebooks named in both docs exist and match `_toc.yml`. No `materials/` folder exists for ch09, and the docs have no Material Files section, so there is nothing to check.

✗ Section sequence, 0901 (ORGANIZATION.md "Scope and Sequencing" item 2; the docs are stale):
  - Listed but not taught anywhere in the chapter (all four notebooks searched):
    - `Temperature` checkpoint exercise
    - object copying
    - pure functions vs. modifier methods
    - design-first development (`Time` as a base-60 example)
    - "programmer-defined types / object mutation". The mutation topic is not taught. "Programmer-defined types" survives only as a phrase in the Summary [68].
  - Listed for 0901 but taught in 0903: `@staticmethod` and `@classmethod` (0903 "## Static and Class Methods"), and operator overloading (0903 "## Operator Overloading").
  - "A first class (`Point`)" is inaccurate. `Point` appears under "## Classes and Instances" [9]. "## A First Class" [16] uses `CalcTwo`.
  - Taught but not listed:
    - "## Why Object-Oriented Programming?" [3-5], the procedural-banking motivation. The docs place "what OOP is and why" and the class/instance analogy in 0900, but 0900 has only a bullet list, and the cookie-cutter analogy is in 0901 [6].
    - The `CalcTwo` example
    - The function-syntax vs. method-syntax call and the "receiver" [27-39]
    - The `BankAccount` running example [43]

✗ Section sequence, 0902 (the docs are stale):
  - ORGANIZATION.md lists the pillars as encapsulation, inheritance, polymorphism, abstraction. The notebook order is Encapsulation → Polymorphism → Inheritance → Abstraction.
  - The docs describe 0902 as "one focused example per pillar". In fact 0902 now teaches `super()` [24-30], specialization [37], `issubclass()` [51], class variables [55], method overriding [57], and ABCs (`abc`) [61]. ORGANIZATION.md assigns all of these to 0903.
  - Taught but not listed: access-level conventions and name mangling [11-12].

✗ Section sequence, 0903 (the docs are stale, and required core is missing):
  - Listed but not taught anywhere in ch09:
    - composition vs. inheritance decision guide or rubric
    - `Protocol`
    - the card game (Card, Deck, comparing, deck operations)
    - Hand as a subclass of Deck
    - shallow vs. deep copy (`copy.copy` / `copy.deepcopy`)
    - "additional advanced object-state topics"
  - Object identity vs. equality (`is` vs `==`) appears only incidentally, in 0901 [13] (unexplained output) and 0903 [5-8] (inside the `__eq__` discussion). There is no dedicated treatment.
  - Listed for 0903 but taught in 0902: inheritance, polymorphism, method overriding, parent/child classes, `super()`, `issubclass()`, ABC.
  - Taught but not listed:
    - comparison dunders (`__eq__`, `__lt__`, `@total_ordering`, `__hash__`) [4-12]
    - frozen dataclasses [21]
    - class vs. instance variables and the mutation trap [30-35]
    - static and class methods [36-45]
    - operator overloading [13-17], which is listed under 0901

✗ Required core not taught: 0903's "composition vs inheritance rubric" appears in both docs and is taught nowhere. 0901's "design-first development" appears in ORGANIZATION.md and is also taught nowhere. MATERIALS.md 0901 required core adds "object mutation and copying" and "pure functions vs. modifier methods"; neither is taught.

✗ Content in neither the required-core list nor the enrichment list:
  - 0903 comparison dunders / `@total_ordering` / `__hash__`
  - 0903 frozen dataclasses
  - class vs. instance variables. These are taught twice: in 0902 "### Class Variables" [55-56] and in 0903 "## Class vs. Instance Variables" [30-35]. That duplication should be resolved.
  - 0902 name mangling

✗ Exercise lists: the only exercise either doc names is the "`Temperature` checkpoint exercise" (ORGANIZATION.md 0901), and it does not exist. Neither doc lists the 8 exercises on disk (0901 ×4, 0902 ×2, 0903 ×2, listed above).

✗ Exercise cell tagging, 0903 namedtuple exercise: cell [28] "### Exercise: namedtuple" has no `thebe-interactive` tag. Its solution cell [29] ("### solution", lowercase) has no `hide-input` tag, so the solution renders in full. This also breaks the ORGANIZATION.md build checklist ("Tag question cells `thebe-interactive` and solution cells `hide-input`").

✗ Exercises that use syntax or idioms the text has not taught:
  - 0901 [66] "Independent Class Practice" requires "Use an empty list for `tasks` when no list is provided". The solution [67] uses the `tasks=None` / `tasks if tasks is not None else []` idiom. No text in ch01-09 teaches the mutable-default/`None` sentinel pattern. 0903 [35] mentions it only in passing, and only later.
  - 0903 examples [8], [10], [12], [15] `return NotImplemented`, and the text never explains `NotImplemented` (it appears in no earlier chapter either).
  - `math.hypot` [10] is also used without explanation (minor).

✗ Stale in-notebook references in 0903:
  - [41] says "Here is `from_string` rewritten as a class method", but no `from_string` precedes it. The static-method example [38] is `parse_record`.
  - The intro table [3] says the notebook "covers five topics" and omits the "## namedtuple" section.

✓ Learning objectives: ORGANIZATION.md has no learning-objectives section, so there is nothing to conflict with the landing page. See the slide and glossary sections for landing-page gaps.

✗ ORGANIZATION.md and MATERIALS.md disagree:
  - Required core for 0903: MATERIALS.md includes `namedtuple`; ORGANIZATION.md omits it.
  - `@staticmethod`, `@classmethod`, `@property`: MATERIALS.md lists them as 0901 required core. ORGANIZATION.md makes the `@classmethod`/`@property` "deep dives" enrichment and omits them from 0901 required core.
  - Copying: ORGANIZATION.md lists it in both 0901 and 0903. MATERIALS.md has it as 0901 required core and 0903 enrichment.
  - The MATERIALS.md "Delivery Order" puts "encapsulation principles" in 0901. Encapsulation is taught in 0902.

✗ Stale references:
  - ORGANIZATION.md "Known Structural Issues" discusses the card-game section of 0903 ("still mixes conceptual content ... with a long applied project"). No card game exists.
  - The Planned Additions entry (Enum "applied to the card game's suits and ranks"; MATERIALS.md "Enum version of the card game's suits and ranks") depends on the missing card game. The Enum topic itself is known and not flagged, but its anchor example needs re-planning.
  - The "merged from former `1202-oop-methods`" notes cite an obsolete chapter-12 numbering. They are harmless history, but they could be dropped.
  - MATERIALS.md Homework says "Model a real-world entity as a class hierarchy with at least two levels". The actual homework has no inheritance task (see Homework).

### Slide deck ↔ chapter

✗ Stale numbering: the title slide (overview.md line 41) reads "*10.0 Intro · 10.1 Design & Methods · 10.2 Four Pillars · 10.3 Advanced Topics*". The chapter is 9, and the section dividers already say 9.1 / 9.2 / 9.3.

✗ Learning goals: the deck has no learning-goals slide. The five goals in 0900 [3] do not appear.

✗ Section headings and order, 9.2:
  - The slide order is Encapsulation → Inheritance → Polymorphism → Abstraction (lines 258, 304, 345, 385). The notebook order is Encapsulation → Polymorphism → Inheritance → Abstraction.
  - The deck's own 9.2 divider (line 254) lists "Encapsulation · Polymorphism · Inheritance · Abstraction", which contradicts the slides that follow it.
  - `issubclass()`, specialization, and method overriding (0902 ### headings) get only one-line bullets on the Inheritance slide.

✗ Section headings, 9.1 and 9.3:
  - 9.1 has no "A First Class" slide (the `CalcTwo` example and the method-call syntax).
  - 9.3 has no slide for "## namedtuple", which the landing glossary (term 16) and MATERIALS.md required core both include.
  - The 9.3 divider subtitle (line 476) omits namedtuple and class vs. instance variables.

✗ Slides teach content the notebooks do not:
  - `@balance.setter` / `@attr.setter` on the Properties slide (lines 213, 233-242) and the Quick Reference (line 737). 0901 teaches only read-only properties ([55-56] demonstrates that assignment raises `AttributeError`).
  - "Duck typing" (Polymorphism slide, lines 352-357) appears in no notebook.
  - The Key Dunder Methods and Operator Overloading slides list `__contains__`, `__getitem__`, `__sub__`, and `__mul__`. None are in the notebooks; `__len__` appears only in a table in 0903 [13].

✗ Key examples: most slide examples do not match the notebooks' running examples.
  - Slides use `Thermostat` (encapsulation), `Animal/Dog/Cat` speak override, `Shape/Circle/Rectangle` (polymorphism), `Vector` (operator overloading), `Temperature` (static/class methods), and `Player`.
  - The notebooks use `Employee`/`BankAccount` (encapsulation), `Checking/Savings/LoanAccount.month_end()` (polymorphism, without inheritance), `SavingsAccount`/`BusinessAccount` (inheritance), `BankAccount.__add__` (operator overloading), and `BankAccount.parse_record`/`from_string` (static/class methods).
  - Still aligned: `Point` (Classes and Instances; `__hash__`), `PaymentMethod` ABC, `Student` dataclass, `SavingsAccount` + `super()`.

✓ No slide references a topic that was removed from the chapter. The problem runs the other way: slides add setter, duck typing, and extra dunders, as flagged above.

✓ The rendered `chapters/09-oop/overview.html` (Aug 31) is newer than `overview.md` (Jun 4), so the render is current with its source. It will need regenerating after the fixes above.

### Preview ↔ chapter

✓ All 10 questions (q1-q10, the `var questions` array in preview.ipynb cell 1) map to taught ch09 concepts: OOP, class, instance, attribute, method, `self`, `__init__`, encapsulation, inheritance, `@dataclass`. None tests a moved topic, and all suit pre-reading.

### Homework ↔ chapter

✓ Submission wiring uses `ch09-homework` throughout (5 true/false questions `ch09-hw-q1..q5`, plus 5 coding cells mapped to q6-q10).

✗ Coverage and difficulty ladder:
  - All 10 items test 0901-level basics: class, instance, method, encapsulation (T/F), plain-attribute object, `__init__`, a method, `__str__`, sorting by attribute.
  - No coding item exercises 0902 or 0903 content: no inheritance/`super()`, `@property`, ABC, comparison dunders, `@dataclass`/`namedtuple`, or class methods. Inheritance appears only as T/F item 5.
  - The ladder is flat and stops before the chapter's second half.
  - This also contradicts MATERIALS.md ("Model a real-world entity as a class hierarchy with at least two levels").

✗ Untaught technique, homework Q6 [11-12]: the task expects an empty `class Ticket: pass` with attributes attached from outside (`ticket.owner = "Ana"`). No ch09 text teaches adding attributes to an instance outside `__init__`.

✓ Q10 [19-20], `sorted(..., key=lambda ...)`, was used in ch08 0803, so it counts as taught earlier.

### Lab ↔ chapter

✓ The lab's five questions use the `BankAccount` domain, consistent with the chapter's running example:
  - `MoneyMarketAccount` subclass with `super()`
  - `__str__`/`__repr__`
  - class attribute plus read-only `@property`
  - `@classmethod` factory
  - `__eq__`/`__lt__` with `sorted()`
  These match the MATERIALS.md lab description. Submission wiring (`ch09-lab`, 5 thebe cells → q1-q5) matches the structure.

✗ Techniques not taught in the text:
  - Q1 [1] ("Methods are values too: store the bound method in a variable") and Q4 [7] ("Classmethods are values too: store `BankAccount.from_string` in a variable") rely on bound methods as first-class values. No ch09 notebook, and no earlier chapter, explains bound methods.
  - Q2 [3] expects `print(accounts)` to use `__repr__` for each list element. 0901 [64] says `__repr__` is used by `repr()` and the interactive shell but never says containers use it.
  - The Q2 solution also uses `{self.owner!r}`, which appears in no ch09 text (minor, since the expected output is given).

### Glossary ↔ content

✓ All 20 glossary terms in 0900 [5] are introduced in a content notebook:
  - OOP, class, instance, attribute, method, `self`, constructor, dunder: 0901
  - the four pillars, method overriding, `super`: 0902
  - dataclass, `namedtuple`, class/instance variable, static/class method: 0903 (class variables also 0902)

✗ Key terms that notebooks introduce (bolded or given a heading) but the glossary lacks:
  - property / `@property` (0901 ### Properties; also a landing-page learning-goal-level feature)
  - abstract base class / abstract method (0902 ### Abstract Base Classes)
  - operator overloading (bolded in 0903 [17]; also named in landing learning goal 5)
  - name mangling (0902 [5], [12])
  - subclass / superclass (bolded in 0902 [36])
  - special method (bolded in 0901 [63]; synonym of the "Dunder method" entry)
  - receiver (bolded in 0901 [39])
  - hashable / `__hash__` (0903 ### Hashability)

✗ Landing learning goals vs. content: the five goals in 0900 [3] cover only 0901-level material. No goal covers the four pillars/inheritance (0902) or the dataclass/namedtuple/class-vs-instance/static-and-class-method material (0903), although those are the bulk of the chapter and appear in the glossary.

### Summary

Sync issues: 27
  - Both planning docs lack `orphan: true` front matter
  - ORGANIZATION 0901 sequence is stale: Temperature, Time/design-first, pure vs. modifier, copying, and mutation are not taught; static/class methods and operator overloading moved to 0903; Point/CalcTwo mislabeled; Why-OOP and the method-call syntax are unlisted
  - ORGANIZATION 0902 pillar order differs from the notebook
  - ORGANIZATION does not record that super/issubclass/overriding/specialization/class vars/ABC now live in 0902
  - ORGANIZATION 0903 lists a card game, Hand/Deck, Protocol, composition-vs-inheritance, and shallow/deep copy that do not exist
  - ORGANIZATION does not list 0903's comparison dunders, frozen dataclasses, class vs. instance vars, and static/class methods
  - Required core untaught: composition vs. inheritance rubric; design-first development (plus MATERIALS' mutation/copying and pure vs. modifier)
  - Content in neither tier; class variables taught twice (0902 and 0903)
  - Exercise lists in the docs are stale (names a nonexistent Temperature exercise; 8 real exercises unlisted)
  - 0903 namedtuple exercise and solution are untagged (solution visible)
  - 0901 TaskList exercise needs the untaught `None`-default idiom
  - 0903 uses `NotImplemented` without explanation
  - 0903 [41] references a nonexistent prior `from_string`; the intro table says "five topics" and omits namedtuple
  - ORGANIZATION and MATERIALS disagree (namedtuple core; static/class/property tier; copying; encapsulation placement)
  - Stale Known Structural Issues and Planned Additions anchor (card game); stale MATERIALS homework description
  - Slide title uses "10.x" numbering
  - Slides have no learning-goals slide
  - Slide pillar order differs from the notebook and from the deck's own divider
  - Slides lack "A First Class" and namedtuple sections
  - Slides teach `@setter`, duck typing, and extra dunders that are absent from the notebooks
  - Most slide examples differ from the notebooks' banking examples
  - Homework covers only 0901 basics; no 0902/0903 coding; flat ladder
  - Homework Q6 needs untaught external attribute assignment
  - Lab Q1/Q4 rely on untaught bound-method-as-value
  - Lab Q2 relies on the untaught "containers use `__repr__`" rule (and `!r`)
  - Glossary misses property, ABC/abstract method, operator overloading, name mangling, subclass/superclass, special method, receiver, hashable
  - Landing learning goals omit 0902/0903 content

### Top fixes

1. **Rewrite the ORGANIZATION.md and MATERIALS.md sequences to match the notebooks, and add `orphan: true`.**
   - Move static/class methods and operator overloading to 0903.
   - Move super/issubclass/overriding/class vars/ABC to 0902, in E-P-I-A order.
   - Add the comparison-dunder, frozen-dataclass, and class-vs-instance sections.
   - Delete (or move to Planned Additions) Temperature, Time/design-first, pure vs. modifier, copying, card game, Hand/Deck, Protocol, and the Known Structural Issues text.
   - Reconcile the two docs' required-core lists and list the 8 real exercises.
2. **Decide on the untaught required core.** Either write a short composition-vs-inheritance section with a checkpoint in 0903 (the only required-core topic in both docs that is missing), or demote it to Planned Additions. Re-anchor the Enum plan, which currently assumes a nonexistent card game.
3. **Add homework coding items for the second half of the chapter.** Cover a two-level inheritance hierarchy with `super()`, `@property`, and `__eq__`/`__lt__` or `@dataclass`, so the homework spans 0901-0903 and matches MATERIALS.md.
4. **Fix the slide deck.**
   - Change "10.x" to "9.x".
   - Add a learning-goals slide.
   - Reorder 9.2 to Encapsulation, Polymorphism, Inheritance, Abstraction.
   - Add "A First Class" and namedtuple slides.
   - Remove or teach `@setter`, duck typing, and `__contains__`/`__getitem__`/`__mul__`.
   - Swap in the notebooks' banking examples, then regenerate `overview.html`.
5. **Close the teaching gaps that exercises and assignments depend on.**
   - Tag 0903 [28] as `thebe-interactive` and [29] as `hide-input`.
   - Explain `NotImplemented` in 0903 [8].
   - Fix the `from_string` reference in [41].
   - Add a sentence each on bound methods as values, containers using `__repr__`, and the `None` default for mutable arguments. Otherwise, reword lab Q1/Q2/Q4 and 0901 [66].
   - Add the missing glossary terms (property, abstract base class, operator overloading, name mangling, subclass/superclass) and learning goals for 0902/0903.
