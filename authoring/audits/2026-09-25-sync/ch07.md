---
orphan: true
---

## Sync Audit — Ch07: Dictionaries

Scope: `chapters/07-dictionaries/` (0700 landing, 0701, 0702, 0703, `assignments/{preview,lab,homework}.ipynb`), `_html_extra/chapters/07-dictionaries/overview.md`, `_toc.yml` lines 98–113. Files audited as on disk (ch07 files have no uncommitted edits). Cell indices are 0-based notebook cell indices. ORGANIZATION.md has no "Planned Additions" section, so every doc/notebook mismatch is reported.

### Planning docs ↔ chapter

✗ Front matter — neither `ORGANIZATION.md` nor `MATERIALS.md` starts with MyST front matter `orphan: true`; both open directly with a `#` heading.

✓ Notebook inventory — both docs name 0700, 0701, 0702, 0703 and the three assignments; all exist and match `_toc.yml` (the `assignments/index.ipynb` wrapper is in `_toc.yml` but not named in the docs; harmless).

✗ Section sequence, 0702 — both docs say 0702 covers "... membership, and iteration". 0702's `##` headings are Accessing Items (4), Adding/Updating Items (18), Deleting Items (30), Membership Testing (35); there is no iteration section. `## Iterating Through Dictionaries` is in **0703 cell 36**. (0702 cell 13 only shows one loop over `keys()/values()/items()`.) Docs are stale.

✗ Section sequence, 0703 — taught but not listed in either doc:
  - `## Iterating Through Dictionaries` (0703 cell 36)
  - `## Why Dictionary Lookup Is Fast` (cell 46: hash tables, Big O note, list-vs-dict timing, `too_slow`/`much_faster` reverse-word pairs)
  - `### Accumulating Results from Dictionary Data` (cell 115: palindromes, accumulator, filtering)
  - ORGANIZATION.md also omits `## Counter` (cell 24) and `## defaultdict` (cell 30), which MATERIALS.md does list.
  - Listed order ("comprehensions, counting, grouping, inversion, sorting, dictionary-as-memory, tuple keys") otherwise matches the notebook order.

✗ ORGANIZATION.md ↔ MATERIALS.md — disagree on 0703 (Counter/defaultdict in MATERIALS only). Neither doc has exercise lists, a required-core/enrichment split, or learning objectives, so the exercise-list, required-core, and objectives checks cannot be run against the docs. Actual exercises: 0701 = 3, 0702 = 4, 0703 = 9 (Comprehension, Counting, Counter, defaultdict, Iterating, Dict vs. List Lookup, Inverting, Sorting by Value, Hashable Tuples).

✗ Stale doc claim — MATERIALS.md says the old lab draft "duplicated `homework.ipynb`'s word-frequency/`Counter`/`defaultdict` content". The current homework has no word-frequency, `Counter`, or `defaultdict` item (Q7 counts departments with `.get`; Q10 groups with `setdefault`).

✗ Topic covered only by an exercise — **tuple keys**. 0703 `### Tuple Keys` (cells 131–136) explains `hash()`/`id()`, has a hashability table, and shows a *failing* tuple key (`(1, [2, 3])`), but no text cell ever builds or reads a dict with a working tuple key. The only working tuple-key code is the exercise at 0703 cell 137 (`grid[(1, 2)]`). Ch06 does not fill the gap: 0602 cell 174 also uses tuple keys only inside an exercise. The lab (Q3) depends on this.

✗ Topic listed but not taught — `dict.setdefault()` appears only in the 0702 cell 3 method table and the cell 19 `{index}` entry; no text or example shows it. It is required by lab Q5 and used in the homework Q10 solution. `d.copy()` (table only) is also never shown.

✗ Topic not taught anywhere — **nested dictionaries**. None of 0701–0703 has a dict-of-dicts example or explanation (grep for "nest" finds only a nested *tuple*, 0703 cell 135), but nested dicts are used by slide "Creating Dictionaries", homework Q3 and Q8, and lab Q5.

✗ Exercises using syntax not yet taught at that point:
  - 0701 cell 12 (Read a Dictionary Diagram) asks for `len(courses)` and `"CS101" in courses`. `len` on a dict and dict membership are first taught in 0702 (cells 14 and 35).
  - 0703 cell 23 solution uses `max(result, key=result.get)`. `key=` functions for `max` are taught later in the same notebook (cell 98), and a bound method as a key function is never explained. (Ch06 0602 does show `max(items, key=second_element)`, so only the `result.get` form is new.)

✗ Duplicated and stale cross-chapter content (not acknowledged in docs):
  - 0703 `### Sorting Dictionaries` (cells 91–101) is nearly word-for-word the same as ch06 0602 `### Sorting by Value`: same `second_element` helper, same `max(items, key=second_element)`, and the same "mississippi" exercise and solution. Also, 0602 cell 120 says "In the previous chapter, we wrote `value_counts`", but `value_counts` is introduced later, in 0703 cell 18 (the dependency is inverted).
  - 0703 `### Dictionaries as Memory` (cells 102–113) repeats ch04 0403 `## Memoization` (same `known` dict and `fibonacci_memo`). It says "If you ran the fibonacci function from ... Chapter 4, maybe you noticed..." as if memoization were new.
  - 0703 cell 115: "In the previous chapter, one of the exercises asked you to write a function that checks whether a word is a palindrome". The previous chapter is ch06, which has no palindrome content; `is_palindrome` appears in ch05 0502 as a unit-testing example, not as an exercise.
  - 0703 cell 111: "In the notebook for this chapter, you'll see where these measurements come from" is left over from Think Python; this page is the notebook.

✗ Build checklist (ORGANIZATION.md) — "at least one checkpoint exercise per major section": 0703 `### Dictionaries as Memory` and `### Accumulating Results from Dictionary Data` have none. Notebook hygiene issues: 0703 cell 80 is a duplicate setup/import cell with no `hide-input` tag (it renders visibly under "Advanced Dictionary Patterns"), and cells 114 (`### Common Dictionary Patterns`) and 125 (`### Hashable Keys and Best Practices`) are empty headings immediately followed by another `###`.

✓ Materials — no Material Files section; no `materials/*07*` folder exists. The archive path `materials/_archived/ch07-dict-split/` exists (contains `0803-dict-advanced.ipynb`).

Minor accuracy note (not counted): 0701 cell 20 tells students to transfer skills from "list comprehension and tuple comprehension". Python has no tuple comprehension (parentheses produce a generator expression).

### Slide deck ↔ chapter

✗ Learning goals — the deck has no learning-goals slide, so none of the 5 landing-page goals (0700 cell 3) appear.

✗ Section headings/order —
  - 7.1: "What Is a Dictionary?" (merges When to Use) and "Creating Dictionaries" match.
  - 7.2: Membership Testing has no slide of its own. It is folded into "Accessing Items", ahead of add/update/delete, while the chapter teaches it last (0702 cell 35).
  - 7.3: there are no slides for Sorting Dictionaries, Dictionaries as Memory (only a quick-reference row "Memoize"), Accumulating Results, or Hashable/Tuple Keys (only callouts, no tuple-key example). Inverting appears on the Comprehension slide instead of after Why Lookup Is Fast.

✗ Inversion contradicts the chapter — the slide "Dictionary Comprehension" shows `{v: k for k, v in original.items()}` and warns "Duplicate values will overwrite each other". The chapter's `invert_dict` (0703 cell 86) solves exactly that case by mapping each value to a list of keys (the `parrot` example). The quick-reference row "Invert" repeats the comprehension form.

✗ Slide content not taught in the chapter —
  - nested dicts (`students["alice"]["gpa"]`, "Creating Dictionaries")
  - `for key in sorted(d)` and the `RuntimeError` warning about mutating a dict during iteration ("Iterating")
  - "view objects ... reflect changes to the dict in real time" (0702 cell 12 only says the methods return a "view")
  - Big-O rows for Insert/Delete, plus `time.perf_counter` and an unused `import random` (the chapter uses `time.time`, 0703 cell 50)
  - `counts.get(word, 0) + 1` counting idiom (the chapter teaches the `if letter not in counter` form, 0703 cell 18)
  - the `records` variable used on the defaultdict slide is never defined there

✗ Figures, tables, and key examples not preserved (AGENTS.md rule) — missing from the slides:
  - the dictionary state diagram (0701 cells 10–11)
  - the Fibonacci call-graph figure (0703 cells 106–108)
  - the hashable-types table (0703 cell 133)
  - the 6-way creation table (0701 cell 19; shown only as code)
  - the reverse-word pairs `too_slow`/`much_faster` case (0703 cells 51–75), which is the chapter's main evidence for O(1) lookup
  - the `value_counts('brontosaurus')` and `invert_dict('parrot')` examples

✓ Rendered HTML — `overview.html` (Aug 31 15:43) is newer than `overview.md` (Aug 31 09:33).

### Preview ↔ chapter

✓ All 10 questions map to current chapter content and glossary terms (dictionary, mapping, key, key-value pair, hashable, lookup, `get`, `items`, `Counter`, inverting). None test moved or removed topics, and the difficulty suits pre-reading.

### Homework ↔ chapter

✗ Q3 "Nested Dictionaries" (cell 5) and Q8 "Summarize nested records" (cell 15) test nested dicts, which no content notebook teaches.

✗ Q10 "Group values by key" solution (cell 20) uses `groups.setdefault(role, []).append(name)`. `setdefault` is untaught (see above), and the chapter's own grouping tool, `defaultdict(list)` (0703 cell 33), is not used.

✓ Q2, Q4–Q7, Q9 use `.get()`, `.items()`, list values, counting, and simple inversion, all present in 0702/0703. The difficulty ladder (5 T/F, then 5 short coding items) is reasonable. Note (not counted): no homework item exercises comprehensions, `Counter`, `defaultdict`, tuple keys, or sorting by value, which is roughly half of 0703.

### Lab ↔ chapter

✗ Q5 "Update a nested student record" (cell 9) requires `.setdefault()` (never taught) and nested-dict access/update (never taught).

✗ Q2 "Invert the capitals dictionary" (cell 3) requires inversion via a swapping dict comprehension. The chapter teaches inversion only through the loop-based `invert_dict` with list values (0703 cell 86); the swap form appears only on the slide.

✗ Q4 "Validate orders against inventory" (cell 7) tells students to "Use `inventory.get(sku, -1)` to test membership". This contradicts 0702 `## Membership Testing` (cell 35), which teaches `in`/`not in` as the membership test, and the `-1` sentinel pattern is taught nowhere. (MATERIALS.md documents this design, so doc and lab agree with each other but not with the text.)

✗ Q3 "Scores keyed by (name, subject)" (cell 5) depends on tuple keys, which the text never demonstrates (see Planning docs). It also uses nested unpacking `for (n, subject), value in scores.items()`, which is not shown in ch07.

✓ Q1 (dict comprehension over prices with `.items()`) matches 0703 cell 5. The submission panel collects exactly 5 `thebe-interactive` cells and has q1–q5 feedback rows, matching the 5 questions; lab id `ch07-lab` follows the naming rule.

### Glossary ↔ content

✓ All 20 glossary terms (0700 cell 5) are introduced in 0701–0703.

✗ Key terms bolded or indexed in the notebooks but missing from the glossary:
  - **hash table** (0703 cell 46, indexed)
  - **hash** / hash value (cell 130)
  - **Big O notation**, O(1)/O(n) (cell 48, indexed)
  - **memo** / memoization (cell 109, indexed)
  - **call graph** (cell 105)
  - **accumulator** (cell 122, indexed)
  - **filtering** (cell 124, indexed)
  - **sort key** (cell 94)
  - dictionary **view** (0702 cell 12)

### Summary
Sync issues: 22
  - Both planning docs lack `orphan: true` front matter
  - Docs place iteration in 0702; the section is in 0703
  - Docs omit 0703's Iterating, Why Lookup Is Fast, and Accumulating sections; ORGANIZATION.md also omits Counter/defaultdict (disagrees with MATERIALS.md)
  - Docs have no exercise lists, required core, or learning objectives
  - MATERIALS.md claims homework has word-frequency/Counter/defaultdict content (stale)
  - Tuple keys taught only via exercise (0703 cell 137)
  - `setdefault` listed in a table but never taught
  - Nested dictionaries never taught
  - 0701 cell 12 exercise uses dict `len`/`in` before 0702 teaches them
  - 0703 cell 23 solution uses `max(..., key=result.get)` before key functions are covered in 0703
  - 0703 Sorting and Dictionaries-as-Memory duplicate ch06 0602 and ch04 0403; ch06 0602 cell 120 refers back to `value_counts`, which is only introduced later in ch07
  - Stale references in 0703 cells 111 and 115
  - 0703 hygiene: visible duplicate setup cell 80, empty headings 114/125, two subsections with no exercise
  - Slides: no learning-goals slide
  - Slides: missing Sorting, Memory, Accumulating, and Tuple-keys sections; membership reordered
  - Slides: inversion semantics contradict `invert_dict`
  - Slides: teach nesting, `sorted(d)`, RuntimeError, live views, and extra Big-O rows not in the chapter
  - Slides: state diagram, call graph, hashable table, and reverse-pairs example not preserved
  - Homework Q3/Q8 nested dicts; Q10 solution uses `setdefault`
  - Lab Q5 needs `setdefault` and nested dicts; Q2 swap inversion untaught
  - Lab Q4 `.get` sentinel membership contradicts the taught `in`; Q3 needs tuple keys and nested unpacking
  - Glossary missing hash table, hash, Big O, memo, call graph, accumulator, filtering, sort key, view

### Top fixes
1. Add text sections to 0702/0703 that teach **nested dictionaries**, **`setdefault()`**, and a working **tuple-key** example (for example `grid[(1, 2)]`) before the Tuple Keys exercise. This removes the untaught dependencies in lab Q3/Q5, homework Q3/Q8/Q10, and the 0703 cell 137 exercise in one pass.
2. Rewrite ORGANIZATION.md and MATERIALS.md. Add `orphan: true`; move iteration to 0703; list Counter, defaultdict, Why Lookup Is Fast, and Accumulating; add per-notebook exercise lists, required core vs. enrichment, and learning objectives matching 0700; delete the stale homework claim.
3. Resolve the duplication with earlier chapters. Either trim 0703 Sorting Dictionaries (and its identical "mississippi" exercise) to a back-reference to 0602 and make 0703 Memory build on 0403 Memoization, or drop them from 0602/0403. Fix 0602 cell 120 ("previous chapter ... value_counts") and the stale references in 0703 cells 111 and 115.
4. Bring the slides in line. Add a learning-goals slide. Replace the swap-comprehension inversion with `invert_dict` (or teach both in 0703). Add Sorting, Memoization, and Hashable/Tuple-key slides with the call-graph figure, hashable table, and reverse-pairs example. Remove or teach nesting, `sorted(d)`, the RuntimeError warning, and live views. Regenerate `overview.html`.
5. Add the missing glossary terms (hash table, hash, Big O notation, memo/memoization, call graph, accumulator, filtering, sort key, view). Change lab Q4 to use `in`/`not in` rather than a `.get(..., -1)` sentinel.
