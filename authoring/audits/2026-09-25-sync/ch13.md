---
orphan: true
---

## Sync Audit — Ch13: Abstract Data Structures

Files audited (working tree, 2026-09-25): `chapters/13-abstract-data-structures/` (1300–1303, ORGANIZATION.md, MATERIALS.md, assignments/preview, lab, homework), `_html_extra/chapters/13-abstract-data-structures/overview.md`, `_toc.yml` lines 200–217. Topics under ORGANIZATION.md "## Planned Additions" (hash tables, linked lists, heaps/`heapq`, Dijkstra) are known and are not flagged.

### Planning docs ↔ chapter

✗ Front matter — neither `ORGANIZATION.md` nor `MATERIALS.md` starts with `orphan: true`. Both open directly with a `# Chapter 13 ...` heading.

✓ Notebook inventory — both docs name 1300–1303 and the four assignment files. These match the folder and `_toc.yml` (lines 200–217).

✗ Section sequence, 1301 — ORGANIZATION.md lists "ADT concept; interface vs. implementation; the ADT contract". MATERIALS.md expands this to "the ADT contract: operations, preconditions, postconditions" and "`collections.deque` as a motivating example". The notebook differs in four ways:
  - **Not taught: the ADT contract.** No 1301 text mentions "contract", "precondition", or "postcondition". A search of all Ch13 notebooks finds no hits.
  - **Terminology mismatch.** The word "interface" never appears in 1301 prose; it shows up only in cell IDs. The text uses "abstract data type" and "concrete implementation" (cell 3). The slides and preview q2 use "Interface".
  - **Not listed: the second section.** `## Operation Tradeoffs` (cells 8–12) is in neither doc. It holds the list/set/dict/deque Big-O table, the structure-choice table, the `time.perf_counter` membership benchmark, and the O(1)/O(n)/O(log n) note.
  - **`deque` is barely taught.** It appears only as a table row (cell 3) and in cell 5, which is tagged `hide-input` and `skip-execution`. The text never explains it.
  - Verdict: the docs are stale for Operation Tradeoffs. The ADT contract is either unwritten or should be dropped from the docs.

✗ Section sequence, 1302 — ORGANIZATION.md lists a "call stack analogy" under Stack. It is not taught: 1302 cell 3 only lists "recursion" as a use case, and "call stack" first appears in Ch14 (1401–1403). The `## Stack or Queue?` section (cells 13–15, with its six-row comparison table) is taught but listed in neither doc. All other 1302 items match: LIFO, push/pop, list-backed `Stack`, balanced brackets as expression evaluation, FIFO, and `deque`.

✗ Section sequence, 1303 — the docs list tree and graph topics the notebook never teaches, and none of them is under Planned Additions:
  - **Not taught.** A search of 1301–1303 finds no binary tree, pre-order/in-order/post-order traversal, adjacency matrix, "height", "depth", or directed vs. undirected. BST basics appear only as a sentence in 1301 cell 10 (O(log n) note) and a table row in 1301 cell 8.
  - **What the notebook actually teaches.** 1303 uses a general n-ary `TreeNode` org chart (cell 4) and a recursive `print_tree` (cell 5), with no prose on how the traversal works. It uses only an adjacency-list `dict` (cell 9).
  - **Heading structure.** 1303 has a separate `## Graph Traversal` heading (cells 12–19), where the docs fold BFS/DFS into "Graphs". The order is consistent.
  - Verdict: either write the binary-tree/BST/traversal/adjacency-matrix content (the slides already present it) or move it to Planned Additions and trim the required core.

✗ Exercise lists — neither doc lists exercises. The notebooks have 9 `### Exercise:` cells:
  - 1301: One Behavior, Two Implementations; Match Workload to Structure
  - 1302: Reverse Words with a Stack; Serve a Customer Line; Pick the Structure
  - 1303: Add a Tree Node; Add a Graph Edge; Stop at a Target; Compare Traversal Orders

  The "at least one checkpoint exercise per major section" rule is met.

✗ Syntax used before the text teaches it (house rule) — four cases:
  - **1301 cell 6** (Exercise: One Behavior, Two Implementations) asks students to build a stack with `deque` `append`/`pop`. `deque` is not taught in any earlier chapter; a search of Ch01–12 finds no hits. In Ch13 its methods appear only in cell 5, which is `hide-input` and `skip-execution`, so it shows no output.
  - **1302 cell 10** holds the only `Queue` implementation and the only `deque.popleft()` code, and it is tagged `hide-input`. The core Queue teaching example is hidden by default, and exercise cell 11 depends on it.
  - **1303 cell 13** holds the BFS implementation, tagged `hide-input`. Exercise cell 14 ("Modify breadth_first_search...") asks students to modify code they cannot see by default. The only prose on BFS is one bullet (cell 12).
  - **1303 cell 5** (`print_tree`) is the chapter's only tree traversal, and no text explains it. The cell 2 outline promises "Recursive traversal". Tree traversal is therefore demonstrated but not taught.

✗ Only in an exercise or hidden cell (house rule) — the BFS algorithm is taught only through a hidden code cell (1303 cell 13) plus a one-line bullet. Stopping BFS at a target appears only in exercise 14. The `bfs_until(..., "erin")` solution (cell 15) also depends on exercise 10's solution having added `erin` to the graph.

✓ Learning objectives — ORGANIZATION.md has no learning-objectives section, so there is nothing to compare with the 1300 goals.

✗ ORGANIZATION ↔ MATERIALS — the section order agrees. MATERIALS.md's Homework description ("Model a real-world process (e.g. browser history, print queue) as an ADT") does not match homework.ipynb, which has 5 true/false questions plus 5 short list/dict snippets and no ADT class. MATERIALS.md's lab task list does match lab.ipynb (5 tasks, same order).

✓ Material files — neither doc has a Material Files section, and there is no `materials/` folder for Ch13.

✓ Stale references — none in the docs.

### Slide deck ↔ chapter

✗ Stale numbering — the title slide reads "*14.0 Intro · 14.1 ADT Basics · 14.2 Stacks & Queues · 14.3 Trees & Graphs*". This should be 13.x; the section divider slides correctly say 13.1–13.3.

✗ Learning goals — the deck has no learning-goals slide. None of the five 1300 goals (cell 3) appears, including goal 2 ("Compare common operations...") and goal 5 ("Choose a data structure based on ... tradeoffs").

✗ Section headings — the deck omits three notebook sections, which conflicts with the AGENTS.md rule to preserve important tables:
  - `## Operation Tradeoffs` (1301): both Big-O tables, the list-vs-set timing demo, and the O(log n) note.
  - `## Stack or Queue?` (1302): its comparison table.
  - `## Graph Traversal` as a unit (1303): DFS has no code slide.

✗ Topics the chapter never teaches — the deck presents content absent from the notebooks:
  - "the ADT contract" (13.1 divider and the "What is an ADT?" slide)
  - Tree "Height", "BST — left < node < right", and pre-/in-/post-order traversals with an `inorder()` binary-`Node` example (Trees slide)
  - "Directed / Undirected" graphs, "BFS → shortest path (unweighted)", and "DFS → reachability, cycle detection" (Graphs & Search slide)
  - "Message queues" and "Function call stack" use cases
  - Quick Reference rows "Tree traversal: Pre/in/post-order" and "BST property"

✗ Examples diverge from the notebooks:
  - **Stack.** The slide `Stack` uses `_data` with no empty checks; 1302 cell 4 uses `_items` and raises `IndexError`.
  - **Balanced brackets.** The slide `is_balanced` uses a bare list and peeks with `stack[-1]`; 1302 cell 6 uses the `Stack` class.
  - **Queue.** The slide uses a raw `deque` of `"task1"`…; the notebook wraps `deque` in a `Queue` class with `enqueue`/`dequeue`.
  - **Tree.** The slide has a binary BST (4/2/6); the notebook has the company/analytics/operations `TreeNode` org chart.
  - **Graph.** The slide uses nodes A–D; the notebook uses alice–dana.
  - **BFS.** The slide version marks nodes visited on enqueue and prints them; 1303 cell 13 marks on dequeue and returns `order`.

  None of the notebook's key datasets appears in the deck: the org chart, the alice/bob/charlie/dana graph, and the undo/print-job table.

### Preview ↔ chapter

✓ All 10 questions (q1–q10) map to glossary terms taught in 1301–1303: ADT, concrete implementation, operation, tradeoff, stack/LIFO, push, queue/FIFO, dequeue, tree, and traversal. None tests a topic that has moved elsewhere, and the difficulty suits pre-reading. The only mismatch is minor: the q2 distractor "Interface" is a term the notebooks never use.

### Homework ↔ chapter

✗ Q7 and Q10 contradict the chapter's queue guidance. Q7 (cell 13, `line.pop(0)`) and Q10 (cell 19/20, `queue = ["A"]` ... `queue.pop(0)`) implement queues with `list.pop(0)`. The chapter teaches `collections.deque` as "usually the right implementation" (1302 cell 9, 1301 cell 12 solution: "list.insert(0, x) is O(n)"), and the slides say "list.pop(0) is O(n) — avoid for queues".

✗ Q6–Q9 do not use the chapter's structures. None uses the `Stack`, `Queue`, or `TreeNode` classes taught in 1302 and 1303. Q8 represents a tree as nested dict keys (`{"CEO": {"Data": {}, ...}}`), a representation the chapter never shows. Q8 (`list(company["CEO"].keys())`) and Q9 (`graph["DS200"]`) are one-line dict lookups that exercise no Ch13 concept. The easy-to-hard ladder is flat until Q10, and nothing exercises DFS or a stack/queue class.

✗ Q3 (tree with "any number of parents") tests a property the text never states. 1303 cell 3 says only "a root and branches. Each node can have children"; it never says each non-root node has exactly one parent. Q4 relies on a course-prerequisite graph, which is directed, and directed graphs are not taught. Q4's answer is still defensible, so this is minor.

### Lab ↔ chapter

✗ Q1 and Q2 require interfaces that conflict with the chapter's ADTs. Both classes must expose `append`/`pop`/`__len__`, not the chapter's `push`/`pop`/`peek`/`is_empty` (1302 cell 3–4) and `enqueue`/`dequeue` (1302 cell 9–10). Q2 also mandates `self.items.pop(0)` (list-backed queue) instead of `deque`. This undercuts the 1301 message that the interface defines the ADT.

✗ Q4 (BFS) requires a list with `pop(0)` as the queue and a list with `visited.count(name) == 0` for membership. 1303 cell 13 teaches BFS with `deque` and a `set`, and 1301 teaches that `set` membership is O(1) and list membership is O(n). If this is a grader constraint, say so in the question; otherwise align the lab with the taught pattern.

✓ Datasets and topics — the Q4 alice–erin graph matches the 1303 graph after exercise 10. The Q3 org chart (operations/charlie/dana) echoes 1303 cell 4, though as a nested dict rather than `TreeNode`. Q5 mirrors the 1301 and 1302 structure-choice exercises.

✓ Submission — `data-assignment-answers="ch13-lab"` and the 5 grader feedback rows match the 5 `### Question N` cells. No dependency on a removed section.

### Glossary ↔ content

✓ All 20 glossary terms in 1300 cell 5 appear in 1301–1303 text. Access, search, insert, and delete are bolded in 1301 cell 8; peek, push, pop, enqueue, and dequeue are defined in 1302 cells 3 and 9; tree, graph, and traversal are in 1303.

✗ Missing from the glossary — seven terms introduced in the notebooks:
  - node, edge (1303 cell 8, bolded as the definition of a graph)
  - leaf, root (1303 cell 3)
  - adjacency list (1303 cell 8)
  - breadth-first search, depth-first search (1303 cell 12, bolded)

### Summary

Sync issues: 20
  - Planning docs lack `orphan: true` front matter.
  - 1301 docs list "ADT contract / preconditions / postconditions" (not taught) and use "interface" (never used in text); `## Operation Tradeoffs` is not listed.
  - 1302 docs list a call-stack analogy (not taught); `## Stack or Queue?` is not listed.
  - 1303 docs list binary trees, pre/in/post-order traversals, BST basics, height/depth, adjacency matrix, and directed/undirected (none taught, none in Planned Additions).
  - No exercise lists in either doc (9 exercises exist).
  - 1301 exercise requires `deque`, which is taught only in a hidden, non-executed cell.
  - The `Queue` class (1302 cell 10) and BFS (1303 cell 13) are hidden by default, yet exercises depend on them.
  - Tree traversal (`print_tree`) is shown without explanatory text.
  - BFS and stop-at-target are taught only via a hidden cell and an exercise; cell 15 depends on exercise 10's output.
  - MATERIALS.md homework description does not match homework.ipynb.
  - Slides: stale "14.x" numbering on the title slide.
  - Slides: no learning-goals slide.
  - Slides: missing Operation Tradeoffs tables, the Stack-or-Queue table, and DFS.
  - Slides: teach BST, binary-tree traversals, directed/undirected, the ADT contract, and shortest-path/cycle-detection claims absent from the chapter.
  - Slides: examples and datasets diverge from the notebooks (Stack, is_balanced, Queue, tree, graph, BFS).
  - Homework Q7/Q10 use `list.pop(0)` queues, contrary to the `deque` guidance.
  - Homework Q6–Q9 bypass the chapter's classes; Q8/Q9 are trivial dict lookups; the ladder is flat.
  - Homework Q3 tests a single-parent tree property the text never states.
  - Lab Q1/Q2/Q4 mandate interfaces and implementations (`append`/`pop`/`__len__`, `pop(0)`, `visited.count`) that contradict the chapter's ADTs.
  - Glossary missing node, edge, root, leaf, adjacency list, BFS, and DFS.

### Top fixes

1. **Close the 1303 tree gap in one direction.** Either add binary trees, pre/in/post-order traversal (with prose explaining `print_tree`-style recursion), and BST basics to 1303, or move them to Planned Additions and remove them from the ORGANIZATION.md/MATERIALS.md required core and the Trees slide. Today the docs and slides promise content the notebook lacks.
2. **Unhide the core teaching code and explain it in markdown.** Remove `hide-input` from 1302 cell 10 (`Queue`/`deque`) and 1303 cell 13 (BFS), and remove `hide-input`/`skip-execution` from 1301 cell 5. Add a short markdown explanation of `deque` (`append`/`pop`/`popleft`) before the 1301 exercise, and of BFS before the "Stop at a Target" exercise.
3. **Align the lab and homework with the taught ADTs.** Use the `push`/`pop`/`peek`/`is_empty` and `enqueue`/`dequeue` interfaces, `deque` for queues, and a `set` for visited nodes (lab Q1, Q2, Q4; homework Q7, Q10). If the grader forbids imports, state that explicitly and explain the O(n) tradeoff.
4. **Resync the slide deck and regenerate `overview.html`.** Fix the "14.x" title line, add a learning-goals slide, and add Operation Tradeoffs (both tables) and Stack-or-Queue slides. Replace the slide examples with the notebook versions (the `Stack`/`Queue` classes, the org-chart `TreeNode`, the alice–dana graph, BFS and DFS). Drop or qualify the contract, directed/undirected, and shortest-path claims.
5. **Update the planning docs and glossary.** Add `orphan: true` front matter, list `## Operation Tradeoffs` and `## Stack or Queue?`, and add the 9 exercise names. Drop or write the ADT contract and call-stack analogy, fix MATERIALS.md's homework description, and add node, edge, root, leaf, adjacency list, breadth-first search, and depth-first search to the 1300 glossary.
