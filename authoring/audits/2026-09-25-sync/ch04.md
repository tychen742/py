---
orphan: true
---

## Sync Audit — Ch04: Functions

Files audited (working tree, 2026-09-25): `chapters/04-functions/ORGANIZATION.md`, `MATERIALS.md`, `0400-functions.ipynb`, `0401-functions.ipynb`, `0402-function-design.ipynb`, `0403-recursion.ipynb`, `assignments/{index,preview,lab,homework}.ipynb`, `_html_extra/chapters/04-functions/overview.md`, `_toc.yml` (lines 50-65), and the ch04 answer keys in `_html_extra/api/lib/quiz-app.php`. Cell numbers are 0-based.

Exercise inventory (15 total):
- 0401 (6): Exercise 1 Polygon [c33], Exercise 2 Default Parameters [c47], Exercise 3 Functions with Parameters [c50], Flexible Score Summary [c58, unnumbered, sits between 3 and 4], Exercise 4 add_numbers [c63], Exercise 5 Return Values [c67]
- 0402 (5): Function Composition [c17], Make the Function Pure [c31], Compose Small Functions [c47], Writing Good Docstrings [c54], Write a Contract Docstring [c58]
- 0403 (4): Trace the Base Case [c22], Add a Stopping Condition [c30], Recursive Sum [c42], Memoized Fibonacci [c52]

### Planning docs ↔ chapter
✗ Front matter: neither `ORGANIZATION.md` nor `MATERIALS.md` starts with MyST front matter `orphan: true`. Both open directly with a `# Chapter 04 ...` heading.
✓ Notebook names: all four notebooks named in both docs exist and match the folder and the `_toc.yml` entries. The assignment files are listed in both docs.
✗ 0401 sequence (taught but not listed): the notebook's final `## Return Values in Applications` section [c76-97] (return in conditionals, exhaustive returns, **dead code**, boolean functions) is not in either doc. Keyword arguments and keyword-only parameters [c40] are also taught but not listed. The listed order otherwise matches: motivation/turtle, defining, parameters/defaults/`*args`/`**kwargs`, return values, type annotations. The docs are stale here.
✗ 0402 sequence and order: the docs say "Scope, pure functions vs. side effects, small-function composition, docstrings as contracts, lambda". The notebook order is `## Scope` [c4] → `## Function Composition` [c12] → `## Pure Functions and Side Effects` [c19] → `## "One Function, One Job"` [c33] → `## Docstrings` [c49] (`### Docstrings as Contracts` [c56]) → `## Lambda Functions` [c60]. So composition comes before pure functions in the notebook but after them in the docs. The `global` keyword [c10-11, c27-30] is taught but not listed. Either reorder the docs or move the section. The docs look stale.
✗ 0403 sequence: `## Infinite recursion` [c24] and `## Fibonacci` [c44] are taught but not named. ORGANIZATION.md orders the end of the notebook as "memoization; practical limits", which matches the notebook (`## Memoization` [c47] → `## When Not to Use Recursion` [c54]). MATERIALS.md says "practical limits; memoization" in both "Notebook Content Targets" and "Delivery Order". MATERIALS.md is stale, so the two docs disagree.
✗ Exercise lists: neither doc lists exercises (0 named), but the notebooks have 15 (see the inventory above). Both docs are missing this.
✗ Checkpoint claim: the ORGANIZATION.md checklist says "at least one checkpoint exercise per major section". These `##` sections have no exercise: 0401 `Defining Functions`, `Type Annotations`, `Return Values in Applications`; 0402 `Scope`, `Lambda Functions`; 0403 `Fibonacci`, `When Not to Use Recursion`.
✗ Topics covered only by exercises (practice, not teaching):
  - Recursion over a list (`nums[0] + f(nums[1:])`, empty-list base case) appears only in 0403 Recursive Sum [c42/43]. Every text example recurses on an integer `n`. Lab Q5 and the MATERIALS lab plan ("recursion over a list") depend on this pattern.
  - Returning a new list instead of mutating the argument appears only in the 0402 Make the Function Pure solution [c32]. The text [c23-25] shows only a scalar pure function and an in-place mutator. Lab Q4 depends on this pattern.
  - A memo cache passed as a parameter with a `cache=None` sentinel (`if cache is None: cache = {}`) appears only in the 0403 Memoized Fibonacci solution [c53]. The new Memoization text [c47-51] uses a global `known` dict and never explains why `None` is used in place of a `{}` default.
✗ Untaught syntax in exercises or examples (by chapter order in `_toc.yml`):
  - List comprehensions (taught in 6.1, `0601-lists` c18): 0402 c32 (solution), c58 (the exercise's given code) and c59, plus lab Q4 solution (below).
  - 0401 Exercise 1 Polygon [c33] needs a function with a parameter `n` (solution c34). At that point the only function shown takes no parameters (`square()` [c30]); parameters are taught later at c40. The prompt ("produce the same output as the cell below") also points to a hidden solution cell.
  - `try`/`except` (Ch05): 0402 c7 and c9 (labeled "next chapter", which is acceptable) and 0403 c28 (no note).
✗ Landing goals vs required core: ORGANIZATION.md has no learning-objectives section, so there is nothing to compare there. However, the 0400 Learning Goals [c3] leave out required-core topics: pure functions and side effects, type annotations, and memoization. Goal 4 names only "local scope, function composition, docstrings, and basic lambda expressions".
✗ Stale or broken references in the notebooks:
  - 0403 c38: "similar to the flow of `countdown` in Chapter 5". `countdown` is in this notebook (4.3). This is a Think Python leftover.
  - 0403 c46: "according to the leap of faith". The leap of faith is never introduced anywhere in the book.
  - 0403 c29: "The traceback indicates that there were almost 3000 frames". c28 catches the `RecursionError` with `try`/`except` and prints only its name, so no traceback is shown.
  - 0403 c32: the `fibonacci.png` figure is embedded in `## Recursion with return values` before the factorial code. It belongs in `## Fibonacci` [c44].
  - 0401 c40 ends with "The following example should make the ideas clear:", but no example follows (c41 moves on to simpler patterns). Keyword-argument calls and keyword-only parameters are taught only through a table.
  - 0402 c21 names `dataclasses(frozen=True)`. The actual syntax is the `@dataclass(frozen=True)` decorator. This mention and the `map`/`filter`/`reduce` references point forward without saying where those topics are taught.
✗ MATERIALS.md homework plan says "Function composition problem; write a recursive solution with explanation". Homework Q10 [c19] asks only for printed output; no explanation is required or graded.
✓ Material Files: neither doc has a Material Files section, and there is no `materials/04*` folder. The figure used (`figures/fibonacci.png`) exists.
✓ Planned Additions (closures) is consistent across both docs and was not flagged.

### Slide deck ↔ chapter
✗ Learning goals: the deck has no learning-goals slide. The five goals in 0400 [c3] do not appear.
✗ Section headings: the deck uses the 4.1/4.2/4.3 section order, but many `##` topics are missing.
  - 4.1: the subtitle promises "type annotations", but no slide covers them. There is no slide for custom-function motivation or turtle encapsulation, or for `Return Values in Applications` (boolean functions, dead code).
  - 4.2: Function Composition is folded into the Scope slide and repeated in "Pure Functions & Composition". There is no "One Function, One Job" slide and nothing on "Docstrings as Contracts".
  - 4.3: the subtitle promises "tracing calls · memoization", but there is only one content slide (factorial and fibonacci). There is nothing on stack diagrams, infinite recursion, the new Memoization section (it appears only as a Quick Reference row, line 345), or When Not to Use Recursion.
✗ Figures and tables not preserved (AGENTS.md slide rule): the recursion stack diagrams (0403 c18-21, c39-40), the Fibonacci figure (0403 c32), and the built-in vs. custom functions table (0401 c5) are absent. The `*args` vs. `**kwargs` comparison table (0401 c54) is only partly reflected.
✗ Slides show material the chapter does not teach, or label it wrongly:
  - The positional-only `/` marker in `def f(pos, /, normal, *, kw_only, **kwargs)` (line 119) is not taught in Ch04.
  - `help(area)` / `area.__doc__` (line 250) are not taught in 4.2.
  - `words.sort(key=...)` (line 266) uses the list method. The chapter uses `sorted(..., key=...)`.
  - The table row "Default | `f(x=2)`" (line 127) labels a keyword-argument call as a default parameter, which changes the meaning.
✓ Key examples still exist: `power`, `add_bonus`, `mean`, `letter_grade`, `celsius_to_fahrenheit`, `factorial`, `fibonacci`, and `greet` are all in the notebooks.
Note: `overview.html` (Aug 31 15:43) is newer than `overview.md` (Aug 31 09:33), so the render is current with its source. The source itself predates today's Memoization section. Regenerate after the slide fixes.

### Preview ↔ chapter
✓ All 10 questions map to current chapter content: function, `def`, parameter, argument, return statement, return value, scope, docstring, lambda, recursion. The answer key in the page matches `quiz-app.php` (lines 491-507).
✓ No question tests a moved topic, and the difficulty suits pre-reading.
Note: there are no questions on pure functions/side effects, base case, `*args`/`**kwargs`, or memoization. This is a coverage gap, not a sync error.

### Homework ↔ chapter
✓ All 10 problems use current chapter concepts (reuse, return vs. print, scope, defaults, base case, formatter, average, default argument, composition, recursion). The T/F and output keys match `quiz-app.php` (lines 962-981). Expected outputs verified: 86.0, 32.4, 15.
✓ The difficulty ladder (T/F, then single functions, then composition, then recursion) follows the chapter order.
✗ The mismatch with MATERIALS.md ("recursive solution with explanation") is reported above.
Note: the Q8 prompt/solution [c15-16] uses the keyword-argument call `add_fee(price, fee=3.0)`. The notebooks teach this only through the 0401 c40 table, with no worked example.

### Lab ↔ chapter
✓ All 5 questions match the MATERIALS.md lab plan (defaults, `*args`/`**kwargs`, BMI composition, pure refactor, list recursion). The expected outputs match `quiz-app.php` (lines 736-747). The submit panel covers q1-q5, matching the 5 question cells.
✗ Q4 solution [c8] uses a list comprehension (`[round(...) for price in prices]`), which is not taught until 6.1.
✗ Q5 [c9] (recursion over a list with `nums[1:]`) and Q4 (return a new list) depend on patterns that appear in the chapter only as exercise solutions (0403 c43, 0402 c32), never in the text.

### Glossary ↔ content
✓ All 25 glossary terms in 0400 [c5] are introduced in a content notebook. Memoization, added today, is in the glossary (#25) and is taught in 0403 c49.
✗ Key terms introduced in the notebooks but missing from the glossary:
  - "global variable" / `global` keyword (0402 c4, c10-11)
  - "keyword argument" and "keyword-only parameter" (0401 c40)
  - "dead code" (bold in 0401 c85)
  - "boolean function" (0401 c86)
  - "frame" (used for stack diagrams in 0403 c17, c20)

### Summary
Sync issues: 19
  - MATERIALS.md and ORGANIZATION.md both lack `orphan: true` front matter
  - 0401 "Return Values in Applications" and keyword/keyword-only arguments are taught but not in the docs
  - 0402 order differs from the docs (composition before pure functions); `global` is not listed
  - 0403 Infinite recursion and Fibonacci are not listed; MATERIALS.md orders memoization after practical limits (stale, and disagrees with ORGANIZATION.md)
  - Neither doc lists the chapter's 15 exercises
  - 7 `##` sections have no checkpoint exercise, contrary to the ORGANIZATION.md checklist
  - Three patterns are covered only by exercises (list recursion, return-new-list, `cache=None` memo)
  - Untaught syntax: list comprehensions (0402 c32/c58/c59), Polygon exercise uses parameters before they are taught, try/except in 0403 c28
  - Stale references in 0403 (Chapter 5, leap of faith, traceback claim, misplaced Fibonacci figure)
  - 0401 c40 promises an example that is missing; keyword arguments are taught only in a table
  - Landing goals leave out pure functions, type annotations, and memoization
  - MATERIALS.md homework plan ("recursive solution with explanation") does not match the homework
  - Slides have no learning-goals slide
  - Slides are missing topics: type annotations, return-value applications, one-function-one-job, contracts, stack diagrams, infinite recursion, memoization, when not to recurse
  - Slides leave out the chapter's key figures and tables (stack diagrams, Fibonacci, built-in vs. custom)
  - Slides show untaught syntax (`/`, `help`/`__doc__`, `list.sort`) and mislabel `f(x=2)` as "Default"
  - Lab Q4 solution uses a list comprehension
  - Lab Q5/Q4 depend on patterns taught only in exercise solutions
  - Glossary is missing global variable, keyword argument/keyword-only parameter, dead code, boolean function, frame

### Top fixes
1. 0403: add a short text example of recursion over a list (base case `if not nums`, then `nums[0]` + recurse on `nums[1:]`) before the Recursive Sum exercise. Also fix the stale "Chapter 5", "leap of faith", and traceback sentences (c38, c46, c29), and move the Fibonacci figure to `## Fibonacci`.
2. Slides: add a learning-goals slide that mirrors 0400. Add slides for type annotations, stack diagrams/tracing, and memoization (the fib call-count comparison), carrying over the stack-diagram and Fibonacci figures. Remove or fix `/`, `help`/`__doc__`, `list.sort`, and the "Default | f(x=2)" row. Then regenerate `overview.html`.
3. Remove list comprehensions from Ch04 (0402 c32, c58, c59; lab Q4 solution c8): use a `for` loop with `append`. In 0402 Pure Functions, add a text example that returns a new list, next to `add_bonus_in_place`.
4. Update ORGANIZATION.md and MATERIALS.md: add `orphan: true` front matter; add per-notebook exercise lists; list Return Values in Applications, keyword arguments, `global`, Infinite recursion, and Fibonacci; match the 0402 order to the notebook (or reorder the notebook); fix the MATERIALS.md memoization order and homework description.
5. 0401: move the Polygon exercise after `## Parameters and Arguments` (or give it a no-parameter task), and add the missing example after c40 showing keyword-argument calls and a keyword-only parameter. Add the missing glossary terms and extend landing goal 4/5 to cover pure functions, type annotations, and memoization.
