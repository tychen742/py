---
orphan: true
---

## Sync Audit — Ch05: Exceptions and Testing

Scope: `chapters/05-testing/` (0500, 0501, 0502, assignments), `_html_extra/chapters/05-testing/overview.md`, `_toc.yml` lines 66-78. Working tree as of 2026-09-25. Server-side answer keys checked in `_html_extra/api/lib/quiz-app.php` (ch05 entries at lines ~509, ~749, ~983).

### Planning docs ↔ chapter
✗ Front matter: neither `ORGANIZATION.md` nor `MATERIALS.md` starts with MyST front matter `orphan: true`; both open directly with `# Chapter 05 ...`. (Sibling chapters 04 and 06 lack it too, so this is project-wide, but it fails this check.)
✓ Notebook inventory: 0500, 0501, 0502 and the four `assignments/` notebooks are named in both docs, exist on disk, and match `_toc.yml`.
✓ Section sequence (0501): ORGANIZATION.md order (error types, tracebacks, handling with `as e` and multiple types, `else`/`finally`, raising with type-hints aside and custom exceptions nested under it, debugging with three techniques, logging) matches the `##`/`###` headings in cells 4-69.
✓ Section sequence (0502): pytest (inline asserts, test files, subprocess, ipytest, `pytest.raises`), unittest (`setUp`/`tearDown`, comparison), doctests, then mocking, parametrize, coverage. This matches the headings in cells 5-63.
✗ Taught but not listed: neither doc mentions the 0502 intro (cell 4: unit vs integration vs end-to-end test spectrum, test-driven development), even though the first 0502 learning goal is "Explain what a unit test is and how it differs from integration and end-to-end tests". The 0502 subsections "When Tests Fail" (cell 26) and "`unittest` Conventions" (cell 30) are also absent from the required-core and enrichment lists. The docs are stale here.
✓ Exercise lists: 0501 has 8 exercises (cells 16, 23, 29, 35, 42, 49, 58, 71) and 0502 has 5 (cells 21, 32, 47, 56, 61). Names and order match both docs exactly.
✓ Required core: every required-core topic is taught in markdown text with examples. The type-hints/`mypy` aside (0501 cell 44) is correctly listed as enrichment.
✗ Exercise-only content (house rule a): `assertAlmostEqual(..., places=1)` appears only in the hint for the 0502 exercise "Write a TestCase for to_celsius" (cell 32). `mock.assert_called_once_with(...)` appears only in the hint for "Mock a database lookup" (cell 56). The text never teaches either one. Cell 51 shows only `return_value` and `call_args`.
✗ Untaught syntax in examples or exercises (house rule b; chapters 1-4 and `_toc.yml` order checked):
  - 0501 cell 61 (Systematic Bisection example) is built on list comprehensions (`[(f - 32) * 5 / 9 for f in readings]`, `[t for t in celsius if ...]`), which are first taught in ch06 `0601-lists` ("List Comprehensions"). Its docstring also has a `>>>` doctest before doctests are taught in 0502.
  - 0502 cell 21, the exercise "Write pytest-style tests", has a starter `count_vowels` that uses a generator expression, `sum(1 for c in s.lower() if c in 'aeiou')`. Nothing in ch01-04 teaches this.
  - 0502 `unittest` section (cells 25, 27, 29, 33): class definitions, subclassing, `self`, and methods are used, and exercise cell 32 requires students to write a `TestCase` subclass. OOP is ch09. Unlike 0501 cell 47, 0502 has no forward note ("classes are covered in the OOP chapter").
  - 0501 exercise "Custom Exception Class" (cell 49) also requires defining a class. It is mitigated by the note in cell 47 and the "body needs only `pass`" hint; low priority.
  - 0502 cells 58-59: `@pytest.mark.parametrize` is called "a decorator" with no explanation. Decorators are not taught before this point (the earliest candidate is ch10).
  - 0501 cell 34: `with open(...)`, `file.read()`, and `IOError` are used, but file I/O is taught only in the appendix (`appendices/packaging/0501-files`). An inline comment explains `with open`; `IOError` is not explained. Low priority.
✓ Learning objectives: ORGANIZATION.md has no objectives section, so this check does not apply.
✗ ORGANIZATION ↔ MATERIALS: required core and exercise lists agree, but MATERIALS.md has stale ordering and descriptions:
  - "Chapter 05 Delivery Order" item 3 describes 0502 as "unittest and doctests". It omits pytest, `pytest.raises`, mocking, parametrize, and coverage, and it contradicts MATERIALS.md's own 0502 targets. (Stale.)
  - "Must-Have Teaching Materials" lists "Unit testing with `unittest`; doctests" before `pytest`. The notebook and ORGANIZATION.md teach pytest first.
  - "Practice and Assessment Pack → Homework: Add exception handling and tests to a prior chapter function" does not describe the actual homework (5 T/F questions plus 5 short coding questions with no tests beyond one plain `assert`). The lab task list does match the lab.
✗ Material Files:
  - Both docs list `materials/05-testing/app.log`, which does not exist. The only `app.log` is `chapters/05-testing/app.log`, a runtime artifact that 0501 cells 64/66 write when the notebook runs.
  - `materials/05-testing/test_temp.py` exists but is not listed. It also looks obsolete, because 0502 cell 14 now creates `test_temp.py` in a `tempfile.TemporaryDirectory()`.
  - `calc.py` exists. (Side note, not counted: `chapters/05-testing/__pycache__/` holds stray `test_calc`/`test_math`/`test_temp` bytecode.)
✓ No stale chapter or section numbers: the cross-references (`ch05-raising-exceptions`, `ch05-else-finally`, "In 5.1") resolve.

### Slide deck ↔ chapter
✓ Section headings: the slides cover Types of Errors, Reading Tracebacks, try/except, Multiple Exceptions, else/finally, Raising (with custom exceptions), Debugging, Logging, then pytest, Testing Exceptions, unittest, Doctests, More Testing Techniques, Mocking. This follows the chapter's `##` headings in order.
✗ Learning goals: the deck has no learning-goals slide, so none of the 7 landing-page goals (0500 cell 3) appear in the slides.
✗ Order within "More Testing Techniques": the slide of that name shows the parametrize example (`test_is_palindrome`) before the separate "Mocking Dependencies" slide. In 0502 the order is Mocking (cell 50), then Parametrized Tests (cell 58), then Coverage (cell 63). Coverage has no slide of its own, only a table row and a callout command.
✗ Slide-only topic: the "Testing Strategy" slide (normal/boundary/error/regression cases, test independence, "testing is design feedback") has no matching section in 0502.
✓ Key examples still exist: `test_calc.py`, `divide`/`pytest.raises` with `match=`, `TestFruitBasket` with `setUp`, `test_is_palindrome`, and `get_price`/`apply_discount` patching all match 0502. The tracebacks, multiple-exceptions, and doctest slides use slide-only examples (`parse_age`, `get_score`, `square`) rather than the notebook's (`cat_twice`/`checkout`, `calculate_from_strings`, `uses_any`), but nothing refers to removed content.
✓ No removed or renamed topics referenced. `chapters/05-testing/overview.html` was regenerated at the same time as `overview.md` (Sep 25 14:55) and contains the current content. Minor note, not counted: the Logging slide's level table omits `CRITICAL`.

### Preview ↔ chapter
✓ All 10 questions (exception, try/except/else blocks, traceback, debugging, `raise`, logging, unit test, mock) map to content taught in 0501/0502, and none tests a topic moved elsewhere.
✗ Coverage balance: 8 of 10 questions target 0501. Only q9 (unit test) and q10 (mock) touch 0502, and no question covers `pytest`, `unittest`, doctest, parametrization, `finally`, or coverage, which is half of the chapter's learning goals (goals 6-7). All items are glossary-definition recall, which is fine for pre-reading difficulty.
✓ The answer key in `quiz-app.php` matches the notebook answers (B, A, B, A, C, B, B, A, B, B), and the `quizId` is `ch05-preview`.

### Homework ↔ chapter
✗ Q9 "Handle missing dictionary keys" (cell 17) is broken. The prompt says to "Print exactly these three lines: Plan: pro / Missing user: chen / Lookup finished". With `user = "chen"`, the solution (cell 18) prints only the last two lines, and the server key (`quiz-app.php` `ch05-homework` q9) expects `"Missing user: chen\nLookup finished"`. A student who follows the prompt fails grading.
✗ Q10 "Validate a function input" (cell 19) is underspecified and uses untaught methods. The prompt never gives the input string, yet it requires the exact output `Q1 Revenue`. The solution calls `clean_label(" q1 revenue ")` with `label.strip().title()`, which are ch08 string methods not taught before ch05, and the server key requires this exact output.
✗ Chapter coverage: every coding problem (Q6-Q10) exercises only 0501 try/except/raise/finally. Q8 uses one bare `assert` script with no test function. Nothing assesses `pytest` test functions, `pytest.raises`, `unittest`, doctest, mocking, parametrize, coverage, logging, custom exception classes, or traceback reading. That leaves landing goals 2, 5 (logging part), 6, and 7 unassessed. The T/F items touch testing only lightly (Q4 assert, Q5 unit-test isolation).
✗ Difficulty ladder: flat. Q6-Q10 are all one-step exercises at the same level as the notebook's in-text exercises, with no progression toward testing code.
✓ No problem references a removed example or dataset. Assignment IDs follow `ch05-homework`. (Minor, not counted: every T/F solution uses boilerplate reasoning, "This workplace case matches the chapter concept." / "The case reverses or overstates the chapter concept.", so students get no concept-specific feedback.)

### Lab ↔ chapter
✗ Scope: all 5 questions cover 0501 handling and raising only (Q1 `try`/`except`, Q2 `assert`, Q3 multiple `except`, Q4 `else`/`finally`, Q5 `raise`). None uses logging, custom exceptions, or any 0502 testing tool. Several are near-clones of the notebook's in-text exercises: Q1 `safe_float` ≈ `safe_int` (0501 cell 23), Q2 `validate_rectangle` ≈ `validate_triangle` (cell 58), Q3 ≈ `safe_index` (cell 29), Q5 ≈ `square_root`/`validate_age` (cells 41-42). The list does match MATERIALS.md's lab task list.
✗ Q2 "Assert-based validator" (cell 3) conflicts with the chapter's guidance. It uses `assert` to validate function inputs, although 0501 cell 56 says "never use `assert` to validate user input". It also makes students catch `except Exception as e` and relabel the error as `"AssertionError: ..."`, although 0501 cell 25 teaches catching specific exceptions and treats `except Exception` as a last resort. The 0501 exercise "Using Assertions" (cell 58, `validate_triangle`) has the same assert-as-input-validation tension.
✗ Q5 "Raising exceptions by condition" (cell 9) requires the message `Password must be a string, got <class 'int'>`. 0501 cell 26 explicitly teaches `type(x).__name__` so that messages read "got str" rather than "got <class 'str'>". The server key (`quiz-app.php` `ch05-lab` q5) locks in the discouraged form.
✓ Submission: the grader panel (cell 11) has rows q1-q5, `collectCodeCells()` takes the first 5 `thebe-interactive` cells, the lab has exactly 5 questions, and `labId = 'ch05-lab'` matches the naming convention and the server key.

### Glossary ↔ content
✓ All 20 glossary terms in 0500 cell 5 are introduced in 0501/0502. Fixture appears in 0502 cell 52, test coverage in cell 63, and log level in 0501 cell 62.
✗ "Assertion" is defined as "A check that a condition is true during a test". 0501 cells 54-57 teach `assert` mainly as an in-code developer check during debugging, which the definition excludes.
✗ Key terms missing from the glossary:
  - syntax error: landing goal 1 and 0501 cell 4; runtime and semantic errors are in the glossary but syntax error is not.
  - bisection (debugging): landing goal 5 and 0501 cell 60.
  - parametrized test / parametrization: landing goal 7 and 0502 cell 58.
  - test-driven development (TDD): bolded in 0502 cell 4.
  - integration test and end-to-end test: 0502 cell 4 and the first 0502 learning goal.
  - test discovery: 0502 cells 5 and 8.

### Summary
Sync issues: 26 (20 grouped findings below; the untaught-syntax finding covers 6 separate locations and the Material Files finding covers 2 files)
  - MATERIALS.md and ORGANIZATION.md lack `orphan: true` front matter
  - Docs omit 0502 intro content (test spectrum, TDD), "When Tests Fail", and "unittest Conventions"
  - `assertAlmostEqual` and `assert_called_once_with` appear only in exercise hints
  - Untaught syntax: list comprehensions and a doctest (0501 cell 61), a generator expression (0502 cell 21), classes in unittest with no forward note (0502 cells 25-33), decorators (0502 cell 59), file I/O (0501 cell 34)
  - MATERIALS.md delivery order and must-have list are stale (0502 = "unittest and doctests"; unittest listed before pytest)
  - MATERIALS.md homework description does not match the actual homework
  - Material Files: `materials/05-testing/app.log` missing; `test_temp.py` unlisted and obsolete
  - Slides have no learning-goals slide
  - Slide order puts parametrize before mocking; no coverage slide
  - "Testing Strategy" slide has no notebook counterpart
  - Preview is 8/10 on 0501, with nothing on pytest, unittest, doctest, parametrize, or coverage
  - Homework Q9 prompt contradicts the solution and the server key ("Plan: pro" line)
  - Homework Q10 gives no input and needs untaught `.strip().title()`
  - Homework assesses none of 0502, logging, custom exceptions, or tracebacks
  - Homework difficulty ladder is flat
  - Lab covers only 0501 handling and raising, and its tasks are near-clones of the notebook exercises
  - Lab Q2 teaches assert-for-input-validation plus `except Exception`, against 0501 guidance
  - Lab Q5 expected output uses `<class 'int'>`, against 0501 cell 26 guidance
  - Glossary "Assertion" definition is too narrow
  - Glossary is missing 7 key terms: syntax error, bisection, parametrized test, TDD, integration test, end-to-end test, test discovery

### Top fixes
1. Fix homework Q9 (`assignments/homework.ipynb` cell 17). Either drop the `Plan: pro` line from the prompt, or have the prompt look up a present user first and add that line to the solution and to the `ch05-homework` q9 key in `_html_extra/api/lib/quiz-app.php`. As written, a correct student answer is graded wrong.
2. Make homework Q10 (cell 19) answerable. State the input (`" q1 revenue "`) and the transformation, or drop `.strip().title()` (ch08 methods) and use a plain return, then update the q10 server key to match.
3. Add assessment for 0502, the half of the chapter that is currently untested. Replace at least 2 homework coding items and 1-2 lab questions with tasks that write `pytest`-style test functions, use `pytest.raises`, run a doctest, or use `patch`/`parametrize`, and add one logging or custom-exception task. Then update the preview (for example, questions on `pytest.raises`, doctest, and parametrize) and the homework and lab descriptions in MATERIALS.md.
4. Align lab Q2 and Q5 with the chapter's own guidance. For Q2, either turn it into a developer-invariant check or switch it to `raise ValueError` with a specific `except`. For Q5, use `type(password).__name__` (expected output `got int`), and update the `ch05-lab` key. Also reconcile 0501 exercise cell 58 (`validate_triangle`) with the "never assert on input" rule in cell 56.
5. Remove untaught syntax from 0502 and 0501 text or add forward notes. Add an OOP forward note at the start of the 0502 `unittest` section, replace the generator expression in 0502 cell 21 with a loop, replace the list comprehensions in 0501 cell 61 with loops (or add a one-line note), and teach `assertAlmostEqual` and `assert_called_once_with` in the text before the exercises that need them. Fix the planning docs at the same time: add `orphan: true` front matter and correct Material Files, the delivery order, and the must-have order.
