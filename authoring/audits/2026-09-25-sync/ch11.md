---
orphan: true
---

## Sync Audit — Ch11: Iterators and Generators

Files audited (working tree, 2026-09-25): `chapters/11-iter-gen/{ORGANIZATION,MATERIALS}.md`, `1100-iter-gen.ipynb`, `1101-iterators.ipynb` (13 cells), `1102-generators.ipynb` (25 cells), `_html_extra/chapters/11-iter-gen/overview.md`, `assignments/{preview,lab,homework}.ipynb`, `_toc.yml` lines 167-180.

### Planning docs ↔ chapter
✗ Front matter: neither `ORGANIZATION.md` nor `MATERIALS.md` starts with MyST front matter `orphan: true`; both open directly with the `# Chapter 11 ...` heading.
✓ Notebook inventory: 1100/1101/1102 and `assignments/index|preview|lab|homework` exist, appear in `_toc.yml`, and are named in both docs.
✓ Section sequence, 1101: the three topics in ORGANIZATION match the notebook's `##` headings (The Iterator Protocol → Built-in Lazy Iterators → Custom Iterator Classes → Summary) in the same order.
✗ Listed but not taught, `itertools.chain`: both docs list it as required core (ORGANIZATION "Scope and Sequencing" item 3 and Required Core; MATERIALS 1102 Required core and slide checklist). No 1102 cell uses or explains `chain`; I searched all chapter notebooks and it appears only in the 1100 glossary (term 18) and in 1102 cell 18, which says "Beyond `islice` and `chain` (already covered)". That claim is false, so the doc is right and the notebook is missing the content (or cell 18 is stale).
✗ Thinly taught, `itertools.islice` (required core): no markdown cell explains it. It first appears as code in 1102 cell 12 (Fibonacci), with the only explanation in a code comment, and is then named in the Summary table (cell 24). Cell 11 ("Practical Use Cases / Infinite sequences") never mentions `islice`.
✗ Section sequence, 1102: the notebook has a `## Practical Use Cases` heading (cell 11) that contains only the infinite-sequence example, and neither doc names it. Also, ORGANIZATION places "`itertools.islice`, `itertools.chain`" right after infinite sequences, but the notebook has no islice/chain subsection there (see above). Apart from that, the order matches: Generator Functions → Generator Expressions (+ `### any()/all()`) → Practical Use Cases → `yield from` → More `itertools` → Bridge: Async Iteration → Summary.
✗ Exercise order: ORGANIZATION File Roles lists 1102 exercises as "Running Total Generator; `any()`/`all()`; itertools". In the notebook, `any()/all()` comes first (cell 9), then Running Total (cell 13), then itertools (cell 21). The doc is stale.
✗ Exercise list (MATERIALS): the 1102 required-core list is "Running Total Generator + one generator-expression task; `any()`/`all()` task". It leaves out the itertools exercise (cell 21) and counts the generator-expression task as a separate exercise, when it is really part 2 of the Running Total exercise (cell 13). This conflicts with ORGANIZATION, which lists 3 exercises including itertools. MATERIALS is stale.
✗ Required vs enrichment mismatch: both docs file `count`/`cycle`/`combinations`/`permutations` under Enrichment. However, ORGANIZATION's own Scope and Sequencing includes them in the core 1102 sequence, the notebook teaches them in a main `## More itertools` section (cells 18-20), and the graded itertools exercise (cell 21) tests them. The docs contradict each other and the notebook. Decide which tier this content belongs in.
✗ Content not in either list: 1102 teaches `next()` with a generator expression as a "first match" pattern (cells 6, 12) and mentions `send()`/`throw()` forwarding (cell 15, not explained). Neither doc lists these.
✗ Exercise uses untaught syntax, 1101 cell 10 (Cycling Iterator): the hint tells students to use `itertools.islice(your_cycle, 7)`, and solution cell 11 depends on it. `itertools` and `islice` are not introduced until 1102 cell 12, and no earlier chapter teaches them.
✗ Exercise uses untaught syntax, 1102 cell 9 (any()/all() exercise, part 3): it asks students to "Use next() with a generator expression to find the first sale above 1000". Before this point, `next(genexpr)` appears only in cell 6, which is tagged `hide-input` + `skip-execution`, so readers never see it in the rendered text. The first visible use is cell 12, after the exercise.
✗ Hidden required-core teaching: 1102 cell 6, the `sys.getsizeof` memory-comparison demo (ORGANIZATION: "memory efficiency (`sys.getsizeof`)"), is tagged `hide-input` and `skip-execution`. Its code is hidden and never runs. The visible markdown (cell 5) gives the ~8 MB vs ~200 bytes claim without showing `sys.getsizeof`.
✗ Exercise tagging breaks the build checklist ("Tag question cells `thebe-interactive` and solution cells `hide-input`"): 1101 exercise cell 10 is tagged `hide-input, skip-execution`, which hides the prompt, and its solution cell 11 has no tags, so the solution shows. In 1102, exercise cells 9, 13, and 21 and solution cells 10, 14, and 22 have no tags at all, so every solution is visible. Cell 10's header is also lowercase `### solution`.
✗ Learning objectives vs doc scope: ORGANIZATION has no learning-objectives section (so there is nothing to compare there), but the landing page goals (1100 cell 3) leave out most of the 1102 required core: generator expressions and memory efficiency, `any()`/`all()` short-circuiting, `itertools`, `yield from`, and the async bridge.
✗ Taught-goal mismatch: landing goal 5 ("Design multi-step data pipelines using chained generators"), the landing intro bullet "Lazy pipelines for streaming data" (1100 cell 1), and 1102's section goals ("large-file processing", "Chain generators into efficient **data pipelines**", cell 2) are not taught anywhere in 1101 or 1102. No pipeline or file-streaming example exists. Neither doc lists pipelines in the required core (MATERIALS lists a "streaming-data mini-project" only as enrichment).
✗ Stale homework description: MATERIALS "Practice and Assessment Pack" says Homework is "Implement an infinite sequence generator; benchmark memory use vs list". `assignments/homework.ipynb` has neither task (its coding questions are iter/next, countdown generator, generator-expression sum, StopIteration handling, and repeat_each). The doc is stale, or the homework is missing planned items.
✓ MATERIALS lab task list matches the 5 lab questions (threshold iterator, batching iterator, running max, genexpr with islice/any/all, infinite generator with islice).
✗ Material files: `materials/11-iter-gen/example.db`, `school.db`, and `users_demo.db` exist and are listed, but both docs call them "archived SQLite example database(s)". No Chapter 11 notebook or assignment references them (grep for `.db` and `materials/` found nothing). They are leftovers from the archived `0701-databases.ipynb` and belong in `materials/_archived/ch11-iter-gen-stray/`, not in the active materials list.
✓ No old chapter or section numbers in the docs. The archived-file note (`materials/_archived/ch11-iter-gen-stray/0701-databases.ipynb`) is accurate.

### Slide deck ↔ chapter
✗ Stale numbering: the title slide (overview.md line 37) reads "*12.0 Intro · 12.1 Iterators · 12.2 Generators*", but the section slides use 11.1/11.2. It should read 11.0/11.1/11.2. The rendered `overview.html` contains the same text.
✗ Learning goals: the deck has no learning-goals slide, so none of the 5 landing-page goals (1100 cell 3) appear in the slides.
✗ Section headings (1102): the chapter has Generator Functions, Generator Expressions, any()/all(), Practical Use Cases (infinite sequences), `yield from`, More itertools, and Bridge: Async Iteration. The deck has Generator Functions (with generator expressions folded into a "Memory efficiency" column), any/all, and "itertools Highlights" (with the Fibonacci infinite sequence folded in). `yield from` appears only as a single row in the Quick Reference, and the async-iteration bridge (required core; also on the MATERIALS slide checklist) is missing entirely.
✓ Section headings (1101): the Iterator Protocol (with built-in lazy iterators) and Custom Iterator Classes match the order in 1101.
✗ Topic not in chapter: the "itertools Highlights" table (line 198) and the Quick Reference (line 241) present `chain(a, b)`, which the chapter never teaches (see Planning docs).
✓ Key examples still exist: `Countdown`, `count_up_to`, the `sys.getsizeof` list-vs-genexpr comparison (though hidden in 1102 cell 6), the any/all `data`/`scores` validation example, Fibonacci with `islice`, and `combinations`.
✓ Rendered HTML (`overview.html`, Aug 31) is newer than the source (`overview.md`, Jun 4).

### Preview ↔ chapter
✓ All 10 questions map to concepts taught in 1101/1102: iterable, iterator, protocol, `iter`, `StopIteration`, lazy iterator, generator function, `yield`, generator expression, `itertools`. None test a moved topic, and all suit pre-reading.
(Note: q10's phrase "efficient iterator pipelines" echoes the untaught pipeline concept, but the correct answer, `itertools`, is covered.)

### Homework ↔ chapter
✗ Coverage gap: the 10 questions (5 T/F + 5 coding) cover only iter/next, StopIteration, generator functions, and generator expressions. Nothing assesses custom iterator classes, the built-in lazy iterators (`enumerate`/`zip`/`map`/`filter`/`reversed`), `itertools`, `yield from`, or `any()/all()` short-circuiting. The ladder is flat (every coding item is a 2-4 line task, and Q10 is not harder than Q7). It is noticeably easier than the lab.
✗ Misleading title: Q10 "Chain generated values" (cell 19) suggests `itertools.chain`, but the task is a nested-loop generator `repeat_each` that chains nothing. Given that `chain` is itself untaught, rename it (for example, "Repeat values with a generator").
✗ Boilerplate feedback: the T/F solution cells (2, 4, 8 and 6, 10) say "This workplace case matches the chapter concept" or "The case reverses or overstates the chapter concept", but none of the five statements is a workplace case. The explanation text does not explain the answer.
✓ Q4 mentions "pipeline", but its answer depends only on generator-expression laziness, which is taught.
✓ No problem references a removed example or dataset.

### Lab ↔ chapter
✓ All 5 questions use the same `sales` list as the 1102 any()/all() exercise (cell 9). They exercise custom iterators, a generator function, genexpr + `islice`/`any`/`all`, and an infinite generator bounded by `islice`, all of which are current chapter topics. The grader panel (cell 11) has q1-q5, matching the 5 questions, and the submission instructions match the lab structure.
(Minor, not counted: solution cell 6 uses `current_max == None` rather than `is None`. Lab Q4/Q5 rely on `islice`, which the text explains only in code comments; see Planning docs.)

### Glossary ↔ content
✓ 19 of 20 glossary terms (1100 cell 5) are introduced in 1101/1102.
✗ Glossary term 18, `chain`, is not introduced or used in any content notebook.
✗ Key terms taught but missing from the glossary: generator object (1102 cells 3-4), short-circuit evaluation (cell 7), `reversed` (1101 cell 5; listed in landing goal 2), `count`/`cycle`/`combinations`/`permutations` (cells 18-21, graded in an exercise), async generator and `async for` (cell 23; only the umbrella term "Async iteration" is present).

### Summary
Sync issues: 25
  - ORGANIZATION.md and MATERIALS.md missing `orphan: true` front matter
  - `itertools.chain` is required core in both docs but untaught; 1102 cell 18 falsely says "already covered"
  - `itertools.islice` taught only via code comments (1102 cell 12), with no prose explanation
  - 1102 `## Practical Use Cases` heading not reflected in docs
  - ORGANIZATION 1102 exercise order stale (any/all, Running Total, itertools in the notebook)
  - MATERIALS 1102 exercise list omits the itertools exercise and miscounts the genexpr task
  - itertools combinatorics labelled enrichment but taught and graded as core; docs self-inconsistent
  - `next(genexpr)` and `send()`/`throw()` content appears in neither doc list
  - 1101 exercise (cell 10) requires `itertools.islice` before it is taught (1102)
  - 1102 exercise (cell 9, part 3) requires `next(genexpr)` shown only in a hidden cell
  - `sys.getsizeof` memory demo (1102 cell 6) hidden and skip-execution
  - Exercise/solution tags wrong in all 4 exercises; all solutions visible; 1101 prompt hidden
  - Landing goals omit most 1102 required core (genexpr, any/all, itertools, yield from, async)
  - Landing goal 5, the intro bullet, and 1102 section goals promise pipelines/large-file processing that is never taught
  - MATERIALS homework description (infinite generator + memory benchmark) does not match homework.ipynb
  - Three archived, unused `.db` files listed as Chapter 11 material files
  - Slides title uses stale "12.0/12.1/12.2" numbering
  - Slides have no learning-goals slide
  - Slides lack `yield from` and async-bridge slides; genexpr and infinite sequences folded into other slides
  - Slides present `itertools.chain`, which the chapter does not teach
  - Homework omits custom iterators, built-in lazy iterators, itertools, yield from, any/all; flat difficulty ladder
  - Homework Q10 title "Chain generated values" is misleading
  - Homework T/F feedback refers to nonexistent "workplace case"
  - Glossary term `chain` not introduced in content
  - Glossary missing generator object, short-circuit evaluation, `reversed`, itertools combinatorics, async generator/`async for` (counted as one)

### Top fixes
1. Teach `itertools.islice` and `itertools.chain` in prose in 1102: add a short markdown and code subsection under Practical Use Cases before cell 12, and fix cell 18's "already covered". This resolves the untaught required core, the slide/glossary `chain` mismatches, and the lab's reliance on `islice`.
2. Fix exercise and demo cell tags: tag exercise cells 1101/10 and 1102/9, 13, 21 as `thebe-interactive`, and solution cells 1101/11 and 1102/10, 14, 22 as `hide-input`. Remove `hide-input`/`skip-execution` from the 1102 cell 6 memory demo so `sys.getsizeof` and `next(genexpr)` are taught before the cell 9 exercise.
3. Reconcile the pipeline promise: either add a short chained-generator pipeline section to 1102 (for example, reading lines, filtering, and transforming the sales data), or drop pipelines and large-file processing from landing goal 5, the 1100 intro bullets, and 1102's section goals. Then rewrite the landing goals to cover genexpr, any/all, itertools, `yield from`, and the async bridge.
4. Update the slides: fix the "12.x" numbering, add a learning-goals slide that mirrors 1100, and add `yield from` and async-bridge slides in chapter order.
5. Sync the planning docs: add `orphan: true` front matter, fix the 1102 exercise order and list in both docs, settle whether the itertools combinatorics are core or enrichment, update the MATERIALS homework description (or add the infinite-generator and memory-benchmark items to the homework), and remove or move the unused archived `.db` files from Material Files.
