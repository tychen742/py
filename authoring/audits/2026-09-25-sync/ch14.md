---
orphan: true
---

## Sync Audit — Ch14: Algorithms

Scope: `chapters/14-algorithms/` (1400–1403, assignments), `_html_extra/chapters/14-algorithms/overview.md`, `_toc.yml` lines 218–230. Audited working tree as of 2026-09-25. The "Planned Additions" items in ORGANIZATION.md (1404 dynamic programming; two-pointer/sliding-window in 1402) are known and not flagged.

### Planning docs ↔ chapter

✗ **Front matter.** Neither `ORGANIZATION.md` nor `MATERIALS.md` starts with MyST front matter `orphan: true`. Both open directly with `# Chapter 14 ...`.

✓ **Notebook inventory.** 1400–1403 and `assignments/{index,preview,lab,homework}` exist, are named in both docs, and match `_toc.yml` entries. Note: `chapters/14-algorithms/TODO.md` is also in the folder (see stale references below).

✓ **Material Files.** Neither doc has a Material Files section, and there is no `materials/*14*` folder, so there is nothing to reconcile.

✓ **Learning objectives in ORGANIZATION.md.** None are present, so there is nothing to compare. (The landing-page goals themselves are a problem; see below.)

✗ **1400 described as a stub (docs are stale and contradict each other).** ORGANIZATION.md (file list, Scope item 1, File Roles) and MATERIALS.md (`1400` required core, the Note, Delivery Order) call 1400 a "chapter intro stub: title and chapter flow map only". ORGANIZATION.md's Required Core says "Full landing page". The notebook is a full landing page: overview bullets, Video, Learning Goals, Chapter Flow, a 20-term Glossary, and a Slides link (cells 1–6). The "stub" wording is stale.

✗ **Landing-page learning goals do not cover two-thirds of the chapter.** In 1400, cell 3, all five goals are 1401 topics: IPO, control patterns, Big O, design checklist. Goal 5 reads "Prepare for search and sorting notebooks", which is not an outcome. No goal covers linear/binary search, `bisect`, hash lookup, sorting algorithms, stability, or benchmarking. By contrast, 1402 cell 3 and 1403 cell 3 each list their own section Learning Goals ("Implement linear and binary search correctly", "Implement insertion, merge, bubble, and quicksort", "Explain stability and memory trade-offs", and others), and these do not appear in 1400.

✗ **1401 section sequence: search implementations taught but not listed, and they overlap with 1402.** ORGANIZATION.md item 2 lists concepts only. Under `## Complexity and Big O`, 1401 actually teaches `### Linear Search` (cells 34–40), `### Binary Search` (cells 41–48, including a trace table and a 1024-node decision tree), and `### Comparing Linear and Binary Search` (cells 49–64: average-case derivation, recursive-space note, pandas/seaborn plot). These are also the first two `##` sections of 1402, which conflicts with ORGANIZATION.md's own rule "Use this sequence to avoid overlap". 1401 also defines three different `binary_search` functions (cell 32 with 0-based bounds, cell 45 with 1-based bounds returning `(mid+1, step)`, and cell 57 `binary_search_steps`), and 1402 cell 13 defines a fourth with a different contract.
  - Correctness side effect (verified by running it): the cell 45 version uses `low, high = 1, len(arr)` with `arr[mid]`. It never checks `arr[0]` (`binary_search(range(1,101) list, 1)` returns `(-1, 6)`), and it raises `IndexError` for any target larger than the maximum. Cell 64 calls it on 0-based `list(range(1_000_000))` and prints the returned `(mid+1, step)` tuple as an "index".
  - Also taught but not listed: `### Properties of an Algorithm` (Knuth: finiteness, definiteness, effectiveness, determinism; cell 12).

✗ **1401 topic order differs from ORGANIZATION.md.** The doc lists: control patterns → complexity → best/avg/worst → benchmarking → **design checklist** → selection guide. The notebook order is: What Is an Algorithm → Implementing Algorithms (**design checklist**, cell 15, *then* Control Patterns, cell 18) → Complexity and Big O (best/avg/worst appears only in the search summary, cell 58) → Benchmarking → Algorithm Selection Guide. The doc is stale.

✗ **1402 section sequence: three `##`/`###` topics taught but not listed in ORGANIZATION.md.** These are `### Recursive Binary Search` (cells 15–18, including a recursion admonition with `countdown`/`factorial`), `## Correctness Checks` with `### Finding First Duplicate` / `binary_search_first()` (cells 37–42), and `## Timing Comparison` (one-lookup vs many-lookup workflow benchmark, cells 43–46). ORGANIZATION.md item 3 stops at "Search trade-offs". MATERIALS.md does mention "Basic timing comparison" and "correctness checks", so the two docs disagree with each other. ORGANIZATION.md is the stale one.

✗ **1403 section sequence: several `##` sections not listed, and the order differs.** Taught but not listed: `## Design Paradigms` (cell 4), `## Sorting Concepts & Intuition` (animated GIFs, cell 16), `## APIs & Complexity` (`### API Contracts`, `### Complexity Growth`, `### Sorting Cheat Sheet`, which also covers selection, shell, smooth, and heap sort plus Timsort), `### Validating Correctness` (randomized testing), and `### Beyond Comparison` (counting sort, cell 48). Order: ORGANIZATION.md places "Stability and in-place vs out-of-place decision criteria" after merge/quick. The notebook teaches stability first (`### Stability and key=`, cell 10) and in-place vs out-of-place in Design Paradigms (cell 4) and API Contracts (cell 37).

✗ **Required-core vs enrichment split does not match what the notebook teaches as core.** Both docs put bubble sort and quicksort (and counting sort) in the Enrichment track. 1403 teaches all four under `## Core Sorting Algorithms`, lists "Implement insertion, merge, bubble, and quicksort" as a section Learning Goal (cell 3), and teaches counting sort in the main text (cell 48) with a required-looking exercise (cell 49). Content that appears in neither list: Knuth properties (1401), recursive binary search and `binary_search_first` (1402), API contracts and the extended cheat-sheet algorithms (1403). Either reclassify in the docs or mark these sections optional in the notebooks. 1402 cell 15 already says "(*optional topic)".

✗ **Exercise lists.** Neither doc names or counts the exercises. The actual exercises are:
  - 1401 (5): Find the Minimum Value, Count Passing Scores, Binary Search Implementation, Benchmark List vs. Set Membership, Choose the Right Algorithm.
  - 1402 (6): Find Minimum Value, Searching a Sorted List, Using the bisect Module, Hash-Based vs. List Lookup, First Occurrence with Duplicates, plus the markdown "Exercise: Trace …" in cell 14.
  - 1403 (6): Sort by Word Length, Count Comparisons, Count Swaps, Merge Two Sorted Lists, Three-Way Partition, Counting Sort.

  MATERIALS.md 1401 says the exercises "classify growth rates, choose algorithms …, interpret small timing results". Only one sub-item (cell 65, part 1) classifies growth, and two exercises (Find Minimum, Count Passing) are not described. ORGANIZATION.md's "`1401`, `1402`, and `1403` each have exercises per section" is false: 1402 `## Timing Comparison` and `## Search Strategy Summary`, and 1403 `## Design Paradigms`, `## Sorting Concepts & Intuition`, and `## APIs & Complexity` have none.

✗ **Duplicate exercise, and one that is not a search exercise.** 1402 cell 8 "Exercise: Find Minimum Value" (`min_value`) repeats 1401 cell 13 "Exercise: Find the Minimum Value" (`find_min`) and lab Question 1. It sits in `## Linear Search` but practices a min scan, not search.

✗ **Exercise uses a concept not yet taught.** In 1401 cell 74, "Choose the Right Algorithm" item 4 expects "Insertion sort, O(n) best case" for nearly sorted data. At that point insertion sort exists only as one row of the selection table (cell 73). It is not explained until 1403 `### Insertion Sort`.

✗ **Topic practiced only in an exercise.** For counting sort, 1403 cell 48 describes the idea in prose (O(n + k), value-as-index), but no worked code example appears in the text. The only implementation is the exercise (cell 49) and its solution. Add a short worked example, or reframe the exercise as enrichment.

✗ **Example uses libraries never taught.** 1401 cell 59 (a visible code cell, not hide-input) imports `pandas` and `seaborn` (`pd.DataFrame`, `.loc`, `sns.scatterplot`). A grep of all chapter notebooks in `_toc.yml` order (ch01–ch13) finds no `import pandas` or `import seaborn`. Either tag the cell `hide-input` as a figure generator or replace it with plain matplotlib.

✗ **Stale references in notebooks.**
  - 1402 cell 11: "O(log n) (as developed in **10.1**)". This should be 14.1.
  - 1403 cell 45: "Connection to **Chapter 08**" (ch08 is Strings) and "See also `**1502**-searching.ipynb`". This should be 1402.
  - 1403 cell 47 code comment: "see also **1502**-searching.ipynb".
  - 1403 cell 39: "The comparison table **above** names the Big O of each algorithm". The Sorting Cheat Sheet is below, in cell 41.
  - 1401 cell 25 exercise says "selection (if)". The text in cell 20 calls this pattern "Decision".

✗ **Stale `TODO.md` in the chapter folder.** It references notebooks 1000/1001/1002 (old numbering) and says "I've removed graph/dp/greedy from coverage", which now conflicts with the DP Planned Addition in ORGANIZATION.md. Neither planning doc names it.

### Slide deck ↔ chapter

✗ **Stale numbering on the title slide.** It reads "*15.0 Intro · 15.1 Algorithm Concepts · 15.2 Searching · 15.3 Sorting*". The section dividers correctly say 14.1/14.2/14.3.

✗ **Learning goals.** The deck has no Learning Goals slide, so none of the five landing-page goals appears, including control patterns and the design checklist.

✗ **Section headings: many chapter `##` sections have no slide.**
  - 1401: `What Is an Algorithm?`, `Implementing Algorithms` (design checklist, control patterns), and `Algorithm Selection Guide` are missing. The deck goes straight to Big O.
  - 1402: `Correctness Checks` (`binary_search_first`) and `Timing Comparison` (one vs many lookups, preprocessing cost) are missing. `Search Strategy Summary` survives only as a small table.
  - 1403: `Design Paradigms`, `Stability and key=` (students demo), bubble sort and quicksort code, `API Contracts` (in-place vs new list), `Evaluation` (input-pattern benchmark), and `Beyond Comparison` (counting sort) are missing. The only sorting slides are "Built-in Sort & Simple Algorithms" (built-in, insertion, merge) and the comparison table.
  - The best/worst/average table is placed on the Benchmarking slide. In the chapter it belongs to Complexity and Big O.

✗ **Figures and tables not preserved (AGENTS.md rule).** The following carry conceptual structure but have no slide equivalent: the 7-class Big-O chart (1401 cell 31), the "guess 42" binary-search trace table (1401 cell 42), the n vs log2 n step table and the 1024 decision tree (1401 cells 43, 53), the linear-vs-binary scatter plot (1401 cell 59), the Algorithm Selection Guide table (1401 cell 73), the `sorted()` vs `list.sort()` table (1403 cell 5), the four sorting animations (1403 cell 16), and the input-pattern benchmark table (1403 cell 47). The Sorting Cheat Sheet is partially preserved as the comparison table.

✗ **Slide contradicts the chapter.** The Benchmarking slide comment reads "`time.perf_counter` — **wall clock**, high precision". 1401 cell 67 says `time.time()` is the wall-clock timer, and `perf_counter()` is a performance counter that "is unaffected by OS clock changes".

✗ **Renamed or divergent code.**
  - The slide's `sorted_contains()` is the notebook's `bisect_search()` idiom (1402 cell 27, heading "The bisect_search() Idiom").
  - The slide's `insertion_sort` returns `None`, but 1403 cell 20 returns `arr`, and API Contracts relies on that.
  - The slide's `merge_sort` returns `lst` itself for the base case, while 1403 returns `items[:]` so that it never aliases input.
  - The slide's `timeit` uses a string statement, while the chapter teaches the `lambda` form (1401 cells 68–69).

✓ **Datasets and examples that are featured still exist.** `linear_search`, `binary_search`, `bisect_left`/`insort`, set lookup, `sorted(key=, reverse=)`, and the merge helper all appear in 1402/1403. The rendered `overview.html` (Aug 31) is newer than `overview.md` (Jun 4), so the HTML is current with its source.

### Preview ↔ chapter

✓ All 10 questions map to glossary terms taught in the chapter: algorithm, correctness, complexity, Big O, constant/linear/logarithmic time, linear search, binary search, stability. No question tests a moved topic, and difficulty suits pre-reading.
  - Observation (not counted): 7 of 10 questions target 1401 terms. Glossary terms `bisect`, hash-based lookup, in-place sort, and divide and conquer are not tested, and sorting gets only the stability question.

### Homework ↔ chapter

✗ **The MATERIALS.md homework spec is not reflected.** MATERIALS.md "Practice and Assessment Pack" says homework = "select algorithm for 3 scenarios and justify with Big O + constraints". `homework.ipynb` has 5 true/false questions plus 5 coding questions (linear search, binary search, insert into a sorted list, `sorted` vs original, count comparisons), and no scenario or justification item. Either the doc is stale or the homework is missing its complexity-reasoning task.

✗ **Coverage and difficulty ladder.** No homework item touches `bisect`, hash lookup, merge sort (a required-core implementation per both docs), stability/`key=`, or benchmarking. The coding ladder does not ascend: Q7 (binary search) and Q8 (insert into sorted list) are followed by Q9 (call `sorted`) and Q10 (count loop iterations), which are the easiest items. Q6 and Q10 also duplicate lab Q5 and 1401 cell 37 (linear search with step counting).

✗ **Uninformative solution rationales.** Q1, Q2, and Q4 solutions all read "This workplace case matches the chapter concept". Q3 and Q5 read "The case reverses or overstates the chapter concept". Q2 (Big O) and Q4 (binary search) are definitions, not workplace cases, and none of the rationales explains why. For example, Q3 should say that linear search works on unsorted data.

✓ All referenced functions and patterns (`enumerate` scan, while-loop binary search, `sorted` non-mutation) are present in the current chapter, and no removed dataset is referenced.

### Lab ↔ chapter

✓ The lab tasks match MATERIALS.md's lab list exactly: min/max in one pass, binary search, `bisect` insert-and-report, insertion sort, and comparison counting in linear search. All techniques are taught in 1401–1403.

✓ **Submission structure.** 5 `### Question N` thebe cells; the grader collects `.slice(0, 5)` and shows q1–q5 rows; `data-assignment-answers="ch14-lab"` follows the `chNN-type` naming.

✗ **The lab contradicts the chapter's API contract for the same function name.** Lab Question 4 asks for `insertion_sort(data)` that "returns a NEW sorted list … original data list is never mutated". 1403 `### API Contracts` (cell 37) teaches that `insertion_sort` sorts **in place** and uses that as the key contrast with `merge_sort`/`sorted()`. Rename the lab function (for example, `insertion_sorted`) or state the contract change explicitly. Also, no lab task practices merge sort, which both docs list as a required implementation.

### Glossary ↔ content

✓ All 20 glossary terms are introduced in content notebooks. Constant, linear, and logarithmic time are taught through the growth-class table (1401 cell 28) rather than by those exact phrases.

✗ **Key terms introduced in the notebooks but missing from the glossary:**
  - Pseudocode (1401 cell 17)
  - Control patterns: sequence / decision / repetition (1401 cell 20; this is also landing goal 2)
  - Best / average / worst case (1401 cell 58, 1402 cell 4)
  - Linearithmic and quadratic time (1401 cell 28)
  - Insertion sort, bubble sort, merge sort, quicksort (1403 cells 19–33)
  - Pivot (1403 cell 32)
  - Timsort (1403 cell 5)
  - Out-of-place sort (1403 cells 4, 37)
  - Insertion point (1402 cell 23)
  - Counting sort (1403 cell 48)

### Summary
Sync issues: 26
  - Planning docs (15): front matter missing (both docs); 1400 described as a stub; landing goals omit search and sorting; 1401 search content not listed and duplicating 1402 (plus a buggy 1-based `binary_search`); 1401 order stale; 1402 sections not listed and ORGANIZATION.md vs MATERIALS.md disagree; 1403 sections not listed and order stale; core/enrichment split mismatched; exercise lists absent or inaccurate; duplicate Find-Minimum exercise; forward-referenced insertion sort in a 1401 exercise; counting sort practiced only in an exercise; untaught pandas/seaborn in 1401; stale refs (10.1, Chapter 08, 1502, "table above"); stale TODO.md
  - Slides (6): 15.x numbering; no learning goals; missing sections; figures not preserved; perf_counter "wall clock" error; renamed/divergent code
  - Homework (3): MATERIALS spec not reflected; coverage gaps and flat ladder; boilerplate rationales
  - Lab (1): `insertion_sort` contract contradicts 1403
  - Glossary (1): about 13 key terms missing

### Top fixes
1. **Fix 1401's search block.** Remove or trim the duplicated linear/binary-search implementations in 1401 (keep the complexity intuition and point to 1402), and fix or delete the 1-based `binary_search` in cell 45. It misses index 0, raises `IndexError` for targets above the maximum, and cell 64 misuses it on 0-based data. Then list what remains in ORGANIZATION.md.
2. **Rewrite the 1400 Learning Goals** so they cover the whole chapter (search strategies incl. `bisect`/hash, sorting algorithms, stability/in-place, benchmarking) and replace "Prepare for search and sorting notebooks". Add the same goals as a slide.
3. **Rebuild the slide deck to match the `##` structure.** Fix the 15.x title numbering and the perf_counter "wall clock" error. Add the missing sections: What Is an Algorithm/design checklist, Selection Guide, Correctness/Timing Comparison, Design Paradigms, API Contracts, Evaluation, and counting sort. Carry over the Big-O chart, trace table, selection table, and sorting animations, then regenerate `overview.html`.
4. **Bring ORGANIZATION.md and MATERIALS.md up to date.** Add `orphan: true`, drop the "stub" wording for 1400, list the actual `##` sequence and the 17 named exercises for 1401–1403, and reconcile core vs enrichment for bubble, quick, and counting sort. Delete or archive the stale TODO.md.
5. **Align the assignments with the required core.** Add a homework item on scenario-based algorithm selection with Big O justification (per MATERIALS.md) and one on merge sort or `bisect`/hash lookup, reorder the coding ladder, and write real T/F rationales. Rename lab Q4's non-mutating `insertion_sort` so it does not contradict 1403's in-place contract.
